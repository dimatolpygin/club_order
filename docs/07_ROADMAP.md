# Дорожная карта реализации — Telegram-бот клуба «11:11»

10 этапов от пустого репозитория до прод-готовности. Каждый этап заканчивается
**демонстрируемым результатом** (что можно потрогать руками в боте/на сервере).

Ключевая идея: **MVP-first**. Этап 1 = тонкая, но живая вертикаль (бот отвечает,
показывает экраны из навигации). Всё остальное наслаивается без перепиливания базы.

> **Где смотреть прогресс**: текущий статус всех этапов — в [`STATUS.md`](STATUS.md).
> Внутри каждого этапа ниже — чекбоксы конкретных acceptance-пунктов. Закрытие этапа
> сопровождается git-тегом `stage-N-done`.

> **Цикл 1 (этапы 0–10) — закрыт** (бот подписки 11:11 в проде). **Цикл 2 (этапы 11–37) —
> закрыт** — обновление «Фокус.Энергия» (билеты, рефералка, скидки) + веб-админка.
> **Цикл 3 (этапы 38–50)** — правки заказчика от 09–11.07.2026 (меню, рефералка на все
> покупки, бонусы, уведомления админам, тарифы/скидки, Йога и Консультации, рассылка по
> событию, согласие на ПД). Детали циклов — в конце файла (разделы «Цикл 2», «Цикл 3»).

## Базовые архитектурные решения (зафиксированы с заказчиком)

- **Модель цен — авто-ступени по местам, ступени СКРЫТЫ от пользователя.** Внутри —
  ценовые ступени-брекеты по числу занятых мест (первые 11 мест — 1111 ₽/мес, 12–22 —
  2222 ₽/мес, …, дальше «Стандарт»), значения/лимиты задаёт админ. Текущая помесячная
  ставка определяется АВТОМАТИЧЕСКИ по числу активных участников клуба: занято <11 →
  ставка ступени 1; с 12-го участника → ступень 2 и т.д. **Пользователь видит только
  длительности** (1/3/6/12 мес или строго 1) с ценой = текущая_ставка × месяцы — без
  каких-либо «ступеней» и чисел мест на экране. За пользователем фиксируется помесячная
  ставка на момент оплаты; итог к оплате = ставка × число месяцев.
- **Фиксация цены.** Пока юзер продлевает без перерыва — его ставка сохраняется. Перерыв
  (подписка закончилась и не продлена) → ставка сбрасывается, новая подписка по актуальной
  доступной ступени.
- **Закрытое пространство — группа (супергруппа с топиками).** Бот — админ группы,
  одобряет join-request по инвайт-ссылке только оплатившим, кикает по окончании подписки.
- **Оплата — ЮKassa, без вебхука.** Создание платежа + фоновый polling pending-платежей
  (домена нет). Чек 54-ФЗ: email-заглушка из настроек, но архитектурно заложен сбор и
  сохранение реального email клиента (колонка в БД + единая точка формирования receipt).
- **Промокоды — полноценно в MVP** (отдельный этап).
- **Стек**: Python 3.12, aiogram 3, Postgres 16 (отдельная схема `club_bot`, чужие таблицы
  не трогаем), Redis (FSM + кеш), Loguru, Alembic (автоприменение миграций при старте),
  APScheduler (напоминания + polling платежей + проверка окончаний), Docker/Compose,
  hot-reload в dev, polling, CI/CD GitHub Actions → репо `dimatolpygin/club_order`.

| #  | Этап | Главный артефакт | Зависит от |
|----|------|------------------|------------|
| 0  | Каркас репозитория | `docker compose up` поднимает бот+pg+redis, `/start` отвечает заглушкой, миграции применяются | — |
| 1  | Навигация и инфо-экраны | Все статичные экраны навигации с HTML и кнопками, FSM-каркас + лог состояний в БД | 0 |
| 2  | Тарифы и места (админ-конфиг) | Экран «Выбрать тариф» строит кнопки из БД, скрывает ступень при исчерпании мест; админ меняет цены/лимиты/длительности на лету | 1 |
| 3  | Оплата ЮKassa + polling | Реальный тестовый платёж → запись подписки (start/end/зафикс. цена), «Моя подписка» показывает данные | 2 |
| 4  | Доступ в группу (join-request) | Бот одобряет заявку оплатившего, отклоняет заявку без активной подписки | 3 |
| 5  | Фиксация цены, продление, авто-удаление | Продление сохраняет цену; обрыв подписки → кик из группы + сброс цены | 4 |
| 6  | Напоминания (крон 3/1/0 дней) | Бот шлёт напоминания за 3/1/0 дней до окончания | 5 |
| 7  | Промокоды | Валидный промокод → спец-цена; невалидный/истёкший обработаны; админ создаёт код | 3 |
| 8  | Админ-панель `/admin` | `/admin` список команд; ручное добавление оплативших; правка настроек; просмотр застрявших в FSM | 2,5,7 |
| 9  | Деплой CI/CD | push в `master` → автодеплой на сервер, бот живой в проде | 8 |

---

## Этап 0 — Каркас репозитория

**Цель**: пустой, но рабочий проект, в котором есть вся инфраструктура для следующих этапов.

**Что делаем**:
- Структура `src/` (config, logger, db, cache, main, handlers/, services/), `migrations/` (Alembic), `docs/`.
- `config.py` на pydantic-settings: токен, admin_ids, схема БД `club_bot`, DSN Postgres/Redis, настройки ЮKassa, email-заглушка для чека.
- Логирование через **Loguru**: каждое действие пользователя (входящие, кнопки, команды) с датой/временем, username, user_id, first_name и текстом ответа — на русском.
- Пул Postgres (asyncpg) с `search_path=club_bot,public` — чужие таблицы не задеваются.
- Redis-подключение, RedisStorage для FSM.
- **Alembic** настроен на схему `club_bot`, автоприменение `upgrade head` при старте.
- Dockerfile, `docker-compose.yml` (прод: бот+pg+redis), `docker-compose.dev.yml` (локально, без проброса портов pg/redis на хост — на ПК уже заняты).
- Hot-reload в dev (watchfiles/`watchmedo`) — аналог `npm run dev`.
- `/start` отвечает заглушкой, бот работает в режиме polling.

**Критерии приёмки** (закрытие → tag `stage-0-done`):
- [x] `docker compose -f docker-compose.dev.yml up --build` поднимает бот + Postgres + Redis без ошибок
- [x] Alembic при старте создаёт схему `club_bot` и применяет миграции (в логе видно `upgrade head`)
- [x] Существующие чужие таблицы в БД на ПК не затронуты (бот работает только в схеме `club_bot`)
- [x] `/start` в боте отвечает заглушкой, в консоли — лог на русском с username/user_id/first_name/текстом ответа
- [x] Изменение `.py`-файла в dev пересобирает/перезапускает бота без ручного рестарта

---

## Этап 1 — Навигация и инфо-экраны

**Цель**: живая навигация бота по `навигация_бот.txt` — все информационные экраны.

**Что делаем**:
- Тексты экранов (HTML-разметка: `<b>`, `<i>` и т.д.) из навигации: приветствие /start, «Что внутри клуба», «Правила участия», «Поддержка» (+ подэкраны), «Главное меню», финальное приветствие.
- Inline-клавиатуры и роутинг переходов между экранами.
- FSM-каркас (aiogram + RedisStorage) и **таблица лога FSM-состояний в БД** (`fsm_log`/`user_states`) — фиксирует, на каком шаге находится/застрял пользователь.
- Таблица `users` в схеме `club_bot` (tg_id, username, first_name, created_at, email — под будущий чек).
- Middleware-логгер всех действий.

**Критерии приёмки** (закрытие → tag `stage-1-done`):
- [x] `/start` показывает приветствие «11:11» с кнопками [Вступить в клуб] [Что внутри] [Правила] [Поддержка]
- [x] Все информационные экраны открываются и возвращаются назад корректно (HTML-форматирование отображается)
- [x] Новый пользователь записывается в таблицу `users` (виден прямым SQL)
- [x] При переходах по экранам в таблице FSM-лога появляется текущее состояние пользователя
- [x] «Поддержка» с подэкранами (проблема с оплатой / нет доступа к чату) работает

---

## Этап 2 — Тарифы и места (админ-конфигурируемые)

**Цель**: динамический экран тарифов с лимитами мест и редактированием админом на лету.

**Что делаем**:
- Таблицы: `price_tiers` (название, помесячная ставка, лимит мест-брекет, порядок, активность), `durations` (месяцы, активность), `subscriptions` (для подсчёта активных участников).
- **Движок текущей ставки**: считает активных участников клуба и по кумулятивным брекетам ступеней определяет текущую помесячную ставку (занято <11 → ступень 1; 12-й участник → ступень 2; …; дальше «Стандарт»). Ступени пользователю НЕ показываются.
- Экран подписки: пользователь видит только **длительности** (1/3/6/12 мес), цена кнопки = текущая_ставка × месяцы. Цена меняется сама при заполнении мест.
- Админка — **пошаговый FSM-сценарий** (инлайн-меню → «введите цену», «введите лимит» и т.п.): правка цен/лимитов/активности ступеней, длительностей, ручное добавление участника. Изменения без перезапуска (кеш в Redis с инвалидацией).

**Критерии приёмки** (закрытие → tag `stage-2-done`):
- [x] Пользователь видит только длительности с ценой (без ступеней/чисел мест), цена = текущая ставка × месяцы
- [x] Текущая ставка определяется автоматически по числу активных участников (при заполнении мест ступени цена сама перескакивает на следующую)
- [x] Админ через пошаговый FSM-сценарий меняет цену/лимит ступени → цена на экране подписки обновляется без рестарта
- [x] Админ включает/выключает длительности (например, оставить только 1 мес) → на экране остаются только активные
- [x] Итоговая сумма к оплате = текущая ставка × число месяцев (видно на экране сводки)

> Сверх плана этапа (по запросу заказчика, объём этапа 8 вынесен раньше): админ-карточка участника (поиск по tg_id, статус подписки/доступа), рассылка по базе (без фото), аннулирование подписки участника.

---

## Этап 3 — Оплата через ЮKassa + polling pending

**Цель**: реальная оплата создаёт активную подписку с зафиксированной ценой.

**Что делаем**:
- Создание платежа ЮKassa (idempotence key, amount, capture, return_url, metadata: tg_id/tier/duration), сохранение `yookassa_payment_id`, `confirmation_url`, статуса.
- `receipt` с email: заглушка из настроек, но через единую функцию `build_receipt(user)` с колонкой `users.email` — точка лёгкой замены на реальный сбор.
- Экраны: «Оплата» [Оплатить][Проверить оплату][Отмена], «Оплата в ожидании», «Успех», «Ошибка».
- Таблицы `payments` и `subscriptions` (tg_id, tier_id, fixed_price, start_date, end_date, status).
- Фоновый poller (APScheduler) опрашивает pending-платежи ЮKassa; при `succeeded` → активирует подписку (start=now, end=start + месяцы), пишет зафиксированную помесячную ставку.
- Кнопка «Проверить оплату» дёргает проверку немедленно.

**Критерии приёмки** (закрытие → tag `stage-3-done`):
- [x] «Оплатить» создаёт платёж в ЮKassa и выдаёт ссылку на оплату
- [x] После реальной тестовой оплаты подписка активируется (запись в `subscriptions` с start/end/fixed_price видна по SQL)
- [x] Polling сам подхватывает оплаченный платёж без нажатия «Проверить оплату» (в пределах интервала)
- [x] «Моя подписка» показывает статус/даты/зафиксированную цену/дни до конца
- [x] Повторная оплата того же платежа не создаёт дубль подписки

> Сверх плана этапа (по запросу заказчика на живом тесте): (1) цена за период —
> МАТРИЦА ступень×период (админ задаёт абсолютную цену за 3/6/12 мес в каждой ступени,
> иначе fallback ставка×месяцы; миграция 0004 `tier_prices`); (2) у оплативших «Вступить
> в клуб» ведёт на «Мою подписку», а не на тарифы; кнопка «Перейти в закрытый чат» из
> `CLUB_CHAT_INVITE_URL` (авто-одобрение заявок — этап 4); (3) команда `/sub` + меню
> команд бота (`/start`, `/menu`, `/sub`).

---

## Этап 4 — Доступ в закрытую группу (join-request)

**Цель**: авто-приём заявок только у оплативших, защита от абуза ссылкой.

**Что делаем**:
- Инвайт-ссылка группы с включённой заявкой на вступление (creates_join_request).
- Хендлер `chat_join_request`: одобрить, если у tg_id активная подписка; иначе отклонить и прислать инфо.
- Кнопка «Перейти в закрытый чат» после успешной оплаты.
- Обработка ошибки добавления (экран «Не удалось добавить в чат»).

**Критерии приёмки** (закрытие → tag `stage-4-done`):
- [x] Оплативший подаёт заявку в группу → бот одобряет автоматически
- [x] Пользователь без активной подписки подаёт заявку → бот отклоняет и шлёт пояснение
- [x] Ссылка, пересланная неоплатившему, не даёт доступа (заявка отклонена)
- [x] Бот корректно обрабатывает случай, когда не может добавить (показывает экран поддержки)

---

## Этап 5 — Фиксация цены, продление, авто-удаление

**Цель**: жизненный цикл подписки — продление с сохранением цены и удаление при обрыве.

**Что делаем**:
- Экран «Продление»: оплата по зафиксированной ставке; продление до окончания срока сохраняет цену (end_date += месяцы от старого end).
- Фоновая проверка окончаний (APScheduler): подписка истекла → статус `expired`, кик пользователя из группы, **сброс зафиксированной цены** (следующая подписка — по актуальной ступени).
- Экран «Подписка закончилась», «нет активной подписки».

**Критерии приёмки** (закрытие → tag `stage-5-done`):
- [x] Продление до конца срока сохраняет зафиксированную цену (видно по SQL: fixed_price не изменился, end_date продлён)
- [x] По истечении подписки бот автоматически кикает пользователя из группы
- [x] После обрыва подписки новая оформляется по актуальной ступени (старая цена не действует)
- [x] «Моя подписка» для неактивного показывает экран «нет активной подписки»

---

## Этап 6 — Напоминания (крон 3/1/0 дней)

**Цель**: своевременные напоминания о продлении.

**Что делаем**:
- APScheduler-джоб: напоминания за 3 дня, за 1 день, в последний день до `end_date`.
- Тексты напоминаний из навигации (с подстановкой цены/даты/дней).
- Защита от повторной отправки (флаги отправленных напоминаний в подписке).
- Интервалы напоминаний конфигурируемы (настройка/команда админа).

**Критерии приёмки** (закрытие → tag `stage-6-done`):
- [x] За 3 дня до окончания приходит напоминание с ценой продления и датой
- [x] За 1 день и в последний день приходят соответствующие напоминания
- [x] Каждое напоминание отправляется один раз (нет дублей при повторных прогонах планировщика)
- [x] Интервалы напоминаний можно изменить без правки кода

---

## Этап 7 — Промокоды

**Цель**: полноценные промокоды со спец-ценой.

**Что делаем**:
- Таблица `promo_codes` (код, тип: спец-цена/скидка, значение, лимит активаций, использовано, срок действия, фиксируется ли цена, активность) и `promo_redemptions`.
- FSM-ввод промокода, валидация (активен / не найден / истёк / лимит исчерпан).
- Применение промокода к оплате (спец-цена становится доступной кнопкой), опциональная фиксация цены.
- Админ-команды: создать/посмотреть/деактивировать промокод.

**Критерии приёмки** (закрытие → tag `stage-7-done`):
- [x] Валидный промокод открывает кнопку со спец-ценой, оплата проходит по ней
- [x] Несуществующий/истёкший/исчерпанный промокод даёт корректное сообщение
- [x] Лимит активаций соблюдается (после исчерпания код не применяется)
- [x] Админ создаёт промокод командой → пользователь сразу может им воспользоваться

---

## Этап 8 — Админ-панель `/admin`

**Цель**: базовый набор админских команд для управления клубом.

**Что делаем**:
- `/admin` — список всех доступных команд с описанием.
- Ручное добавление уже оплативших людей (бот начинает их отслеживать: создаёт подписку с заданными датами/ценой).
- Правка тарифов/мест/длительностей/интервалов напоминаний (консолидация команд из этапов 2,6,7).
- Просмотр пользователей, застрявших в FSM (из таблицы лога состояний).
- Базовая статистика (число активных подписок, занятые места по ступеням).

**Критерии приёмки** (закрытие → tag `stage-8-done`):
- [x] `/admin` показывает полный список команд (только админам из `admin_ids`)
- [x] Админ вручную добавляет оплатившего → у того появляется активная подписка, бот её отслеживает
- [x] Админ видит список застрявших в FSM пользователей с их шагом
- [x] Команды правки тарифов/напоминаний доступны и применяются на лету
- [x] Не-админ не имеет доступа к админ-командам

---

## Этап 9 — Деплой CI/CD

**Цель**: автодеплой на сервер при пуше в `master`.

**Что делаем**:
- `scripts/install.sh` (по образцу `sub_bot/scripts/install.sh`): Docker, клон репо, `.env`, запуск стека с нуля на чистой Ubuntu (свои pg+redis).
- `.github/workflows/deploy.yml`: деплой по SSH при пуше в `master`.
- Постановка бота админом группы, проверка polling и логов в проде.
- README с инструкцией запуска (какая команда в cmd) для разработчика.

**Критерии приёмки** (закрытие → tag `stage-9-done`):
- [x] Чистый сервер поднимается с нуля до полного стека (выполнено эквивалентной okdeploy-процедурой: swap+Docker+git+clone+.env+compose; `install.sh` присутствует и соответствует этим шагам, дословно не запускался)
- [x] push в `master` → GitHub Actions деплоит на сервер автоматически
- [x] Бот живой в проде (отвечает на `/start`, логи идут), миграции применены
- [x] README объясняет локальный запуск одной командой

---

## Этап 10 — Перенос на сервер с доступом к ЮKassa + автодеплой

**Цель**: оплата работает в проде — бот развёрнут на сервере, с которого доступен
API ЮKassa, и автодеплой переключён на этот сервер.

**Контекст**: текущий прод-сервер `72.35.242.127` (HOSTKEY, Германия) не имеет
сетевого маршрута до `api.yookassa.ru` — создание платёжной ссылки висит/падает.
Проба прокси (`45.10.81.203:8000`) показала нестабильный egress. Проверка сервера
`163.5.153.147` (Амстердам) подтвердила прямой доступ к ЮKassa (авторизованный
запрос вернул реальный список платежей). Решение по конкретному серверу/способу —
за заказчиком (перенос на рабочий сервер ИЛИ стабильный RU-прокси к текущему).

**Что делаем**:
- Выбрать целевой сервер с подтверждённым доступом к `api.yookassa.ru` (либо
  настроить стабильный прокси `YOOKASSA_PROXY` к текущему серверу).
- Развернуть стек на целевом сервере (`scripts/install.sh` / okdeploy): Docker,
  клон `master`, боевой `.env`, запуск, миграции.
- Переключить автодеплой: GitHub secret `SERVER_HOST` на новый IP (SSH-ключ тот же
  `.secrets/deploy_key` — его pub добавить в `authorized_keys` нового сервера);
  проверить, что push в `master` деплоит на новый сервер.
- Корректный cutover без конфликта токена: остановить бота на старом сервере перед
  запуском на новом (один long-polling на токен).
- Боту дать права администратора группы на новом окружении (если меняется).

**Критерии приёмки** (закрытие → tag `stage-10-done`):
- [x] С целевого сервера `api.yookassa.ru` отвечает (авторизованный запрос — 200)
- [x] Стек развёрнут на целевом сервере, миграции применены, бот отвечает на `/start`
- [x] Оплата проходит end-to-end: создание ссылки → оплата → активация подписки
- [x] Автодеплой переключён: push в `master` выкатывает на целевой сервер
- [x] Старый бот остановлен (нет конфликта `getUpdates` за один токен)

---

# Цикл 2 (v2) — «Фокус.Энергия» (билеты) + реферальная программа + веб-админка

Бот становится двойным: подписка 11:11 (цикл 1, готово) + продажа билетов на офлайн-события
(бани, ретриты) со скидками и рефералкой, плюс отдельная **веб-админка** на поддомене.
MVP-first сохраняется: каждый этап заканчивается тем, что можно потрогать руками.

Источники требований: `docs/апдейт_этапы.csv` (для заказчика), `docs/вопросы_заказчику.md`
(согласования), раздел «Обновление…» в `CLAUDE.md`, `docs/рекомендации_админка.txt` +
`docs/пример_админки.png` (дизайн админки).

## Базовые решения цикла 2 (зафиксированы с заказчиком 22.06.2026)

- **Типы билетов**: мужской / женский / парный М+Ж / парный Ж+Ж / парный М+М — у каждого
  **своя цена** (задаёт админ). Гендер-баланс (галочка события): места общие или раздельно М/Ж;
  парный М+Ж = 1М+1Ж, парный Ж+Ж/М+М = 2 места своего пола. Мест нет → «Мест нет» + кнопка
  «Запрос в Поддержку».
- **Показ событий**: ретриты — все; баня — только ближайшая (после даты — автопереключение/
  скрытие); галочка «Показывать заранее» — показать конкретную баню заранее.
- **После оплаты билета**: сообщение с правилами + 2 кнопки «С правилами согласен (показать
  адрес)» / «Есть вопросы». Адрес — после согласия. QR/номер не нужен. Билет НЕ даёт доступа
  в группу (она открытая).
- **Скидка подписчикам на билеты**: индивидуально на каждое мероприятие (бани — общая,
  каждый ретрит — своя).
- **Стекинг**: скидка подписчика и промокод НЕ суммируются → берём **максимальную** (меньшая
  итоговая цена). Бонусы суммируются с любым из них, оплата бонусами **не более 50%**. Порядок:
  сначала скидка/промокод, затем бонусы до −50% от цены со скидкой.
- **Рефералка**: пока **только бани**. `/ссылка`: новичок (кто ещё ничего не покупал) — скидка
  (дефолт 500 ₽); пригласивший — бонус (дефолт 1000 ₽) **после даты мероприятия**. Суммы — в
  админке. 1 бонус = 1 ₽.
- **Возвраты/переносы**: кнопки «Перенос» нет; возвраты только через менеджера, индивидуально,
  ручным переводом (ЮKassa-автовозврат отложен). Авто-движок правил не делаем.
- **Веб-админка**: отдельная панель на `admin.agat-key.ru`, общая БД (схема `club_bot`).
  Дизайн: светлая, сайдбар слева, база чёрный+белый, показатели/графики — синий+зелёный,
  кастомные иконки, **без смайлов и фиолетовых градиентов**, законченный UX (вход/выход/смена
  пароля, нет нерабочих кнопок), человеческие формулировки (не сырые данные/переменные).
  Текущую `/admin` в боте **замораживаем** — новый функционал только в вебе.
- **Сервер**: `163.5.153.150` (2 CPU / 2 ГБ + swap), запас под админку есть. Стек — общий
  Docker Compose (добавляется сервис веб-админки + Nginx/Let's Encrypt для https).

| #  | Этап | Главный артефакт | Зависит от |
|----|------|------------------|------------|
| 11 | Каркас веб-админки + авторизация + https | `admin.agat-key.ru` по https: вход/выход/смена пароля, пустой каркас дашборда | 10 |
| 12 | Админка: мероприятия (CRUD) | Создание бань/ретритов: цены за тип билета, места/гендер-баланс, флаги, скидка подписчику per событие | 11 |
| 13 | Бот: раздел «Мероприятия» + показ + выбор билета | В боте видны события по правилам показа, выбор типа билета, «Мест нет» при исчерпании | 12 |
| 14 | Бот: оплата билета + электронный билет + учёт мест | Тестовая оплата → место списано (без переполнения), сообщение с правилами/адресом | 13 |
| 15 | Скидка подписчикам на билеты | Подписчик видит цену со скидкой события, не-подписчик — полную | 14 |
| 16 | Промокоды на билеты + стекинг (макс. скидка) | При наличии подписки и промо берётся бóльшая скидка (не суммируются) | 15 |
| 17 | Реферальная программа (бани) + бонусы | Ссылка `/ссылка`, скидка новичку, бонус пригласившему после события, оплата бонусами до 50% | 16 |
| 18 | Мои билеты + запрос на возврат | «Мои билеты» (прошедшие скрыты), запрос на возврат уходит менеджеру | 14 |
| 19 | Админка: подписки + кнопки меню | Правка подписок и кнопок меню (переименование/скрытие) из веба отражается в боте | 12 |
| 20 | Админка: статистика (дашборд) + уведомления | Дашборд «Доход» за период со сравнением, по направлениям; напоминание за день, отмена события | 17,19 |
| 21 | Сквозное тестирование и выкат в прод | Все сценарии работают в проде на сервере + `admin.agat-key.ru` | 11-20 |

---

## Этап 11 — Каркас веб-админки + авторизация + https

**Цель**: живая веб-админка на поддомене, в которую можно войти, — фундамент для всех
админ-разделов.

**Что делаем**:
- Новый сервис веб-админки (Python: FastAPI + серверный рендер шаблонов; общая схема БД
  `club_bot`), добавлен в Docker Compose; hot-reload в dev.
- Авторизация: таблица админ-аккаунтов (логин + хэш пароля), вход/выход (сессия), смена
  пароля; первый админ засевается из настроек. Доступ ко всем страницам — только после входа.
- Базовый макет по дизайн-референсу: сайдбар слева, шапка, пустой дашборд-каркас; чёрный+белый,
  акценты синий/зелёный, кастомные иконки, без смайлов/фиолетовых градиентов.
- Nginx + Let's Encrypt: `https://admin.agat-key.ru` (A-запись `admin` → 163.5.153.150).

**Критерии приёмки** (закрытие → tag `stage-11-done`):
- [x] `https://admin.agat-key.ru` открывается с валидным сертификатом — Let's Encrypt 2026-06-26, заказчик подтвердил вживую
- [x] Неавторизованный пользователь не видит внутренних страниц (редирект на вход) — проверено локально
- [x] Вход по логину/паролю работает; выход завершает сессию — проверено локально
- [x] Смена пароля из админки работает (старый пароль перестаёт пускать) — проверено локально
- [x] Каркас дашборда отрисован в стиле референса (сайдбар, без смайлов/фиолетовых градиентов) — проверено локально

---

## Этап 12 — Админка: мероприятия (CRUD)

**Цель**: админ ведёт расписание и цены событий сам, без программиста.

**Что делаем**:
- Миграция: `events` (тип баня/ретрит, название, дата-время, флаг гендер-баланса, флаг
  «показывать заранее», места: общее ИЛИ М/Ж), `event_ticket_prices` (цена за каждый тип
  билета: М/Ж/М+Ж/Ж+Ж/М+М), поле скидки подписчику per событие.
- UI: список/календарь событий; создание/редактирование/удаление; ввод цен по типам билетов;
  места и галочки; индивидуальная скидка подписчику. Человеческие подписи, валидация форм.

**Критерии приёмки** (закрытие → tag `stage-12-done`):
- [x] Админ создаёт баню и ретрит с датой/временем и ценами по типам билетов
- [x] Задаются места (общие или раздельно М/Ж) и галочки «гендер-баланс»/«показывать заранее»
- [x] Задаётся скидка подписчику отдельно для каждого события
- [x] Редактирование и удаление события работают; список/календарь отражает изменения
- [x] Нет нерабочих кнопок и сырых данных на экране

---

## Этап 13 — Бот: раздел «Мероприятия» + показ + выбор билета

**Цель**: пользователь видит события по бизнес-правилам и выбирает билет.

**Что делаем**:
- Кнопка «Мероприятия»: список событий с названием/датой/временем/ценой. Логика показа:
  ретриты — все; баня — только ближайшая + те, где стоит «показывать заранее»; после даты —
  следующая или скрытие.
- Выбор события → меню типов билетов (М/Ж/парные) с ценами. Исчерпаны места нужного типа →
  «Мест нет» + текст про Поддержку + кнопка «Запрос в Поддержку».

**Критерии приёмки** (закрытие → tag `stage-13-done`):
- [x] Ретриты показываются все, баня — только ближайшая (+ «показывать заранее») — проверено вживую заказчиком (ретрит + ближайшая баня видны, более поздняя баня без флага скрыта)
- [x] После прохождения даты ближайшей бани показ переключается на следующую/скрывается — покрыто smoke-тестом `tests/test_events.py` (фильтр по `now`, автопереключение на следующую баню)
- [x] Меню типов билетов показывает корректные цены из админки — проверено вживую (цены по типам из `event_ticket_prices`)
- [x] При исчерпании мест — «Мест нет» + кнопка «Запрос в Поддержку» — логика покрыта smoke-тестом (доступность по гендер-балансу; распродано → экран «Мест нет» + кнопка поддержки)

---

## Этап 14 — Бот: оплата билета + электронный билет + учёт мест

**Цель**: билет реально продаётся, место корректно списывается, без переполнения.

**Что делаем**:
- Оплата через ЮKassa (переиспользуем создание платежа + polling + чек 54-ФЗ с email-заглушкой),
  `payments`/новая таблица `tickets` с типом билета и событием.
- Атомарный учёт мест (под блокировкой, как `activate_payment`): парный М+Ж = 1М+1Ж, парный
  Ж+Ж/М+М = 2 места своего пола; защита от овербукинга при гонках.
- После оплаты — сообщение с правилами посещения + 2 кнопки «С правилами согласен (показать
  адрес)» / «Есть вопросы (написать в поддержку)».

**Критерии приёмки** (закрытие → tag `stage-14-done`):
- [x] Тестовая оплата билета проходит → создаётся запись билета — проверено вживую (2 сценария заказчиком, в БД 3 билета `status=paid`)
- [x] Место(а) списываются корректно по типу билета; повторная оплата/гонка не плодит дубль и не уводит места в минус — учёт под блокировкой строки события (FOR UPDATE), идемпотентность по `ticket_id`; занятость подтверждена счётчиком в админке (М: 2/11, Ж: 1/3) и smoke-тестом
- [x] После оплаты приходит сообщение с правилами и 2 кнопками; адрес — после согласия — проверено вживую (`rules_agreed` проставляется по согласию, адрес — только владельцу билета)
- [x] При нехватке мест на шаге оплаты сделка не проходит (корректное сообщение) — pre-check мест перед платежом + экран «Мест нет»; распроданное событие скрывается из бота; покрыто smoke-тестом

---

## Этап 15 — Скидка подписчикам на билеты

**Цель**: активный подписчик 11:11 видит цену билета со скидкой.

**Что делаем**:
- При показе цены и при оплате — проверка активной подписки; применяется скидка события
  (бани — общая, ретрит — своя). Не-подписчик платит полную цену.

**Критерии приёмки** (закрытие → tag `stage-15-done`):
- [x] Подписчик видит и платит цену со скидкой события — проверено вживую (Тест 1 −12%, Тест 2 −50%); в меню и сводке старая цена зачёркнута, новая выделена; сумма платежа считается той же `apply_discount`
- [x] Не-подписчик видит и платит полную цену — скидка применяется только при активной подписке (`repo.get_active_subscription`), иначе процент = 0
- [x] Скидка берётся из настройки конкретного события (разная для разных событий) — `events.subscriber_discount_percent` per-событие (Тест 1 = 12%, Тест 2 = 50%, Тест 3/4 = 0%)

---

## Этап 16 — Промокоды на билеты + стекинг (макс. скидка)

**Цель**: промокоды работают и на билетах, правила совмещения со скидкой подписчика соблюдены.

**Что делаем**:
- Расширение промокодов на билеты. Калькулятор цены: скидка подписчика и промокод НЕ
  суммируются — применяется **максимальная** (меньшая итоговая цена). Чистая логика покрыта
  smoke-тестом.

**Критерии приёмки** (закрытие → tag `stage-16-done`):
- [x] Промокод применяется к билету (валидный/невалидный обработаны)
- [x] При наличии и подписки, и промокода берётся бóльшая скидка (пример 1000→900, не 950)
- [x] Скидка подписчика и промокод не складываются ни при каком сочетании

---

## Этап 17 — Реферальная программа (бани) + бонусы

**Цель**: сквозная рефералка для бань с отложенным начислением и кошельком бонусов.

**Что делаем**:
- Миграция: `referral_links`, `referrals` (кто кого привёл), `bonus_ledger` (начисления/
  списания, идемпотентно). `/ссылка` выдаёт ссылку. Новичок (никогда не покупал) — скидка
  (дефолт 500 ₽) на первую покупку бани; пригласивший — бонус (дефолт 1000 ₽) **после даты
  мероприятия** (APScheduler-джоб, анти-возврат). Суммы — в админке.
- Оплата бонусами: 1 бонус = 1 ₽, до **50%** от цены уже со скидкой/промо (порядок зафиксирован).
- Админ: суммы рефералки, просмотр цепочек приглашений, ручное начисление/списание бонусов.

**Критерии приёмки** (закрытие → tag `stage-17-done`):
- [x] `/ссылка` выдаёт реферальную ссылку; переход новичка фиксирует связь
- [x] Новичок получает скидку на первую покупку бани; «не новичок» — нет
- [x] Пригласившему бонус начисляется только после даты мероприятия (не сразу)
- [x] Бонусами можно оплатить не более 50% суммы (после скидки/промо), без двойного списания
- [x] Админ видит цепочки приглашений и может вручную начислить/списать бонусы

---

## Этап 18 — Мои билеты + запрос на возврат

**Цель**: пользователь видит свои билеты и может запросить возврат через менеджера.

**Что делаем**:
- «Мои билеты»: список активных (прошедшие скрыты), детали заказа. Кнопка возврата →
  заявка менеджеру через существующий флоу Поддержки (ручной возврат). Кнопки «Перенос» нет.

**Критерии приёмки** (закрытие → tag `stage-18-done`):
- [x] «Мои билеты» показывает только активные (прошедшие скрыты)
- [x] Запрос на возврат уходит менеджеру/админам с данными билета
- [x] Кнопки «Перенос» нет (по решению заказчика)

---

## Этап 19 — Админка: подписки + кнопки меню

**Цель**: управление подписками и текстами кнопок из веба.

**Что делаем**:
- Раздел «Подписки»: список с активными подписками и сроками, ручное продление/отключение.
- Кнопки меню бота: переименование + показать/скрыть (без правки кода).

**Критерии приёмки** (закрытие → tag `stage-19-done`):
- [x] Админ видит подписки со сроками, вручную продлевает/отключает — отражается в боте
- [x] Переименование кнопки меню в вебе меняет текст в боте
- [x] Скрытие кнопки убирает её из меню бота

---

## Этап 20 — Админка: статистика (дашборд) + уведомления

**Цель**: владелец видит деньги и ключевые показатели; автоматические уведомления по событиям.

**Что делаем**:
- Дашборд по референсу: **Доход** по умолчанию за 30 дней со сравнением с прошлым периодом;
  общий и раздельно по направлениям (бани/ретриты, расширяемо); число активных подписчиков;
  эффективность рефералки. Произвольный период (всё время/год/месяц/диапазон). Графики
  синий/зелёный, аккуратные оси без артефактов.
- Уведомления: напоминание за 1 день до мероприятия всем купившим; отмена события админом →
  уведомление купившим (+пометка на ручной полный возврат).

**Критерии приёмки** (закрытие → tag `stage-20-done`):
- [x] Дашборд показывает доход за период со сравнением с предыдущим, общий и по направлениям
- [x] Период переключается (30 дней/год/месяц/произвольный), графики без артефактов по осям
- [x] За 1 день до события купившим уходит напоминание
- [x] Отмена события уведомляет всех купивших и помечает на возврат

---

## Этап 21 — Веб-админка: билеты + отметка фактического возврата

**Цель**: менеджер видит проданные билеты и отмечает фактический возврат — после
того как вручную вернул деньги переводом (закрывает «хвост» возвратов из этапа 18).

**Контекст**: на этапе 18 пользователь запрашивает возврат → билету ставится флаг
`tickets.refund_requested` (статус остаётся `paid`, место НЕ освобождается). Кто-то
должен подтвердить фактический возврат — это делает менеджер в веб-админке.

**Что делаем**:
- Раздел «Билеты» в веб-админке: список проданных билетов (фильтр по событию;
  покупатель, тип, цена, статус, дата покупки), заметная пометка «возврат запрошен».
- Кнопка «Отметить возврат» у билета: статус → `refunded`, фиксируется кто/когда
  (`refunded_at`, кем). После этого место освобождается и билет пропадает из «Моих
  билетов» у пользователя. Опц. уведомление пользователю «возврат произведён».
- Анти-возврат рефералки уже учтён (этап 17): возврат до события переводит реф-связь
  в `void` — проверить, что отметка возврата это триггерит (или задокументировать ручной кейс).

**Критерии приёмки** (закрытие → tag `stage-21-done`):
- [x] В админке виден список проданных билетов с заметной пометкой «возврат запрошен»
- [x] «Отметить возврат» меняет статус билета на `refunded`: место освобождается,
  билет исчезает из «Моих билетов» у пользователя
- [x] Зафиксировано, кто и когда отметил возврат (аудит)

---

## Этап 21.1 — Возвраты: связь с покупателем + уведомление о возврате

**Цель**: довести раздел «Билеты» до удобной работы менеджера — дать связь с
покупателем и автоматическое уведомление о произведённом возврате с понятным
фоллбэком, если бот не смог доставить сообщение.

**Контекст / пожелание заказчика (25.06)**: на демо этапа 21 заказчик отметил, что
из раздела «Билеты» нельзя выйти на связь с покупателем, и спросил про сценарий
«юзер не принимает ЛС». Telegram-ограничение: бот не пишет тем, кто не начинал с ним
диалог, но покупатель билета диалог начинал — недоставка возможна только если он
заблокировал бота; на этот случай нужен явный сигнал менеджеру.

**Что делаем**:
- **A. Связь с покупателем — мост через бота** (двусторонний). Прямая tg-ссылка по
  numeric id для юзеров БЕЗ @username не работает (ограничение Telegram — нельзя
  инициировать диалог по id). Поэтому: где есть @username — рабочая ссылка
  `t.me/<username>`; плюс для ВСЕХ — поле «Написать через бота» в карточке билета:
  менеджер пишет текст → бот доставляет покупателю от своего имени (он уже в диалоге
  с ботом). Ответы покупателя боту пересылаются всем админам (как заявки этапа 18).
  Паттерн «очередь + бот-джоб» (веб-процесс отдельный от бота). Статусы доставки
  (доставлено/не доставлено) видны в админке.
- **B. Уведомление о возврате + алерт недоставки** (паттерн «флаг + бот-джоб», как
  отмена события этапа 20): отметка возврата ставит билет в очередь уведомления;
  бот-джоб шлёт пользователю «возврат произведён». При успехе — пометка «уведомлён»;
  при неудаче (юзер заблокировал бота) — пометка «не доставлено».

**Критерии приёмки** (закрытие → tag `stage-21.1-done`):
- [x] Из карточки билета менеджер пишет покупателю через бота (с username и без);
  доставка отражается статусом, при недоставке — пометка «не доставлено»
- [x] Ответ покупателя боту приходит в веб-админку (раздел «Билеты», пометка «новый
  ответ» + переписка), а не в Telegram-аккаунт менеджера
- [x] После отметки возврата пользователю приходит «возврат произведён»; при
  недоставке — заметная пометка «не доставлено» в админке

---

## Этап 22 — Веб-админка: редактор экранов бота (тексты + кнопки по разделам)

**Цель**: владелец редактирует не только подписи/видимость кнопок (этап 19), но и
**тексты экранов** бота — по разделам, в одном месте. Расширяет Этап 19.

**Контекст / пожелание заказчика (25.06)**: на демо этапа 19 заказчик уточнил, что
нужно редактировать и тексты экранов, а не только кнопки. Желаемый UX: список разделов
бота (по навигации) → клик в раздел → внутри сразу: текст экрана (HTML-форматирование)
+ подписи и видимость его кнопок. Навигационная схема экранов снята в
`docs/навигация_экранов.md` (черновик-источник для этого этапа). Реализации пока нет —
этап заведён в план.

**Что делаем** (детализируется при взятии в работу):
- Реестр редактируемых экранов бота (ключ → дефолтный текст + перечень кнопок), как
  `services.menu` для кнопок. Тексты-дефолты — из `texts.py`.
- Хранение переопределений текстов в БД (таблица по образцу `menu_buttons`); бот
  рендерит экран из БД с fallback на дефолт, применяется без рестарта.
- В веб-админке: раздел «Экраны бота» — дерево/список экранов → форма правки текста
  (с предпросмотром HTML) + подписи/видимость кнопок этого экрана (объединить с этапом 19).
- Решить, какие экраны редактируемые (инфо-экраны: приветствие, «Что внутри», «Правила»,
  «Поддержка», экраны подписки/билетов) и какие плейсхолдеры в тексте допустимы.

**Критерии приёмки** (закрытие → tag `stage-22-done`):
- [x] В админке виден список экранов бота (по навигации) с их текущим текстом
- [x] Правка текста экрана в вебе меняет текст в боте без рестарта
- [x] Подписи/видимость кнопок этого экрана правятся там же (объединено с этапом 19)

---

> **Перенос `/admin` в веб (этапы 23–27).** Архитектурное решение: бот-команда `/admin`
> заморожена, весь админ-функционал — только в веб-админке. Этапы 23–27 переносят
> оставшиеся разделы `/admin` в веб ПЕРЕД выкатом в прод (этап 28). Уже в вебе:
> статистика (20), мероприятия (12), билеты (21), подписки (19), рефералка (17),
> экраны/кнопки бота (19/22). Источник дефолтов и логики — соответствующие разделы
> `src/handlers/admin.py`; переносим UI в FastAPI/Jinja, бизнес-логику переиспользуем
> из `services.*`/`repo` (как делали в этапах 12–22). Веб-процесс отдельный от бота:
> где нужна отправка в Telegram (рассылка) — через очередь + бот-джоб (паттерн моста
> возвратов, этап 21.1).

## Этап 23 — Веб-админка: тарифы (ценовые ступени + цены за периоды + длительности)

**Цель**: владелец управляет ценами подписки из веба (сейчас только в `/admin`).

**Что делаем**:
- Раздел «Тарифы»: ценовые ступени (ставка ₽/мес, лимит мест, название, вкл/выкл),
  матрица цен за периоды (3/6/12…), длительности (значение+единица, добавить/удалить/
  вкл-выкл). Инвалидация Redis-кеша тарифов при правке (как `/admin`).

**Критерии приёмки** (закрытие → tag `stage-23-done`):
- [x] Ступени правятся в вебе (ставка/лимит/название/вкл-выкл), бот применяет без рестарта
- [x] Цены за периоды и длительности правятся в вебе, отражаются в боте
- [x] Заказчик подтвердил вживую

---

## Этап 24 — Веб-админка: промокоды подписки (CRUD)

**Цель**: управление промокодами подписки в вебе (сейчас в `/admin` → «Промокоды»).

**Что делаем**:
- Раздел «Промокоды»: список, создание (тип `fixed_price`/`percent`, значение, лимит,
  срок, фиксация цены), вкл/выкл. Логика — существующие `services.promo`/`repo`.

**Критерии приёмки** (закрытие → tag `stage-24-done`):
- [x] Промокод создаётся/выключается в вебе, активируется в боте корректно
- [x] Лимит активаций и срок действия соблюдаются
- [x] Заказчик подтвердил вживую

---

## Этап 25 — Веб-админка: участники (поиск/карточка/история + ручное добавление + аннулирование)

**Цель**: работа с участником в вебе (сейчас в `/admin`: поиск, карточка, добавить,
аннулировать, история). Расширяет раздел «Подписки».

**Что делаем**:
- Поиск участника по tg_id → карточка (статус подписки/доступа, цена, даты).
- Ручное добавление оплатившего (создаёт активную подписку `source='manual'`).
- Аннулирование с киком (через механизм этапа 19: `end_date=now()` → фон-проверка кикает).
- История действий участника (journey из `fsm_states`/событий).

**Критерии приёмки** (закрытие → tag `stage-25-done`):
- [x] Поиск по tg_id показывает карточку статуса участника
- [x] Ручное добавление создаёт активную подписку, бот пускает в группу
- [x] Аннулирование завершает подписку и кикает из группы
- [x] Заказчик подтвердил вживую

---

## Этап 26 — Веб-админка: рассылка (текст/фото/альбомы через бота)

**Цель**: рассылка по базе из веба (сейчас в `/admin` → «Рассылка», с фото и media_group).

**Что делаем**:
- Форма рассылки (текст + до N фото/альбом). Веб ставит задачу в очередь (таблица),
  бот-джоб рассылает (паттерн моста этапа 21.1), троттлинг + авто-пометка заблокировавших,
  статус выполнения (отправлено/ошибок). Фото — в S3 (`services.storage`, как этап 8).

**Критерии приёмки** (закрытие → tag `stage-26-done`):
- [x] Текстовая рассылка из веба доходит до пользователей бота
- [x] Рассылка с фото/альбомом доходит корректно
- [x] Заблокировавшие бота помечаются, статус рассылки виден в вебе
- [x] Заказчик подтвердил вживую

---

## Этап 27 — Веб-админка: настройки (напоминания + ссылка поддержки + администраторы + диагностика)

**Цель**: перенести в веб оставшиеся разделы `/admin`: напоминания (пороги), ссылка
поддержки, администраторы доступа, «кто на каком экране» (FSM-диагностика). После этого
`/admin` в боте можно отключить.

**Что делаем**:
- Напоминания: единица + пороги early/soon/last (DB-настройки `bot_settings`, на лету).
- Ссылка поддержки: URL (`app_settings`).
- Администраторы: список доп-админов бота по tg_id (получают заявки/доступ), добавить/убрать.
- FSM-диагностика: список «кто на каком экране» (застрявшие), как `/admin` → «Кто на экране».
- ~~Отключить вход в `/admin` из бота~~ — **отменено заказчиком**: `/admin` оставлен для
  быстрого доступа из бота, функционал дублируется в вебе (не заморожен).

**Критерии приёмки** (закрытие → tag `stage-27-done`):
- [x] Пороги напоминаний и ссылка поддержки правятся в вебе, применяются ботом
- [x] Администраторы доступа правятся в вебе
- [x] FSM-диагностика показывает экраны пользователей
- [x] ~~`/admin` в боте отключён~~ — отменено заказчиком: `/admin` оставлен рабочим
  (быстрый доступ из бота); доп-админы согласованы между ботом и вебом (DB-авторитетно +
  periodic reload)
- [x] Заказчик подтвердил вживую

---

## Этап 28 — Дашборд: графики-распределения (бублики) + переключатель графиков

**Цель**: на дашборде веб-админки появляется блок с переключаемыми графиками; первыми
— два круговых (donut) графика распределения. Один экран, выбор графика — выпадающим
списком (без перезагрузки страницы или с лёгким серверным рендером — на усмотрение).

**Что делаем**:
- Инфраструктура переключения: на дашборде блок «Графики» с выпадающим списком выбора
  («Распределение подписчиков», «Билеты: подписчики vs гости», далее столбчатые из
  этапа 29). Все графики живут на одном экране, переключаются списком.
- Donut «Распределение подписчиков клуба»: три сегмента — запускали бота без подписки в
  клуб / с активной подпиской / с просроченной (бывшие). Считаем из `users` +
  `subscriptions` (active / expired / нет подписки).
- Donut «Билеты мероприятий: с подпиской vs без»: среди купивших билеты — у скольких на
  момент покупки (или сейчас) есть активная подписка на клуб, у скольких нет.
- Графики — серверный inline-SVG (как график дохода этапа 20), легенда с числами/%.

**Критерии приёмки** (закрытие → tag `stage-28-done`):
- [x] На дашборде есть блок «Графики» с выпадающим списком выбора графика (1 экран)
- [x] Donut распределения подписчиков (без подписки / активные / просроченные) с цифрами
- [x] Donut по билетам (купившие с подпиской vs без) с цифрами
- [x] Цифры сходятся с БД (сверка SQL)
- [x] Заказчик подтвердил вживую

---

## Этап 29 — Дашборд: графики динамики (столбчатые) — регистрации и активность по датам

**Цель**: в тот же переключатель графиков (этап 28) добавляются два столбчатых графика
динамики во времени.

**Что делаем**:
- Bar «Регистрации в боте по датам»: число новых пользователей (`users.created_at`) по
  дням/месяцам в выбранном периоде (гранулярность авто, как у графика дохода).
- Bar «Активность в боте по датам»: число активных пользователей/действий по датам
  (источник — `user_events` или входящие апдейты; уникальные пользователи за день).
- Оба графика — пункты того же выпадающего списка на дашборде; период переключается
  существующими кнопками периода (30 дней / год / всё / произвольный).

**Критерии приёмки** (закрытие → tag `stage-29-done`):
- [x] Bar «Регистрации по датам» в переключателе, цифры сходятся с БД
- [x] Bar «Активность по датам» в переключателе, цифры сходятся с БД
- [x] Графики уважают выбранный период и гранулярность
- [x] Заказчик подтвердил вживую

---

## Этап 30 — Сквозное тестирование и выкат в прод

**Цель**: весь цикл 2 (включая полную веб-админку) работает в проде.

**Что делаем**:
- Сквозные проверки всех сценариев (билеты, скидки, промо, рефералка, возвраты, админка).
- Применение миграций в проде, деплой веб-админки + Nginx/https на сервере, smoke в проде.

**Критерии приёмки** (закрытие → tag `stage-30-done`):
- [x] Бот и веб-админка работают в проде (`163.5.153.150` + `admin.agat-key.ru`)
- [x] Сквозной сценарий «выбор события → оплата билета → билет» проходит вживую
- [x] Сквозной сценарий рефералки (ссылка → скидка → бонус после события) проходит
- [x] Дашборд статистики показывает реальные цифры из прода
- [x] Заказчик подтвердил приёмку вживую («вроде работает всё», 2026-06-26)

---

## Этап 31 — UX меню: контекстное мини-меню (снизить перегруз)

**Цель**: убрать перегруз внимания в приветствии и главном меню — показывать только
релевантное статусу подписки, выделить главное действие, спрятать редкое во второй уровень.

**Что делаем**:
- **Контекст по статусу подписки**: гость видит «Вступить в клуб» (главное действие,
  отдельным рядом); подписчик — «Продлить подписку» (главное) + «Моя подписка», без
  «Вступить». Раскладку выбирает код по `repo.get_active_subscription`.
- **Подменю «О клубе»**: инфо-кнопки (Что внутри · Правила) + «Пригласить друга» уезжают
  под одну кнопку-хаб «О клубе» (новый `NAV_ABOUTMENU`), верхний уровень разгружается.
- **Промокод убран с верхнего уровня** — ввод остаётся в флоу оплаты/тарифа.
- Без эмодзи (правило проекта): иерархия — позицией (главное действие первым рядом во всю
  ширину) и краткостью верхнего уровня (~5 кнопок вместо 11).
- Подписи/видимость кнопок остаются правимыми из веб-админки; новая кнопка «О клубе»
  и экран подменю — в реестре `services/menu.py` / редакторе экранов.

**Критерии приёмки** (закрытие → tag `stage-31-done`):
- [x] Гость видит компактное меню с «Вступить в клуб» главным; «Моя подписка»/«Продлить» скрыты
- [x] Подписчик видит один раздел «Моя подписка» (продление — внутри него); «Вступить» скрыто
- [x] «О клубе» открывает подменю (Что внутри · Правила · Пригласить друга · Назад)
- [x] Промокода нет на верхнем уровне; ввод промокода доступен в оплате/продлении
- [x] Верхний уровень — не более ~5 кнопок; навигация «Назад» возвращает корректно
- [x] Заказчик подтвердил приёмку вживую («да ок», 2026-06-27, на проде)

---

## Этап 32 — Веб-админка «Билеты»: поиск, фильтры, пагинация, список на вход (CSV)

**Цель**: найти конкретного покупателя среди тысяч билетов и выгрузить печатный
check-in лист для сверки людей на входе.

**Что делаем**:
- **Поиск по покупателю** — одно поле: имя / `@username` / Telegram ID (частичное
  совпадение, регистронезависимо). Сужает список до билетов искомого человека.
- **Фильтр по статусу** — Все / Оплачен / Возврат запрошен / Возвращён.
- **Пагинация** — по 50 на страницу + счётчик «Найдено: N» (страница не грузит тысячи строк).
- **Экспорт CSV «список на вход»** — печатный check-in лист по выбранному событию,
  только оплаченные: № · Имя · Контакт (@username или id) · Тип билета · Гостей
  (парный = 2) · пустая колонка «Отметка» для галочки. Сортировка по имени; UTF-8 с BOM
  (корректная кириллица в Excel).
- Серверная фильтрация/пагинация в `repo.list_sold_tickets` (поиск + статус + limit/offset
  + общий count); экспорт уважает текущие фильтры (событие/поиск/статус).

**Критерии приёмки** (закрытие → tag `stage-32-done`):
- [x] Поиск по имени/@username/id находит билеты конкретного покупателя
- [x] Фильтр по статусу работает; комбинируется с поиском, событием и датой покупки
- [x] Пагинация по 50 + счётчик «Найдено: N»; большой список не тормозит
- [x] Экспорт CSV даёт удобный печатный список на вход (событие/дата/имя/@username/Telegram ID/тип/гостей/отметка; кириллица читается в Excel)
- [x] Заказчик подтвердил приёмку вживую («да ок», 2026-06-27, на проде)

---

## Этап 33 — Номер билета (бот + поиск в админке) + фикс вёрстки даты

**Цель**: у билета есть видимый номер, по нему можно найти билет в админке; фильтр
даты в «Билетах» не разъезжается.

**Что делаем**:
- **Бот «Мои билеты»**: показать номер билета (`tickets.id`, например «Билет №123») —
  в списке и/или карточке билета, чтобы покупатель мог назвать его менеджеру.
- **Админка «Билеты» — поиск по номеру билета**: запрос вида `#123` (или просто число)
  находит билет по `tickets.id`. Расширяем существующий поиск (имя/@username/tg_id) так,
  чтобы число матчило и номер билета, а `#N` — точный номер.
- **Фикс вёрстки фильтра даты**: «Куплен с:» и «по:» — в две строки друг под другом
  (сейчас при wrap во flex-баре всё съезжает). Дату вынести в отдельный аккуратный блок
  (два поля, подписи над/перед полями, без наезда на остальные фильтры).

**Критерии приёмки** (закрытие → tag `stage-33-done`):
- [x] В «Мои билеты» у каждого билета виден его номер (бот)
- [x] В админке «Билеты» поиск по номеру билета (`#123`/число) находит нужный билет
- [x] Блок даты «Куплен с / по» выровнен в две строки, вёрстка не съезжает (проверено в браузере)
- [x] Заказчик подтвердил приёмку вживую


## Этап 34 — Чек-ин на точке: онлайн-отметка «Пришёл» + адаптив под мобилку

**Цель**: на входе админ (с телефона) отмечает пришедших, видит «пришло X из N»;
повторный проход по одному билету не задваивается; страница чек-ина удобна на мобильном.

**Контекст**: раздел «Билеты» (этапы 21/32) даёт поиск/фильтры/CSV, но онлайн-отметки
присутствия нет — на 100 гостей админ не видит живую картину входа и не защищён от
двойного прохода. Заказчик выбрал онлайн-отметку «Пришёл» + адаптив под мобилку
(сбор реального ФИО и «только CSV» — отклонены).

**Что делаем**:
- **Миграция** `0024_ticket_checkin`: поля `tickets.attended_at TIMESTAMPTZ NULL`,
  `tickets.attended_by TEXT NULL` (логин админа, кто отметил).
- **repo**: `mark_ticket_attended` (идемпотентно — повтор не плодит, отдаёт «уже на месте»),
  `unmark_ticket_attended` (снять ошибочную отметку), `checkin_stats(event_id)` —
  билетов оплачено/пришло + людей всего/пришло (парный = 2).
- **Админка «Билеты»**: у оплаченного билета кнопка «Отметить пришёл» ↔ «Пришёл ✓
  (отменить)» с временем/админом; сводка по событию «Пришло: X из N · людей Y из M»;
  фильтр «Пришёл / Не пришёл». Все отметки — через PRG (F5 не задваивает).
- **Мобильный режим**: адаптив таблицы билетов (на узком экране — карточки-строки,
  без горизонтального скролла) + крупная кнопка «Пришёл» под палец.

**Критерии приёмки** (закрытие → tag `stage-34-done`):
- [x] У оплаченного билета есть кнопка «Отметить пришёл»; повтор не задваивает (защита от двойного прохода)
- [x] По событию видна сводка «Пришло X из N» (с учётом парных = людей)
- [x] Ошибочную отметку можно снять (отмена)
- [x] Посещённый билет (отмечен «пришёл») исчезает из «Мои билеты» у юзера и недоступен для возврата
- [x] Страница чек-ина удобна с телефона (крупные строки/кнопки, без горизонтального скролла — проверено в браузере)
- [x] Заказчик подтвердил приёмку вживую


## Этап 35 — Адаптив всей веб-админки под мобильный

**Цель**: вся веб-админка (а не только «Билеты») удобна с телефона — есть мобильное
меню, контент не обрезается, дашборд/таблицы/формы/фильтры читаемы и кликабельны.

**Контекст**: чек-ин на точке (этап 34) — с телефона, но остальные разделы под мобилку
не доведены. Сайдбар фиксирован 256px (на телефоне съедает экран, контента не видно),
мобильной навигации нет. Card-transform таблиц из этапа 34 применился глобально, но
`data-label` есть только у билетов → в прочих разделах таблицы рендерятся «карточками»
с пустыми подписями — надо доделать целостно.

**Что делаем**:
- **Мобильное меню**: на узком экране сайдбар прячется за «гамбургером» в шапке, выезжает
  поверх контента с затемнением-оверлеем; клик по пункту/оверлею закрывает (CSS-only,
  чекбокс-хак — без JS; переход по ссылке сбрасывает состояние сам).
- **Каркас**: на мобилке уменьшить отступы topbar/content; гамбургер слева от заголовка.
- **Таблицы**: card-transform сделать opt-in (класс только на таблице билетов — там
  `data-label` + чек-ин). Прочие таблицы на мобилке — горизонтальный локальный скролл
  (`.table-wrap { overflow-x:auto }`), без обрезки и пустых подписей.
- **Дашборд**: KPI-плитки в 1–2 колонки, переключатель периода переносится/скроллится,
  SVG-графики масштабируются по ширине.
- **Формы и фильтры**: поля во всю ширину, гриды в одну колонку, ничего не вылазит.

**Критерии приёмки** (закрытие → tag `stage-35-done`):
- [x] На телефоне есть мобильное меню (гамбургер), все разделы открываются
- [x] Контент не обрезается, нет горизонтального скролла всей страницы (таблицы скроллятся/складываются локально)
- [x] Дашборд (KPI, период, графики) читаем на телефоне
- [x] Формы и фильтры удобны на телефоне (поля во всю ширину, не вылазят)
- [x] Заказчик подтвердил приёмку вживую с телефона


## Этап 36 — Веб-админка «Мероприятия»: поиск + фильтры + пагинация архива

**Цель**: в разделе «Мероприятия» можно найти событие по названию, отфильтровать по
направлению/статусу; архив «Прошедших» пагинируется и не грузится целиком.

**Контекст**: раздел грузит ВСЕ события разом и делит на «Предстоящие»/«Прошедшие», без
поиска/фильтров/пагинации. Предстоящих всегда мало — им фильтры не критичны; «Прошедшие»
растут без предела (бани регулярно + ретриты) и грузятся целиком при каждом открытии.
Объём — десятки/сотни (не тысячи, как билеты) → инструмент лёгкий. Заказчик выбрал полный
набор.

**Что делаем**:
- **Поиск по названию** (ILIKE, частичный) — над обоими списками.
- **Фильтр по направлению** (баня / ретрит / все) и **статусу** (в боте / скрыто /
  отменено / все).
- **Пагинация «Прошедших»** + перенос фильтрации в SQL (архив не грузится целиком);
  «Предстоящие» — полным списком сверху (их мало), тоже под фильтр/поиск.
- `events_repo`: общий построитель фильтров + `list_upcoming_events` /
  `list_past_events`(limit/offset) / `count_past_events`. Цены/занятость — как сейчас.
- Вёрстка фильтр-бара — мобильно-дружелюбная (как этапы 32/35), счётчик «Найдено».

**Критерии приёмки** (закрытие → tag `stage-36-done`):
- [x] Поиск по названию находит мероприятие в обоих списках
- [x] Фильтр по направлению (баня/ретрит) и статусу (в боте/скрыто/отменено) работает
- [x] «Прошедшие» пагинируются, архив не грузится целиком (фильтрация в SQL)
- [x] Фильтр-бар нормально выглядит на телефоне
- [x] Заказчик подтвердил приёмку вживую


## Этап 37 — Картинка к экранам бота (опционально, без поломки навигации)

**Цель**: к любому редактируемому инфо-экрану (этап 22) можно прикрепить картинку из
веб-админки; бот показывает экран с фото и без, **переходы между экранами не ломаются**
(edit/send/delete безопасны), экран без картинки работает ровно как раньше.

**Контекст / риск (главное)**: навигация показывает инфо-экраны через
`cb.message.edit_text(...)` (редактирует текущее сообщение на месте; `navigation.py:_show`),
а `/start`/`/menu` — через `message.answer(...)`. Telegram **не позволяет** `edit_text` по
сообщению-фото и не превращает текстовое сообщение в фото через edit. Значит, наивная
картинка ломает переходы на экран/с экрана. Картинки храним в S3 (как рассылка, этап 26),
бот шлёт по публичному URL.

**Решения с заказчиком (27.06.2026)**:
- Текст у экрана **с фото** ограничивается **1024 символами** (идёт подписью под фото —
  одно сообщение, простая навигация). Админка показывает счётчик/предупреждение.
- Фото опционально у **всех** редактируемых экранов реестра `services.screens`.

**Что делаем**:
- **Миграция `0025_screen_photo`**: `screen_texts.photo_url TEXT NULL` (NULL = без фото).
- **repo**: `get_screen_overrides` отдаёт текст + photo_url; `upsert_screen_photo`
  (set/clear URL) — отдельно от текста, чтобы правки не затирали друг друга.
- **services.screens**: резолв экрана возвращает `{text, photo_url}` (дефолт photo=None);
  `screen_list/screen_one` отдают текущее фото.
- **Безопасный показ (ядро, центральный helper)** — единое правило для ВСЕХ точек показа
  инфо-экранов (`navigation._show`, `start.cmd_start`, `start.cmd_menu`):
  - целевой экран **без фото** и текущее сообщение текстовое → `edit_text` (как сейчас,
    плавно, без регресса);
  - целевой экран **без фото**, но текущее сообщение — фото → `delete` + `send_message`;
  - целевой экран **с фото**, текущее — фото → `edit_message_media`(InputMediaPhoto+caption)
    (на «not modified»/ошибку → `delete` + `send_photo`);
  - целевой экран **с фото**, текущее — текст → `delete` + `send_photo`(caption=text).
  - `/start`,`/menu` (новое сообщение): `answer_photo` если фото, иначе `answer` (как сейчас).
  - Все delete/edit обёрнуты в suppress — отсутствие/устаревание сообщения не валит флоу.
- **Админка** (`webadmin/screens.py` + `screen_form.html`): загрузка картинки в S3
  (`services.storage`, как этап 26), превью текущей, кнопка «Убрать картинку»; при наличии
  фото — серверная валидация длины текста ≤1024 (+ подсказка/счётчик в форме). PRG-redirect.
- **Без регресса**: если у экрана нет фото — все ветки сводятся к текущему поведению
  (edit_text / answer), ничего в существующих экранах не меняется.

**Критерии приёмки** (закрытие → tag `stage-37-done`):
- [x] В админке у экрана можно загрузить картинку, увидеть превью и убрать её
- [x] Экран с картинкой показывается в боте (фото + подпись) и при входе (`/start`,`/menu`), и при переходе по кнопкам
- [x] Переходы экран-с-фото ↔ экран-без-фото не падают; «Назад»/меню работают в любом порядке
- [x] Экран без картинки работает ровно как раньше (нет регресса edit_text)
- [x] При фото текст ограничен 1024 (подпись не обрезается, админка предупреждает)
- [x] Заказчик подтвердил приёмку вживую

---

# Цикл 3 (v3) — правки заказчика (июль 2026)

Пакет доработок по списку заказчика от **09.07.2026** (`прав.txt`) + дополнения из переписки
**11.07.2026** (кнопки к автосообщениям, уведомления админам о продажах). Бот и веб-админка
уже в проде (цикл 2, `v0.3.3x`) — цикл 3 наслаивается без перепиливания базы.

Источники требований: `прав.txt` (список заказчика), переписка 11.07.2026, смета
`docs/смета_правки_2026-07.csv` (что входит в оплаченный объём).

## Базовые решения цикла 3

- **Оплата**: единый блок за 40 000 ₽ (объединённые спринты, гибкий срок / низкий приоритет).
  Гарантийные баги (этап 38) — бесплатно. ИИ-ассистент — **вне цикла 3**, отдельный проект.
- **Гарантия vs новое**: этап 38 чинит то, что не соответствует ТЗ цикла 2 (показ событий,
  «Мест нет») — это гарантия. Остальные этапы — новый оплаченный функционал.
- **Кнопки Йога/Консультации** появляются в меню (этап 39) **скрытыми** (через существующий
  механизм видимости кнопок, этап 19) и открываются вместе со своими разделами (этапы 46, 47) —
  чтобы в меню не было нерабочих кнопок.
- **Скидка по реф-ссылке «24 часа»** — по согласованию только **надпись**, без реального
  ограничения по времени (техническое ограничение — опция сверх сметы).
- **Уведомления админам** уходят в список админов из настроек веб-админки (этап 27).
- **`/admin` в боте** остаётся замороженным — новые настройки только в веб-админке.

**Решения раунда 4 (ответы заказчика 11.07.2026)** — все блокеры сняты:
- **Ступени цены по местам — УБИРАЕМ совсем** (этап 43). Заказчик регулирует стоимость,
  в т.ч. для первых участников, **промокодами**. Остаются две цены: **вход** и **продление**.
  Это упрощает ценовую модель цикла 1 (авто-ступени по числу мест больше не считаются).
- **Реф-суммы** (этап 40): любые значения, меняются в админке **по каждой категории**; плюс
  можно задать не фиксированную сумму, а **процент от суммы покупки**.
- **Уведомление админам** (этап 42): строка «продано X билетов на сумму YYY» — **по этому
  мероприятию** (заполненность и маржинальность конкретного события).
- **Йога и Консультации — НЕ события** (этапы 46, 47). Это продажа услуги «по количеству»
  без даты, мест и счётчика в боте. После оплаты — сообщение с редиректом к менеджеру
  **@zhannazlotnikova** (она ведёт запись и остаток занятий в личке). Йога онлайн, два
  продукта: **индивидуальное** и **групповое** занятие, у каждого — выбор количества.
  Консультации — пакеты **1 / 4 / 8 / 12**. Ни календаря, ни счётчика остатка в боте.
  Контакт менеджера — настраиваемый в админке (не хардкод).

| #  | Этап | Главный артефакт | Зависит от |
|----|------|------------------|------------|
| 38 | Гарантийные баги + мелкие правки навигации | Видны все события (ретриты + ближайшая баня); при нуле мест событие остаётся с «Мест нет»; «Назад» на всех экранах | 37 |
| 39 | Новое главное меню | Меню по схеме заказчика: Клуб · Бани/Ретриты · Йога/Консультации · Пригласить друга · Мои билеты/Поддержка | 38 |
| 40 | Рефералка на все покупки + уведомление пригласившему | Реф-ссылка работает на ретриты/подписку/йогу/консультации; пригласившему приходит «твой друг купил…» | 39 |
| 41 | Раздел «Мои бонусы» + кнопки к автосообщениям | Юзер видит активные/будущие/всего бонусов; в напоминании о событии — «Мои билеты», в сообщении о бонусах — «Мои бонусы»/«Главное меню» | 40 |
| 42 | Уведомления админам о продажах | После продажи билета/подписки и выхода за неуплату админам приходит уведомление со сводкой | 41 |
| 43 | Тариф подписки: вход/продление (без ступеней по местам) | Две цены (вход дороже, продление дешевле), ступени по местам убраны; перерыв → снова цена входа | 38 |
| 44 | Скидка подписчикам на продление + абсолютные цены | В админке задаётся полная цена и цена для подписчика (сумма, не только %); промокод имеет приоритет над скидкой | 43 |
| 45 | Динамические цены ранним покупателям | Цена билета растёт по мере приближения к событию по порогам из админки | 38 |
| 46 | Йога — продажа занятий по количеству | Кнопки «Индивидуальное»/«Групповое» + выбор количества, без даты/счётчика; после оплаты — редирект к менеджеру | 39 |
| 47 | Консультации — продажа пакетов | Пакеты 1/4/8/12, цена за пакет из админки; после оплаты — редирект к менеджеру (без счётчика) | 39 |
| 48 | Рассылка участникам события | Рассылка только тем, у кого билет на конкретное событие | 38 |
| 49 | Согласие на обработку персональных данных | При первом входе — согласие + текст условий, факт согласия сохранён | 38 |
| 50 | Сквозное тестирование и выкат цикла 3 в прод | Все сценарии цикла 3 работают в проде + приёмка заказчиком | 38-49 |

---

## Этап 38 — Гарантийные баги + мелкие правки навигации

**Цель**: чиним расхождения с ТЗ цикла 2 (бесплатно, по гарантии) и наводим порядок в
навигации. Демо: заказчик открывает «Мероприятия» и видит все свои тестовые события; когда
места кончаются — событие не исчезает.

**Контекст**: заказчик завёл Баня 1, Баня 2 (с галочкой «Показывать заранее») и Ретрит 1, а
в боте видел только Баню 1 — ретрит не показывался вовсе (ТЗ цикла 2: «ретриты — все»).
После исчерпания мест в Бане 1 раздел опустел — ТЗ требовало «Мест нет» + кнопка «Запрос в
Поддержку», а событие пропадало.

**Что делаем**:
- **Баг показа событий**: ретриты показываются все; бани — ближайшая + отмеченные
  «Показывать заранее». Разобраться, почему ретрит и вторая баня отфильтровывались.
- **Баг нуля мест**: событие остаётся в списке с пометкой «Мест нет» и кнопкой
  «Запрос в Поддержку» вместо скрытия (скрывается только по дате/статусу).
- Кнопка «Главное меню» ведёт на `/start`-экран.
- Раздел `menu` убран (дублировал главное меню).
- Кнопка «Назад» на всех экранах — ведёт в предыдущее окно.
- «Мои билеты»: кнопка «Возврат» скрыта, вместо неё «Поддержка».

**Критерии приёмки** (закрытие → tag `stage-38-done`):
- [x] При заведённых бане и ретрите в боте видны оба (ретриты — все, баня — ближайшая + «показывать заранее»)
- [x] Событие с нулём свободных мест остаётся в списке с «Мест нет» и кнопкой «Запрос в Поддержку»
- [x] Кнопка «Главное меню» открывает стартовый экран
- [x] Раздел `menu` недоступен, ссылок на него не осталось
- [x] На каждом экране бота есть «Назад», ведущая в предыдущее окно (проверено обходом всех экранов)
- [x] В «Мои билеты» вместо «Возврат» — «Поддержка», ведёт в поддержку
- [x] Заказчик подтвердил приёмку вживую (2026-07-16, на живых данных прода v0.3.34)

---

## Этап 39 — Новое главное меню

**Цель**: главное меню по схеме заказчика. Демо: заказчик открывает бота и видит новую
раскладку; у подписчика верхняя кнопка называется иначе.

**Раскладка (из `прав.txt`)**:
- верхняя длинная кнопка **«Вступить в Клуб 11:11»**; если подписка активна — **«Клуб 11:11 — Новая реальность»**;
- ряд: **«Бани»** · **«Ретриты»**;
- ряд: **«Йога»** · **«Консультации»**;
- длинная: **«Пригласить друга»**;
- ряд: **«Мои билеты»** · **«Поддержка»**.

**Что делаем**:
- Переписать раскладку меню (`menu.welcome_kb` / `main_menu_kb`) под схему выше, сохранив
  контекстность по подписке (этап 31).
- Разделить «Мероприятия» на два входа — «Бани» и «Ретриты» (фильтр по направлению).
- «Пригласить друга» убрать из подменю «О клубе» на главную.
- Кнопки «Йога»/«Консультации» добавить **скрытыми** (видимость из админки, этап 19) —
  открываются на этапах 46/47.
- Подписи/видимость всех новых кнопок остаются правимыми из веб-админки.

**Критерии приёмки** (закрытие → tag `stage-39-done`):
- [x] Гость видит «Вступить в Клуб 11:11», подписчик — «Клуб 11:11 — Новая реальность»
- [x] Кнопки стоят рядами по схеме: Клуб · Бани/Ретриты · Йога/Консультации · Пригласить друга · Мои билеты/Поддержка
- [x] «Бани» открывает только бани, «Ретриты» — только ретриты
- [x] «Пригласить друга» есть на главной и её нет в «О клубе»
- [x] Кнопки «Йога»/«Консультации» скрыты и не видны пользователю
- [x] Подписи кнопок правятся из веб-админки и меняются в боте
- [x] Заказчик подтвердил приёмку вживую

---

## Этап 40 — Рефералка на все покупки + уведомление пригласившему

**Цель**: реф-программа перестаёт быть «только про баню». Демо: друг покупает ретрит/подписку
по ссылке → пригласившему приходит сообщение.

**Решение раунда 4 (блокер снят)**: суммы задаются в админке **по каждой категории**
(баня, ретрит, подписка, йога, консультации) и могут быть либо **фиксированной суммой**,
либо **процентом от суммы покупки** (для скидки новичку и для бонуса пригласившему).

**Что делаем**:
- Реф-скидка новичку и бонус пригласившему распространяются на все категории, значения
  задаются **по категории** в веб-админке (сейчас — глобальные «баня»-суммы).
- **Тип значения** на выбор: фиксированная сумма (₽) или процент от суммы покупки — для
  скидки и для бонуса отдельно.
- Момент начисления: за билет (баня/ретрит) — **после даты мероприятия** (как сейчас); за
  подписку, йогу и консультации (без даты) — **сразу после оплаты**.
- Уведомление пригласившему после покупки друга: «твой друг купил …, начислим ХХХ бонусов
  после ДД.ММ» (с датой — для билета) / «начислено ХХХ бонусов» (сразу — для покупок без даты).
- При переходе по реф-ссылке — надпись «скидка действует 24 часа» (без реального лимита).

**Критерии приёмки** (закрытие → tag `stage-40-done`):
- [x] Новичок по реф-ссылке получает скидку на ретрит, подписку, йогу и консультации (не только баню)
      — проверено вживую на ретрите (997 → 497 ₽) и подписке (2222 → 1722 ₽); йога/консультации
      покрыты инфраструктурно (правила + расчёт), фактическая проверка — с этапами 46/47
- [x] Суммы скидки/бонуса задаются по категории в веб-админке и применяются в боте
- [x] Значение можно задать и суммой, и процентом от покупки — оба варианта считаются верно
      (процент покрыт smoke-тестом `test_referral_rules`)
- [x] После покупки друга пригласившему приходит уведомление с суммой и (для билета) датой начисления
      — код проверен, но реальной оплатой на приёмке не прогонялось (платёж не доводили до конца)
- [x] За подписку/йогу/консультации бонус начисляется сразу, за билет — после даты события
      — то же: логика на месте, живой оплатой не прогонялось
- [x] При переходе по ссылке видна надпись про 24 часа
- [x] Заказчик подтвердил приёмку вживую

---

## Этап 41 — Раздел «Мои бонусы» + кнопки к автосообщениям

**Цель**: пользователь видит свои бонусы, а автосообщения перестают быть тупиковыми. Демо:
заказчик открывает «Мои бонусы» и видит три числа; в напоминании о событии жмёт «Мои билеты».

**Что делаем**:
- Раздел **«Мои бонусы»**: активные (можно тратить сейчас) · скоро начислится (ждут даты
  события) · всего за всё время.
- Кнопка **«Мои билеты»** в автонапоминании о событии.
- Кнопки **«Мои бонусы»** и **«Главное меню»** в сообщении о начислении бонусов.
- Вход в «Мои бонусы» — из главного меню / «Пригласить друга» (без перегруза верхнего уровня).

**Критерии приёмки** (закрытие → tag `stage-41-done`):
- [x] «Мои бонусы» показывает активные, будущие и суммарные бонусы; числа сходятся с прямым SQL
      — сверено на боевых данных (`@Polarasing`: доступно/всего 1000 ₽, будущих нет)
- [x] В напоминании о событии есть кнопка «Мои билеты», она открывает раздел
- [x] В сообщении о начислении бонусов есть «Мои бонусы» и «Главное меню», обе работают
- [x] Начисленный после события бонус переезжает из «скоро начислится» в «активные»
      — механика та же, что у джоба этапа 40 (qualified → accrued); отдельной оплатой
      билета с последующим сдвигом даты на приёмке не прогонялось
- [x] Заказчик подтвердил приёмку вживую

---

## Этап 42 — Уведомления админам о продажах

**Цель**: админы видят продажи в реальном времени, не заходя в админку. Демо: заказчик
покупает тестовый билет — ему в личку падает уведомление со сводкой.

**Формат (из переписки 11.07.2026)** — билет:

```
На мероприятие <Событие>
<Ник> купил билет (<тип: М / Ж / парный>) за <цена>
Всего продано <X> билетов из <мест>
Всего продано <X> билетов на сумму <ХХХ> рублей
```

Подписка (оплата / продление / выход за неуплату) — событие + строка:

```
Всего активных подписчиков <X> человек, на сумму <ХХХ> рублей
```

где сумма = **сколько каждый активный подписчик заплатил за последнее продление** (бесплатный
участник = 0). Пример заказчика: 3 подписчика (бесплатный + 3000 + 2000) — «3 человек, на
сумму 5000 рублей».

**Решение раунда 4**: строка «Всего продано Х билетов на сумму ХХХ» — **по этому
мероприятию** (заказчику важна заполненность и маржинальность конкретного события), обе
строки сводки считаются в разрезе события.

**Что делаем**:
- Уведомление админам после **оплаты билета**: событие, ник, тип билета, цена + сводка **по
  этому событию** (продано/мест) + продано билетов на сумму по этому событию.
- Уведомление админам после **оплаты/продления подписки** + сводка по активным подписчикам.
- Уведомление админам о **выходе за неуплату** (кик из группы по окончании) + та же сводка.
- Получатели — список админов из настроек веб-админки (этап 27); недоступный админ
  (заблокировал бота) не валит отправку остальным.
- Сводки считаются в SQL одним запросом (без N+1), «последний платёж» — последняя успешная
  оплата активного подписчика.

**Критерии приёмки** (закрытие → tag `stage-42-done`):
- [x] После оплаты билета всем админам приходит уведомление: событие, ник, тип билета, цена
- [x] В уведомлении о билете верны «продано X из N» и «продано X билетов на сумму YYY» (сходится с админкой)
      — сводки сверены с прямым SQL на боевых данных (прогон боевых функций в контейнере)
- [x] После оплаты и после продления подписки админам приходит уведомление
- [x] При выходе участника за неуплату админам приходит уведомление
- [x] В каждом уведомлении по клубу есть «Всего активных подписчиков X человек, на сумму YYY», сумма считается по последнему платежу каждого (бесплатный = 0)
      — логика проверена на синтетике в откаченной транзакции (2 чел., 3000 ₽ по последнему платежу)
- [x] Список получателей берётся из настроек админки; заблокировавший бота админ не ломает рассылку остальным
- [x] Заказчик подтвердил приёмку вживую

**Хвост (не блокер)**: аннулирование подписки из веб-админки идёт тем же путём
(`end_date=now()` → джоб), поэтому админам приходит «выход за неуплату». Заказчик принял
как есть; при желании развести формулировки — отдельная правка.

---

## Этап 43 — Тариф подписки: вход/продление (без ступеней по местам)

**Цель**: вход в клуб дороже продления, ценовая модель упрощается до двух цен. Демо: новичок
видит цену входа, подписчик при продлении — цену продления.

**Решение раунда 4 (блокер снят)**: **авто-ступени по числу занятых мест УБИРАЮТСЯ совсем**.
Заказчик регулирует стоимость (в т.ч. для первых участников) **промокодами**. Остаются две
цены: **вход** (первый месяц) и **продление**. Это упрощение цикла 1, а не усложнение.

**Контекст / риск**: убрать расчёт ставки по местам нужно аккуратно — сейчас за активными
подписчиками зафиксирована ступенчатая ставка (цикл 1, этап 5). При переходе:
- существующие активные подписки не должны сломаться/подорожать задним числом — их продление
  считается по новой цене «продления»;
- механизм фиксации/сброса ставки при перерыве переиспользуется, но фиксируется теперь
  признак «активная непрерывная подписка», а не числовая ступень.

**Что делаем**:
- В веб-админке — две цены: **вход** и **продление** (вместо таблицы ступеней по местам).
- Расчёт ставки по числу занятых мест из цикла 1 отключается; экраны тарифов/напоминаний и
  создание платежа берут цену входа (для новичка / после перерыва) или продления (для
  непрерывного подписчика).
- **Перерыв** (подписка кончилась и не продлена) → следующий вход снова по цене входа —
  переиспользует существующий сброс при перерыве.
- Проверить, что промокодами можно опустить цену входа для первых участников (замена прежней
  «ступени для первых мест»).

**Критерии приёмки** (закрытие → tag `stage-43-done`):
- [x] В админке задаются ровно две цены: вход и продление; таблицы ступеней по местам больше нет
- [x] Новый пользователь видит и платит цену входа
- [x] Подписчик, продлевающий без перерыва, видит и платит цену продления
- [x] После перерыва тот же пользователь снова платит цену входа _(маршрут: нет активной подписки → вход; переиспользует сброс при перерыве цикла 1)_
- [x] Существующие активные подписки не подорожали/не сломались при переходе (продление по новой цене) _(сид миграции 0027: вход=продление=прежняя входная ставка 4444, задним числом ничего не изменилось)_
- [x] Промокодом можно снизить цену входа (регулировка для первых участников)
- [x] Заказчик подтвердил приёмку вживую _(2026-07-30, прод v0.3.41)_

**Хвост (не блокер):** был баг ввода цены с пробелом-разделителем тысяч («4 444» → «числа больше нуля»), пофикшен `0098aaf` (v0.3.41) — парсер убирает любые пробелы. Скидка подписчикам на продление конкретной суммой — отдельно в этапе 44.

---

## Этап 44 — Скидка подписчикам на продление + абсолютные цены

**Цель**: скидки задаются суммой, а не только процентом; промокод не складывается со скидкой.

**Что делаем**:
- В админке у мероприятия и у подписки можно задать **конкретные значения**: полная цена и
  цена для подписчика (в дополнение к текущему проценту).
- Скидка активным подписчикам распространяется и на **продление подписки**.
- **Приоритет промокода**: если у подписчика действует фиксированная промо-цена, скидки не
  суммируются — платит по промокоду (уточнение правила «максимальной скидки» цикла 2).

**Объём согласован (2026-07-30)**: по смете п.8–9 — **только подписка + приоритет
промокода**; мероприятия не трогаем (скидка билета остаётся процентом цикла 2).
«Абсолютные суммы» подписки уже даёт этап 43 (вход = полная цена, продление = цена
подписчика). Новое в этапе 44 — приоритет фикс-промо-цены на продлении.

**Критерии приёмки** (закрытие → tag `stage-44-done`):
- [x] В админке задаётся полная цена и цена для подписчика конкретной суммой — вход/продление в bot_settings, правятся в «Тарифы» (реализовано этапом 43)
- [x] Активный подписчик видит цену продления со скидкой, гость — полную — вход (гость/новичок) vs продление (подписчик); единый расчёт `tariffs.renewal_monthly_for`
- [x] При действующей фиксированной промо-цене подписчик платит по промокоду (скидки не сложились) — `subscriptions.price_locked` (миграция 0028) + `renewal_monthly_for`; до этапа 44 продление ошибочно игнорировало фикс-промо-цену. Юнит + живой прогон в dev пройдены; на проде путь живой оплатой фикс-промокода **не прогонялся** (0 из 4 подписок залочены — фикс-промокодом никто не покупал), проверить при первой реальной фикс-промо-продаже
- [x] Процентные скидки цикла 2 продолжают работать (нет регресса) — движок скидок билетов не трогали
- [x] Заказчик подтвердил приёмку вживую — команда «закрыть этап» после выката `v0.3.42` (`dff6ab0`) в прод 2026-07-30

---

## Этап 45 — Динамические цены ранним покупателям

**Цель**: чем раньше куплен билет — тем дешевле. Демо: заказчик выставляет пороги и видит,
как цена меняется по мере приближения к дате.

**Пример заказчика**: за неделю до события — 1000, за 3 дня — 1500, в день события — 2000.

**Что делаем**:
- В админке у мероприятия — **пороги по дням до события** (список: «за N дней — цена»),
  цена вне порогов = базовая цена типа билета.
- Бот считает актуальную цену на момент покупки и показывает её на экране выбора билета.
- Совместимость со скидкой подписчика/промокодом/бонусами: динамическая цена — это **базовая
  цена момента**, дальше применяются правила стекинга цикла 2 (макс. скидка, затем бонусы ≤50%).
- Цена фиксируется в момент создания платежа (изменение порогов не меняет уже выставленный счёт).

**Реализация (dev, 2026-07-30, `v0.3.43`, коммит `951b46d`)**: пороги привязаны к
**типу билета** — таблица `event_price_tiers(event_id, ticket_type, days_before, price)`.
Цена момента (`services.events.price_for_day`): среди порогов типа с `days_before >= дней
до события` берётся тариф с наименьшим `days_before` (ближайший к дате); вне порогов —
базовая цена типа из `event_ticket_prices`. Так цена растёт при приближении даты, а
событие без порогов продаётся по базе (нет регресса). Единый расчёт для показа и оплаты —
`tickets.event_prices_now` (заменил прямое чтение базовых цен во всех точках покупки),
цена фиксируется при создании платежа. Админка — сетка порогов в форме мероприятия
(«за N дней» + цена по каждому типу, добавление строк). Для бани с одним типом это ровно
пример заказчика; многотиповые ретриты не смешивают цены. Проверено в dev: pure-тест
`tests/test_event_price_tiers`, живой прогон `event_prices_now` (цена момента + fallback),
смоук парсинга формы (валид + дубль порога + «дни без цены»).

**Критерии приёмки** (закрытие → tag `stage-45-done`; отмечать только после живой верификации):
- [x] Пороги «за N дней — цена» задаются в админке по каждому мероприятию
- [x] Цена в боте соответствует текущему порогу и растёт при приближении даты
- [x] Скидка подписчика/промокод/бонусы считаются от актуальной цены момента по правилам цикла 2
- [x] Уже созданный платёж не меняет сумму при смене порогов
- [x] Мероприятие без порогов продаётся по базовой цене (нет регресса)
- [x] Заказчик подтвердил приёмку вживую (2026-07-30, прод `v0.3.43`)

---

## Этап 46 — Йога: продажа занятий по количеству

**Цель**: продажа онлайн-занятий йоги без даты и мест. Демо: заказчик открывает «Йога» →
покупает индивидуальное/групповое занятие с выбором количества → после оплаты видит контакт
менеджера.

**Решение раунда 4**: йога — **НЕ событие** (не как баня/ретрит). Онлайн, два продукта:
**индивидуальное** и **групповое** занятие. Пользователь выбирает продукт и **количество**,
без привязки к дате. Запись и учёт остатка ведёт менеджер **@zhannazlotnikova** в личке —
**счётчик остатка в боте не нужен**. Сверху раздела — описание (по каким дням/времени идут
занятия), редактируется как экран (этап 22).

**Что делаем**:
- Раздел «Йога» в боте (кнопка из этапа 39 открывается): описание сверху + две кнопки
  «Индивидуальное» / «Групповое» → выбор количества → цена = цена продукта × количество → оплата.
- В веб-админке — цена индивидуального и группового занятия (+ скидка подписчику как у
  мероприятий; участие в рефералке — этап 40).
- После оплаты — сообщение с редиректом к менеджеру (контакт из настроек админки, не хардкод);
  доступа в группу и «билета» не даёт, счётчик не ведётся.
- Продажи йоги попадают в уведомления админам (этап 42) и статистику дохода по направлению.

**Реализация (dev, 2026-07-30, v0.3.44, миграция 0030):**
- Общая таблица `service_products` (category/code/title/price/subscriber_discount_percent/
  is_active) — переиспользуется консультациями (этап 47). Сид: два продукта йоги
  «Индивидуальное»/«Групповое» с **плейсхолдер-ценами** (2000/1000 ₽) — админ правит в разделе «Йога».
- Платёж услуги — общий конвейер, новый `payments.kind='service'` + колонки
  `service_product_id`/`quantity`; `event_id`/`ticket_id` не заполняются (билет/место не выдаются).
  Активация (`repo.activate_service_payment`) лишь фиксирует оплату — без выдачи билета.
- Движок цены/платежа — `services/service_sales.py` (`compute_service_pricing`,
  `start_service_payment`, `sync_service_payment`); поллер (`payments.poll_pending`) диспетчеризует
  `kind='service'`. Скидка подписчику (процент продукта) и скидка новичка НЕ суммируются —
  максимальная (`ev.best_ticket_price`). Промокоды/бонусы на услуги — отложены на этап 47.
- Бот: `handlers/yoga.py` (раздел `NAV_YOGA` больше не заглушка): описание (экран `yoga`,
  правится в «Экранах бота») + форматы → выбор количества (инлайн 1–8) → сводка → оплата →
  сообщение с кнопкой «Написать менеджеру» (контакт из настроек, дефолт @zhannazlotnikova).
- Рефералка (этап 40): категория `yoga` — мгновенная, бонус пригласившему начисляется сразу.
- Уведомление админам `admin_alerts.service_sold` (этап 42); статистика — новое направление
  «Йога» (`stats` по `service_products.category`).
- Веб-админка: раздел «Йога» (цена/скидка/вкл-выкл форматов), контакт менеджера — в «Настройках».
- Тесты: `tests/test_service_sales.py` (цена×кол-во, максимум скидок, мгновенная рефералка) — зелёные.
- _Решение по объёму (раунд 4, ROADMAP): для йоги — скидка подписчику + рефералка; промокоды и
  бонусы явно НЕ требуются (они в списке этапа 47). Выбор количества — инлайн-кнопки 1–8._

**Критерии приёмки** (закрытие → tag `stage-46-done`):
- [x] Цены индивидуального и группового занятия задаются в веб-админке
- [x] Кнопка «Йога» открывает раздел с описанием сверху и двумя продуктами
- [x] Пользователь выбирает количество, платит цену = цена × количество
- [x] После оплаты приходит сообщение с контактом менеджера (@zhannazlotnikova, из настроек)
- [x] Счётчик остатка в боте не показывается; «билет»/доступ в группу не выдаётся
- [x] Продажа учтена в уведомлениях админам и статистике по направлению
- [x] Заказчик подтвердил приёмку вживую (2026-07-30, прод `v0.3.44`)

---

## Этап 47 — Консультации: продажа пакетов

**Цель**: продажа пакетов консультаций. Демо: заказчик задаёт цены пакетов → пользователь
покупает пакет → после оплаты видит контакт менеджера.

**Решение раунда 4**: консультации — **НЕ событие**. Календарь **не нужен**, счётчик остатка
в боте **не нужен**. После оплаты все детали (время, контроль остатка) решаются в личке с
менеджером **@zhannazlotnikova**. Тот же паттерн, что йога (этап 46) — общий механизм
«покупка услуги по количеству без даты/счётчика + редирект к менеджеру».

**Что делаем**:
- В админке — пакеты консультаций: **1 / 4 / 8 / 12** (число + цена пакета вручную; выгода
  за штуку задаётся ценой, не формулой). Возможность включить/выключить пакеты.
- Раздел «Консультации» в боте: выбор пакета → оплата → сообщение с редиректом к менеджеру.
- Контакт менеджера — из настроек админки (общий с этапом 46). Без счётчика и «билета».
- Скидка подписчика / промокод / бонусы / рефералка — по общим правилам (циклы 2, этап 40).
- Продажи попадают в уведомления админам (этап 42) и статистику по направлению.

**Реализация** (dev, ждёт выката и приёмки, `v0.3.45`): пакеты консультаций —
`service_products` category='consult' (миграция `0031`, сид 1/4/8/12 с плейсхолдер-
ценами); общий движок `services.service_sales` расширен промокодами и бонусами (для
йоги остаются выключенными). Раздел бота — `handlers/consult.py` (пакет → сводка со
скидкой участника / промокодом / бонусами → оплата → редирект к менеджеру
@zhannazlotnikova); счётчика/календаря/билета нет. Веб-админка — раздел
«Консультации» (`/consult`: цена пакета, скидка участника, вкл/выкл). Продажа идёт в
уведомления админам (этап 42, «× 1» скрыт) и статистику направления «Консультации».
Тест `tests/test_consult_sales.py` (промокод+бонусы, стекинг «максимум»). Кнопка
«Консультации» по умолчанию скрыта (механизм видимости, этап 19) — админ включает на
приёмке. Заглушка `nav_coming_soon` убрана.

**Критерии приёмки** (закрытие → tag `stage-47-done`): ✅ пройдены 2026-07-31, прод `v0.3.45`
- [x] Пакеты 1/4/8/12 с ценами задаются и включаются/выключаются в админке
- [x] Пользователь выбирает пакет, оплачивает
- [x] После оплаты приходит сообщение с контактом менеджера (из настроек)
- [x] Счётчик остатка и календарь в боте отсутствуют (по решению заказчика)
- [x] Скидка подписчика и промокод применяются к пакету по правилам цикла 2
- [x] Продажа учтена в уведомлениях админам и статистике по направлению
- [x] Заказчик подтвердил приёмку вживую (2026-07-31)

---

## Этап 48 — Рассылка участникам события

**Цель**: адресная рассылка. Демо: «сегодня баня — отправить инфо только участникам этой бани».

**Что делаем**:
- В разделе «Рассылка» веб-админки (этап 26) — выбор аудитории: все · подписчики ·
  **участники конкретного события** (выпадающий список событий).
- Переиспользует существующий движок рассылки (текст/фото/альбомы, отчёт о доставке).
- Отправка только тем, у кого **оплаченный** билет на выбранное событие (возвращённые — нет).

**Реализация** (dev, `v0.3.46`): аудитория «Участникам события» в форме рассылки (этап 26) —
выпадающий список событий (предстоящие + прошедшие с участниками, со счётчиком получателей).
В БД кодируется как `event:<id>` (`repo.AUDIENCE_EVENT_PREFIX`); джоб отправки
(`services/broadcasts`) резолвит её через `repo.get_audience_ids` → `DISTINCT tg_id` владельцев
билетов `status='paid'` (refunded исключены, дедуп по человеку, без заблокированных). Список
рассылок показывает «Событие: <название>». Схема не менялась (миграции нет). Проверено на dev:
`event:5` (2 paid + 2 refunded) → 2 получателя, `event:8` → 1, дедуп по юзеру, рендер формы/списка.
Выкачено в прод `v0.3.46` (`72f16a2`), приёмка пройдена 2026-07-31.

_Примечание: «Йога»/«Консультации» — не события (услуги «по количеству», без билетов), поэтому
в выпадашке событий отсутствуют намеренно; рассылка покупателям услуг этапом 48 не покрывается
(при желании — отдельная доработка сверх блока 40k, рядом с этапом 51)._

**Критерии приёмки** (закрытие → tag `stage-48-done`):
- [x] В рассылке можно выбрать конкретное событие как аудиторию ✅ пройдено 2026-07-31, прод `v0.3.46`
- [x] Сообщение приходит только владельцам оплаченных билетов на это событие ✅ пройдено 2026-07-31, прод `v0.3.46`
- [x] Владельцы возвращённых билетов и участники других событий сообщение не получают ✅ пройдено 2026-07-31, прод `v0.3.46`
- [x] Отчёт о доставке показывает верное число получателей ✅ пройдено 2026-07-31, прод `v0.3.46`
- [x] Заказчик подтвердил приёмку вживую ✅ пройдено 2026-07-31, прод `v0.3.46`

---

## Этап 49 — Согласие на обработку персональных данных

**Цель**: закрыть требование 152-ФЗ. Демо: новый пользователь при первом входе видит согласие.

**Что даёт заказчик**: текст «Политики обработки персональных данных» (юридическая часть —
на стороне заказчика, мы встраиваем).

**Что делаем**:
- Экран согласия при первом входе: кнопка «Согласен» + ссылка/экран с текстом условий.
- Факт согласия сохраняется у пользователя (дата + версия текста).
- Текст условий редактируется из веб-админки (реестр экранов, этап 22).
- Существующие пользователи не блокируются задним числом (согласие спрашивается при
  следующем входе, без потери доступа).

**Реализация (2026-07-31, dev `654a93d`, `v0.3.47`)**:
- Гейт на стартовом экране: `/start` и `/menu` до согласия показывают экран `pd_consent`
  (текст «Политики» + кнопка «Согласен»), дальше не пускают. Реф-привязка по deep-link
  срабатывает до гейта (код из ссылки не теряется).
- Факт согласия — `users.pd_consent_at` + `pd_consent_version` (миграция 0032). Версия в
  `services/consent.PD_CONSENT_VERSION` (=1): существенно изменили текст → бамп → согласие
  спросят заново. Старое согласие с меньшей версией не засчитывается.
- Текст «Политики» — экран `pd_consent` в реестре (правится из веб-админки, этап 22); дефолт
  в `texts.PD_CONSENT` — **плейсхолдер**, финальную редакцию даёт заказчик.
- Существующие пользователи не блокируются: NULL-согласие → спросим при следующем входе,
  подписка/доступ в группу не затрагиваются (гейт только на экране бота).
- Проверено на dev: миграция применилась; гейт-логика (нет → показать · сохранение даты+версии
  · повторно не спрашиваем · бамп версии → спросить заново) — ALL PASS; экран виден в редакторе
  веб-админки.
- **Выкачено в прод 2026-07-31**: `master` `e30f536`, `v0.3.47`, миграция 0032 применилась на
  боевой БД, бот `@Club_11_bot` стартовал чисто (polling). **Ждёт живой приёмки заказчиком** +
  вставки финального текста «Политики» через веб-админку.

**Критерии приёмки** (закрытие → tag `stage-49-done`):
- [ ] Новый пользователь при первом входе видит согласие и текст условий
- [ ] После нажатия «Согласен» факт и дата сохранены в БД
- [ ] Повторно согласие не спрашивается
- [ ] Текст условий правится из веб-админки
- [ ] Заказчик подтвердил приёмку вживую

---

## Этап 50 — Сквозное тестирование и выкат цикла 3 в прод

**Цель**: весь цикл 3 живёт в проде и принят заказчиком.

**Что делаем**:
- Сквозные сценарии: новое меню — покупка билета (баня/ретрит/йога) — уведомление админам —
  рефералка — бонусы; вступление в клуб по цене первого месяца — продление со скидкой —
  перерыв — снова первый месяц; рассылка по событию; согласие на ПД.
- Проверка отсутствия регресса в функционале циклов 1–2.
- Бэкап боевой БД до миграций, мерж `dev→master` (авто-деплой), проверка логов и версии.

**Критерии приёмки** (закрытие → tag `stage-50-done`):
- [ ] Все сценарии цикла 3 проходят в проде на реальном боте
- [ ] Регресса в подписках, билетах и админке циклов 1–2 нет
- [ ] Миграции применились, бэкап до выката снят
- [ ] Заказчик подтвердил приёмку вживую

---

## Этап 51 — Лог покупок услуг в разделах «Йога»/«Консультации»

**Цель**: админ может самостоятельно проверить продажи услуги (кто, что, сколько занятий/
пакетов, на сумму) — не полагаясь только на ленту уведомлений. Демо: заказчик открывает
раздел «Йога» → видит список продаж с поиском по покупателю и фильтрами, итог «продано N
занятий на сумму X, Y покупателей».

**Контекст**: запрос заказчика на приёмке этапа 46 (2026-07-30). У услуг намеренно нет счётчика
остатка в боте (запись ведёт менеджер в личке, решение раунда 4), но админам нужен **журнал
продаж** для сверки («правда ли человек купил 10 занятий»). Уведомления (этап 42) и доход по
направлению на дашборде (этапы 20/46) это не закрывают — нет поиска/фильтров/поимённого списка.

**Решение заказчика (2026-07-30)**: делать **отдельным этапом после Консультаций** (чтобы лог
сразу охватил обе услуги); блок лога — **внутри разделов «Йога» и «Консультации»** (свой блок в
каждом, а не единый раздел). **Это доработка сверх согласованного блока цикла 3 (этапы 38–50,
40 000 ₽) — оценивается отдельно.**

**Что делаем**:
- В веб-разделе «Йога» (и «Консультации», этап 47) — блок «Продажи»: таблица успешных продаж
  услуги этой категории: дата · покупатель (имя / @username / Telegram ID) · формат/пакет ·
  количество · сумма. Данные — из `payments` (`kind='service'`, `status='succeeded'`) + JOIN
  `service_products`/`users`; дозапись не нужна, лог доступен и по прошлым продажам.
- **Поиск** по покупателю (имя/username/ID) + **фильтры**: период покупки, формат/пакет.
- **Итоги по текущему фильтру**: «продано N занятий/пакетов на сумму X ₽, Y покупателей».
- **Пагинация** + выгрузка **CSV** (по образцу раздела «Билеты», этапы 32/33).
- Общий слой данных (repo-запрос с параметром категории) — не дублировать SQL между йогой и
  консультациями, несмотря на два UI-блока.

**Критерии приёмки** (закрытие → tag `stage-51-done`):
- [ ] В разделе «Йога» виден список продаж йоги; в «Консультациях» — продаж консультаций
- [ ] Поиск по покупателю (имя/username/ID) и фильтры (период, формат/пакет) работают
- [ ] Итоги «продано N на сумму X, Y покупателей» считаются по текущему фильтру
- [ ] Пагинация и выгрузка CSV работают
- [ ] Прошлые (уже совершённые) продажи попадают в лог
- [ ] Заказчик подтвердил приёмку вживую
