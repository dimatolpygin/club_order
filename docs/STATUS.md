# STATUS — текущее состояние проекта

> **Для Claude и человека**: это первый файл, который читается в новой сессии. После
> прочтения **обязательно** сверить с реальностью: `git tag -l "stage-*"`,
> `git log --oneline -20` — таблица ниже может быть устаревшей.

**Последнее обновление**: 2026-07-31 (Этап 48 реализован в dev — рассылка участникам события `v0.3.46`, ждёт выката и приёмки)
**Текущий этап**: Цикл 3, Этап 48 — Рассылка участникам события (🚧 реализован в dev `v0.3.46`, **не выкачен, не принят**). Этап 47 закрыт (тег `stage-47-done`, `8dee7bf`, прод `v0.3.45`). Циклы 1–2 (этапы 0–37) закрыты. Разработка — в `dev`, в `master` мержить только по явному указанию (авто-деплой).
**Следующий шаг**: выкатить этап 48 в прод (`dev→master`, авто-деплой) и провести живую приёмку по чек-листу — отправить рассылку аудитории «Участникам события» на тестовое событие, убедиться, что получают только владельцы оплаченных билетов, а возвращённые/чужие — нет; отчёт показывает верное число. Критерии — [`07_ROADMAP.md`](07_ROADMAP.md#этап-48--рассылка-участникам-события). Прод: бот `@Club_11_bot` `v0.3.45` + веб-админка `https://admin.agat-key.ru` на `163.5.153.150`; логин/секрет — в `.secrets/server.env` (`ADMIN_WEB_*`).

---

## Сводка этапов

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|------|--------|-----------------|------------------|---------------|
| 0 | Каркас репозитория | ✅ | `stage-0-done` | `1a1edba` | 2026-06-12 |
| 1 | Навигация и инфо-экраны | ✅ | `stage-1-done` | `1b33982` | 2026-06-12 |
| 2 | Тарифы и места (админ-конфиг) | ✅ | `stage-2-done` | `92b1344` | 2026-06-12 |
| 3 | Оплата ЮKassa + polling | ✅ | `stage-3-done` | `43c2af0` | 2026-06-12 |
| 4 | Доступ в группу (join-request) | ✅ | `stage-4-done` | `f079224` | 2026-06-12 |
| 5 | Фиксация цены, продление, авто-удаление | ✅ | `stage-5-done` | `223bf40` | 2026-06-13 |
| 6 | Напоминания (крон 3/1/0 дней) | ✅ | `stage-6-done` | `672cd87` | 2026-06-13 |
| 7 | Промокоды | ✅ | `stage-7-done` | `291982d` | 2026-06-13 |
| 8 | Админ-панель `/admin` | ✅ | `stage-8-done` | `d8a7777` | 2026-06-13 |
| 9 | Деплой CI/CD | ✅ | `stage-9-done` | `3f3afd8` | 2026-06-14 |
| 10 | Перенос на сервер с доступом к ЮKassa + автодеплой | ✅ | `stage-10-done` | `8d696c8` | 2026-06-14 |

### Цикл 2 — «Фокус.Энергия» (билеты) + рефералка + веб-админка

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|------|--------|-----------------|------------------|---------------|
| 11 | Каркас веб-админки + авторизация + https | ✅ | `stage-11-done` | `b820fdc` | 2026-06-26 |
| 12 | Админка: мероприятия (CRUD) | ✅ | `stage-12-done` | `76be5f3` | 2026-06-22 |
| 13 | Бот: «Мероприятия» + показ + выбор билета | ✅ | `stage-13-done` | `1d674b7` | 2026-06-23 |
| 14 | Бот: оплата билета + билет + учёт мест | ✅ | `stage-14-done` | `fe1c252` | 2026-06-23 |
| 15 | Скидка подписчикам на билеты | ✅ | `stage-15-done` | `d717812` | 2026-06-23 |
| 16 | Промокоды на билеты + стекинг (макс. скидка) | ✅ | `stage-16-done` | `d373c5f` | 2026-06-23 |
| 17 | Реферальная программа (бани) + бонусы | ✅ | `stage-17-done` | `70ec13f` | 2026-06-23 |
| 18 | Мои билеты + запрос на возврат | ✅ | `stage-18-done` | `a9d4e72` | 2026-06-25 |
| 19 | Админка: подписки + кнопки меню | ✅ | `stage-19-done` | `d37795d` | 2026-06-25 |
| 20 | Админка: статистика (дашборд) + уведомления | ✅ | `stage-20-done` | `8b84b79` | 2026-06-25 |
| 21 | Веб-админка: билеты + отметка фактического возврата | ✅ | `stage-21-done` | `06a4af9` | 2026-06-25 |
| 21.1 | Возвраты: связь с покупателем + уведомление о возврате | ✅ | `stage-21.1-done` | `cb2a89f` | 2026-06-25 |
| 22 | Веб-админка: редактор экранов бота (тексты + кнопки) | ✅ | `stage-22-done` | `572d32e` | 2026-06-25 |
| 23 | Веб-админка: тарифы (ступени + цены за периоды + длительности) | ✅ | `stage-23-done` | `1507603` | 2026-06-25 |
| 24 | Веб-админка: промокоды подписки (CRUD) | ✅ | `stage-24-done` | `9cd7b0d` | 2026-06-26 |
| 25 | Веб-админка: участники (поиск/карточка/история + добавление + аннулирование) | ✅ | `stage-25-done` | `15ce064` | 2026-06-26 |
| 26 | Веб-админка: рассылка (текст/фото/альбомы через бота) | ✅ | `stage-26-done` | `3d92d23` | 2026-06-26 |
| 27 | Веб-админка: настройки (напоминания + поддержка + админы + диагностика) | ✅ | `stage-27-done` | `bb8df7a` | 2026-06-26 |
| 28 | Дашборд: графики-распределения (бублики) + переключатель графиков | ✅ | `stage-28-done` | `f88aebd` | 2026-06-26 |
| 29 | Дашборд: графики динамики (столбчатые) — регистрации и активность по датам | ✅ | `stage-29-done` | `31f6623` | 2026-06-26 |
| 30 | Сквозное тестирование и выкат в прод | ✅ | `stage-30-done` | `b820fdc` | 2026-06-26 |
| 31 | UX меню: контекстное мини-меню (снизить перегруз) | ✅ | `stage-31-done` | `7b97f5a` | 2026-06-27 |
| 32 | Веб-админка «Билеты»: поиск/фильтры/пагинация/CSV на вход | ✅ | `stage-32-done` | `7b97f5a` | 2026-06-27 |
| 33 | Номер билета (бот + поиск в админке) + фикс вёрстки даты | ✅ | `stage-33-done` | `3d2f201` | 2026-06-27 |
| 34 | Чек-ин на точке: онлайн-отметка «Пришёл» + адаптив под мобилку | ✅ | `stage-34-done` | `10b9e57` | 2026-06-27 |
| 35 | Адаптив всей веб-админки под мобильный (меню + дашборд/таблицы/формы) | ✅ | `stage-35-done` | `fbee758` | 2026-06-27 |
| 36 | «Мероприятия»: поиск + фильтры (направление/статус) + пагинация архива | ✅ | `stage-36-done` | `821dcfc` | 2026-06-27 |
| 37 | Картинка к экранам бота (опц., без поломки навигации edit/send) | ✅ | `stage-37-done` | `345fcd9` | 2026-06-27 |

### Цикл 3 — правки заказчика (июль 2026)

Оплачен единым блоком (40 000 ₽); гарантийные баги (этап 38) — бесплатно. Источники:
`прав.txt`, переписка 11.07.2026, смета `docs/смета_правки_2026-07.csv`.

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|------|--------|-----------------|------------------|---------------|
| 38 | Гарантийные баги + мелкие правки навигации | ✅ | `stage-38-done` | `fc550e8` | 2026-07-16 |
| 39 | Новое главное меню | ✅ | `stage-39-done` | `42191e0` | 2026-07-16 |
| 40 | Рефералка на все покупки + уведомление пригласившему | ✅ | `stage-40-done` | `c1dc58d` | 2026-07-23 |
| 41 | «Мои бонусы» + кнопки к автосообщениям | ✅ | `stage-41-done` | `a2a247a` | 2026-07-23 |
| 42 | Уведомления админам о продажах | ✅ | `stage-42-done` | `2d21194` | 2026-07-23 |
| 43 | Тариф подписки: вход/продление (без ступеней по местам) | ✅ | `stage-43-done` | `0098aaf` | 2026-07-30 |
| 44 | Скидка подписчикам на продление + приоритет промокода | ✅ | `stage-44-done` | `dff6ab0` | 2026-07-30 |
| 45 | Динамические цены ранним покупателям | ✅ | `stage-45-done` | `1156f6d` | 2026-07-30 |
| 46 | Йога — продажа занятий по количеству | ✅ | `stage-46-done` | `78dff05` | 2026-07-30 |
| 47 | Консультации — продажа пакетов | ✅ | `stage-47-done` | `8dee7bf` | 2026-07-31 |
| 48 | Рассылка участникам события | 🚧 | `stage-48-done` | dev `v0.3.46` | — |
| 49 | Согласие на обработку персональных данных | ☐ | `stage-49-done` | — | — |
| 50 | Сквозное тестирование и выкат цикла 3 в прод | ☐ | `stage-50-done` | — | — |
| 51 | Лог покупок услуг в разделах «Йога»/«Консультации» (сверх блока 40k) | ☐ | `stage-51-done` | — | — |

Детальные критерии приёмки каждого этапа — в [`07_ROADMAP.md`](07_ROADMAP.md).

## Активная работа

**Цикл 3 в работе (2026-07-31): этапы 38–47 закрыты, следующий — 48 (рассылка по событию).** 13 этапов (38–50) по
правкам заказчика от 09.07.2026 (`прав.txt`) + дополнениям из переписки 11.07.2026 (кнопки к
автосообщениям, уведомления админам о продажах). Оплата — единым блоком 40 000 ₽; этап 38
(гарантийные баги) бесплатно. Смета — `docs/смета_правки_2026-07.csv`, согласования —
`docs/вопросы_заказчику.md` (раунд 4).

**Блокеров нет** — заказчик ответил на раунд 4 (11.07.2026), решения занесены в ROADMAP
(этапы 40/42/43/46/47) и `docs/вопросы_заказчику.md`. Ключевое: ступени цены по местам
убираем совсем (этап 43 — упрощение цикла 1, регулировка промокодами); йога и консультации —
не события, а покупка «по количеству» без даты/счётчика + редирект к менеджеру
@zhannazlotnikova (этапы 46/47); реф-суммы — по категориям, суммой или процентом (этап 40).

**Этапы 38–47 закрыты** — детали в «Истории закрытий» ниже. Порядок дальше: **48 (рассылка по
событию)**, 49 (согласие ПД); 50 — сквозняк и выкат; **51 — лог покупок услуг** (запрос заказчика
от 30.07.2026, **сверх блока 40k, оценка отдельно**): журнал продаж йоги/консультаций внутри их
разделов (поиск/фильтры/итоги/CSV) — делаем после этапа 47, чтобы охватить обе услуги. Детали и
критерии — `07_ROADMAP.md` этап 51.

**Этап 48 реализован в dev (`v0.3.46`), ждёт выката и приёмки.** В разделе «Рассылка»
веб-админки (этап 26) добавлена аудитория «Участникам события» — выпадающий список событий
(предстоящие + прошедшие с участниками, со счётчиком получателей) поверх существующего движка
(текст/фото/альбомы, отчёт о доставке). Аудитория кодируется в БД как `event:<id>`; отправка —
только владельцам **оплаченных** билетов (refunded исключены, дедуп по человеку, без
заблокированных). Схема БД не менялась (миграции нет). Проверено на dev-стеке: `event:5`
(2 paid + 2 refunded) → 2 получателя, `event:8` → 1, дедуп по юзеру, рендер формы/списка,
существующие сегменты и тесты событий не задеты. **Дальше**: `dev→master` (выкат) + живая
приёмка по чек-листу ниже, затем тег `stage-48-done`. Следом — 49 (согласие ПД).

Чек-лист приёмки этапа 48 (после выката):
1. В «Рассылке» выбрать «Участникам события» → появляется выпадашка событий со счётчиком.
2. Отправить на тестовое событие (напр. «Тест 2» — 2 оплаченных + 2 возвращённых билета).
3. Получают ровно 2 человека — владельцы **оплаченных** билетов; возвращённые и участники
   других событий не получают; отчёт показывает «отпр. 2 / 2».

## Известные хвосты (не блокеры)

- **`dd44cef` вне этапов (выкачен).** Коммит «поиск участника в Подписках + bump 0.3.33»
  не привязан ни к одному этапу. Вопреки прежней записи здесь, он был выкачен в прод ещё
  ~02.07.2026 (сервер стоял на `dd44cef`/`v0.3.33` до выката этапа 38) — STATUS это проглядел.
  Теперь в проде вместе с этапом 38 (`v0.3.34`). Функцию поиска в «Подписках» проверить на
  приёмке цикла 3.
- **Подписки (этап 19) — нет PRG.** Кнопки «Продлить»/«Отключить» в `/subscriptions`
  рендерят ответ прямо на POST (как было в билетах до фикса 21.1). Обновление страницы
  (F5) может повторно продлить/отключить подписку. Латентный баг, заказчик в курсе;
  починить тем же Post/Redirect/Get при случае (events extend/disable аналогично стоит
  проверить).
- **Тестовая переписка с Gats (id 7060768358)** в `manager_messages` содержит дубли
  сообщений, созданные ДО фикса PRG (этап 21.1) — косметика тестовых данных, можно
  вычистить при желании.

## Известные блокеры

Нет. Оба блокера цикла 3 сняты 11.07.2026 ответами заказчика (раунд 4): реф-суммы по
категориям (сумма/процент) — этап 40; ступени цены по местам убираются совсем, остаются две
цены вход/продление — этап 43. Уточнения по этапам 42/46/47 тоже получены. Детали —
`docs/вопросы_заказчику.md` (раунд 4 + ответы).

## История закрытий

- **2026-07-31 — Этап 47: Консультации — продажа пакетов — tag `stage-47-done`** — прод `v0.3.45` (`8dee7bf`; feat `8dee7bf` + docs `112eaa5`). Вторая «услуга по количеству» на общем движке этапа 46, но **впервые с промокодами и бонусами** (для йоги решением раунда 4 они не нужны). **Модель**: пакеты 1/4/8/12 — строки `service_products` category='consult' (**миграция `0031_consult_packages`**, сид p1/p4/p8/p12 с плейсхолдер-ценами 3000/11000/20000/28000 ₽); покупается один пакет (quantity=1, число консультаций несёт название) — экрана выбора количества нет, из списка сразу в сводку. **Движок `services/service_sales.py` расширен**: `_promo_percent` (percent-промокоды, как у билетов — `fixed_price` только для подписки); `compute_service_pricing` получил `promo_id`/`use_bonus` — стекинг «максимум» (подписчик/промокод/новичок НЕ суммируются, `ev.best_ticket_price`) + бонусы добивают цену ≤50% (`ev.bonus_cap`); `start_service_payment` — те же параметры, привязка промокода к платежу только если он выиграл, `spend_bonuses` под advisory-локом (при неудаче `bonus_failed`); `sync_service_payment` возвращает бонусы при отмене; новый `cancel_service_payment` (возврат бонусов при ручной отмене, сверка с ЮKassa перед отменой). **Совместимость с йогой**: старые ключи dict сохранены, йога зовёт расчёт без промо/бонусов → всё по нулям, регресса нет (тест йоги зелёный). **Бот `handlers/consult.py`**: раздел = описание (редактируемый экран `consult`, реестр `services.screens`) + пакеты → сводка (скидка участника/промокод/бонусы, ввод промокода через `ConsultPromoStates`, тоггл бонусов) → оплата → «Оплачено» + кнопка «Написать менеджеру» (контакт `app_settings.manager_url`, дефолт @zhannazlotnikova). Билет/доступ в группу/счётчик остатка НЕ выдаются. Заглушка `nav_coming_soon` (последний скрытый раздел) убрана из `navigation.py`. **Рефералка (этап 40)**: категория `consult` — мгновенная (бонус пригласившему сразу). **Уведомление админам** `admin_alerts.service_sold` (текст `admin_service_sale` — «× 1» скрыт для пакетов). **Статистика**: направление «Консультации» (ключи уже были с этапа 46). **Веб-админка**: раздел «Консультации» (`webadmin/services.py` обобщён под йогу+консультации, `consult.html`, пункт nav между «Йога» и «Подписки») — цена пакета/скидка участника/вкл-выкл; FSM-подписи экранов консультаций в диагностике. Тест `tests/test_consult_sales.py` (стекинг промокод/подписка/новичок + бонусы до 50%) + весь набор зелёные. **Проверка dev**: миграция `0030→0031` накатилась, 4 пакета засеяны, бот/админка стартовали чисто, боевой прогон `compute_service_pricing` на dev-БД (11000 ₽, поля промо/бонусов на месте, итог=база без скидок), йога-тест не сломан; маршруты `/consult`, `/yoga` живы (рендер под сессией не проверен — dev-пароль админки разошёлся, шаблон — копия рабочего yoga.html). **Выкат** `dev→master` ff (`78dff05..8dee7bf`) → авто-деплой: прод `v0.3.45`, alembic head `0031`, 4 пакета consult на месте, бот `@Club_11_bot` в polling без ошибок, postgres/redis нетронуты. **Кнопку «Консультации» включает админ** (по умолчанию скрыта, этап 39); на приёмке задать реальные цены пактов. Заказчик подтвердил приёмку вживую («вроде ок всё, закрыть этап»). **Хвост (не блокер)**: реальной оплаты пакета на проде не проводили — путь «оплата → редирект к менеджеру → рефералка/бонусы/уведомление админам» проверен прогоном функций и тестами, но не живой продажей; подтвердить попутно на этапе 50. На ветке `dev`.

- **2026-07-30 — Этап 46: Йога — продажа занятий по количеству — tag `stage-46-done`** — прод `v0.3.44` (`78dff05`; feat `0908f7d` + docs `78dff05`). Первая «услуга по количеству» (не событие: нет даты, мест, счётчика остатка; билет/доступ в группу не выдаются). **Решение по объёму (раунд 4)**: для йоги — скидка подписчику + рефералка; промокоды/бонусы явно НЕ требуются (они в списке этапа 47). Выбор количества — инлайн-кнопки 1–8. **Миграция `0030_service_products`**: общая таблица `service_products(category, code, title, price, subscriber_discount_percent, is_active, sort_order, UNIQUE(category,code))` — **переиспользуется консультациями (этап 47)**; + колонки `payments.service_product_id`→`service_products` ON DELETE SET NULL и `payments.quantity`; сид двух форматов йоги (`individual`/`group`) с **плейсхолдер-ценами** 2000/1000 ₽ (админ правит в разделе «Йога»). **Движок `services/service_sales.py`**: `compute_service_pricing` (база = цена×кол-во; скидка подписчику-процент и скидка новичка НЕ суммируются — макс через `ev.best_ticket_price`), `start_service_payment` (общий конвейер ЮKassa, `kind='service'`, metadata product/quantity; билет/место НЕ трогаются), `sync_service_payment` (активация `repo.activate_service_payment` — только фиксирует оплату без выдачи билета; затем рефералка, уведомление админам, сообщение с контактом менеджера). Поллер `payments.poll_pending` диспетчеризует `kind='service'`. **Бот `handlers/yoga.py`** (`NAV_YOGA` больше не заглушка `nav_coming_soon` — там остались только Консультации): раздел = описание (редактируемый экран `yoga`, реестр `services.screens`, правится в «Экранах бота») + форматы → выбор количества (инлайн 1–8) → сводка (цена×кол-во со скидкой) → оплата → «Оплачено» + кнопка «Написать менеджеру» (контакт из настроек `app_settings.manager_url`, дефолт @zhannazlotnikova). **Рефералка (этап 40)**: категория `yoga` — мгновенная (бонус пригласившему сразу, `qualify_and_notify`). **Уведомление админам** `admin_alerts.service_sold` (этап 42, текст `admin_service_sale`). **Статистика**: `_DIRECTION_SQL` + LEFT JOIN `service_products` → направление по `category`; `stats` получил ключи `yoga`/`consult`/`service` (пустые направления скрыты, как «прочее» билетов). **Веб-админка**: раздел «Йога» (`webadmin/services.py` + `yoga.html`, пункт nav) — цена/скидка/вкл-выкл форматов; контакт менеджера — в «Настройках» (`/settings/manager`, нормализация как у поддержки, дефолт при пустом); FSM-подписи экранов йоги в диагностике. Тесты `tests/test_service_sales.py` (цена×кол-во, максимум скидок, мгновенная рефералка) + существующие зелёные. **Проверка**: миграция накатилась в dev и проде (head `0030`), продукты и колонки на месте; боевой прогон функций на dev-БД (2000×3=6000, новый SQL-джойн статистики не ломает `subscription/banya/retreat`, тексты строятся); бот/админка стартовали чисто. **Выкат** `dev→master` ff (`78dff05`) → авто-деплой: прод `v0.3.44`, alembic head `0030`, `service_products` (2 продукта), колонки `payments` на месте, бот `@Club_11_bot` в polling без ошибок, postgres/redis нетронуты. **Кнопку «Йога» включает админ** (Экраны бота → Приветствие; по умолчанию скрыта, этап 39). Заказчик подтвердил приёмку вживую («ок по этапу, закрыть»). **Вопрос заказчика на приёмке (не блокер)**: в разделе «Йога» нет списка/счётчика покупателей услуги — отдельного реестра продаж услуг (аналог «Билетов») в цикле 3 не заложено; продажи видны в уведомлениях админам и в доходе по направлению «Йога» на дашборде, поимённый учёт ведёт менеджер в личке (решение раунда 4). Возможная будущая доработка — отдельный раздел «Продажи услуг» (список/счётчики), в объём этапов 46/47 не входит. На ветке `dev`.

- **2026-07-30 — Этап 45: Динамические цены ранним покупателям — tag `stage-45-done`** — прод `v0.3.43` (`1156f6d`; feat `951b46d` + docs `1156f6d`). «Чем раньше куплен билет — тем дешевле»: у мероприятия задаются пороги «за N дней до события → цена». **Решение по объёму**: пороги привязаны к **типу билета** (для бани с одним типом это ровно пример заказчика — за 7 дн 1000 / за 3 дн 1500 / в день 2000; многотиповые ретриты не смешивают цены). **Миграция `0029_event_price_tiers`**: таблица `event_price_tiers(event_id→events CASCADE, ticket_type, days_before, price)` + `UNIQUE(event_id, ticket_type, days_before)` + индекс по `event_id`. **Цена момента** — чистые функции `services.events`: `days_until_event` (целых суток вниз) + `price_for_day` (среди порогов типа с `days_before >= дней до события` берётся тариф с наименьшим `days_before` — ближайший к дате; подходящего нет → базовая цена типа из `event_ticket_prices`). Так цена растёт при приближении даты, а до первого порога / у события без порогов действует базовая цена (нет регресса). **Единый расчёт `tickets.event_prices_now(event)`** заменил прямое `repo.get_event_prices` во ВСЕХ точках покупки (карточка `show_event`, выбор билета, ввод промокода, тоггл бонусов, создание платежа) → показ и оплата всегда совпадают; стекинг цикла 2 (макс. скидка, затем бонусы ≤50%) считается уже от цены момента без изменений; цена **фиксируется при создании платежа** (расчёт на текущее время → смена порогов не трогает выставленный счёт). `repo.get_event_price_tiers` отдаёт пороги. **Веб-админка**: в форме мероприятия блок «Динамические цены ранним покупателям» — сетка «за N дней» × цена по каждому типу (`_parse_tiers` + валидация: дубль порога, «дни без цены», отрицательные; `events_repo.get_price_tiers`/`set_price_tiers` — перезапись; JS «+ Добавить порог»). **Проверка**: миграция накатилась в dev (head `0029`); pure-тест `tests/test_event_price_tiers` (days_until_event + price_for_day по примеру заказчика, монотонность, fallback) + существующие тесты событий/билетов зелёные; живой прогон `event_prices_now` на dev-БД (10/5/2/0 дней → база/1000/1500/2000, событие без порогов → база) + смоук парсинга формы (валид + дубль + «дни без цены»). **Выкат** `dev→master` ff (`1156f6d`) → авто-деплой: прод `v0.3.43`, alembic head `0029`, таблица `event_price_tiers` на месте, бот `@Club_11_bot` в polling без ошибок, postgres/redis нетронуты. Заказчик подтвердил приёмку вживую («все ок»). На ветке `dev`.

- **2026-07-30 — Этап 44: Приоритет фикс-промо-цены на продлении подписки — tag `stage-44-done`** — прод `v0.3.42` (`dff6ab0`). **Объём согласован по смете п.8–9 (уточнён 2026-07-30)**: только подписка + приоритет промокода; мероприятия не трогаем (скидка билета — процент цикла 2, без регресса). «Абсолютные суммы» подписки (вход = полная цена, продление = цена подписчика) уже даёт этап 43 — критерии 1–2 закрыты им. **Суть правки**: этап 43 переключил ВСЕ продления на общую цену продления (`renewal_rate`) и этим случайно обнулил фиксацию цены промокодом — подписчик, купивший по спец-цене `fixes_price`-промокода, на продлении стал бы платить обычную цену. Этап 44 возвращает приоритет промокода. **Миграция `0028_subscription_price_lock`**: колонка `subscriptions.price_locked` (BOOL, дефолт false, NOT NULL) + бэкофилл `true` для подписок, оформленных платежом с `fixes_price`-промокодом (JOIN payments→promo_codes). **`repo.activate_payment`**: при активации ставит `price_locked` по `promo.fixes_price` (процентный промокод даёт `source='promo'`, но цену НЕ фиксирует — поэтому `source` для различия недостаточно, нужен явный флаг). **`tariffs.renewal_monthly_for(pool, sub)`** — единый расчёт ставки продления: залочено → своя `fixed_price` (по промокоду, скидка продления не суммируется); иначе → текущая `renewal_rate`; страховка → `sub.fixed_price`, если цена продления не задана. Используют `payments.start_renewal`, `handlers.nav_renew` (экран «Продлить») и `reminders` (напоминания показывают ту же цену). `get_subscriptions_for_reminders` отдаёт `price_locked`. Новый `tests/test_renewal_price_lock` (обычная → цена продления; залоченная → своя цена даже если дешевле; нет цены продления → ставка подписки) + весь набор зелёные. **Проверка**: миграция накатилась в dev (head `0028`); живой прогон в контейнере — обычная подписка (fixed 9999) продлевается по цене продления (10), залоченная (fixed 333) → по 333, запрос напоминаний с новой колонкой не падает. **Выкат** `dev→master` ff (`dff6ab0`) → авто-деплой: прод `v0.3.42`, alembic head `0028`, колонка `price_locked` на месте, бэкофилл `0` из `4` подписок (фикс-промокодом на проде исторически не покупали), postgres/redis нетронуты. **Не прогонялось живой оплатой на проде** (0 залоченных подписок): реальный путь «покупка по фикс-промокоду → продление по промо-цене» — проверить при первой реальной фикс-промо-продаже (попутно на этапе 50). Заказчик дал команду закрыть после выката. На ветке `dev`.

- **2026-07-30 — Этап 43: Тариф подписки вход/продление (без ступеней по местам) — tag `stage-43-done`** — прод `v0.3.41` (`0098aaf`; feat `3efc393` + фикс ввода цены `0098aaf`). Авто-ступени цены по числу занятых мест (цикл 1, этапы 2/5/23) **убраны совсем** — упрощение, регулировка первых участников теперь промокодами. **Миграция `0027_subscription_two_prices`**: два ключа в `bot_settings` — `subscription_entry_price` (вход, новичок/после перерыва) и `subscription_renewal_price` (продление, непрерывный подписчик); обе засеяны текущей входной ставкой (`monthly_price` активной ступени с наим. sort_order) через COALESCE — цена для людей **не изменилась** (на проде обе = 4444), существующие подписки не подорожали задним числом. `price_tiers`/`tier_prices` **не удалены** (историч. FK платежей/подписок), но движком цены больше не читаются. **`services/tariffs`**: `get_current_tier` → синтетическая «ступень» с ценой входа (без счёта мест, `tier_id`=NULL — уже поддержанный кейс «промо/кастом»); `entry_rate`/`renewal_rate`; `period_price`/`prices_for` = ставка×месяцы (матрица цен-за-период ушла). **`app_settings`**: `subscription_prices`/`set_subscription_price` (+ `_to_price`). **Продление** (`payments.start_renewal` + `handlers.nav_renew`) и **напоминания** (`reminders`) берут текущую цену продления, а не зафиксированную когда-то ставку (страховка на `sub.fixed_price`, если цена продления не задана) — так активные подписки следуют новой цене, не ломаясь. **Перерыв** → нет активной подписки → маршрут сам ведёт на цену входа (переиспользует сброс цикла 1). **Веб-админка «Тарифы»**: таблица ступеней/матрица цен заменены двумя полями (вход/продление); длительности как были; правка инвалидирует Redis-кеш. Копирайт `pay_success`/`renew_choose` очищен от обещания «зафиксированной цены». Новый `tests/test_subscription_prices` (парсинг цен, синтетическая ступень, ставка×месяцы) + весь набор (13) зелёные. **Проверка**: миграция и движок прогнаны на локальной и боевой БД (в контейнере: цена входа→get_current_tier, цена продления→renewal_rate, ставка×3); веб-админка проверена реальным HTTP (рендер двух полей, сохранение, валидация). **Хвост-фикс на приёмке**: цена с пробелом-разделителем тысяч («4 444») отбивалась «числа больше нуля» — `_parse_price` не убирал пробел; починено (`0098aaf`, v0.3.41): удаляются любые пробельные символы + запятая как десятичный разделитель. Выкат `dev→master` ff (сначала `3efc393`/v0.3.40, затем фикс `0098aaf`/v0.3.41) → авто-деплой: миграция `0026→0027`, обе цены засеяны 4444, postgres/redis нетронуты. Заказчик подтвердил приёмку вживую. На ветке `dev`.

- **2026-07-23 — Этап 42: Уведомления админам о продажах — tag `stage-42-done`** — прод `v0.3.39` (`2d21194`), **миграций нет**. Админы видят продажи в реальном времени, не заходя в веб-админку. Новый `services/admin_alerts.py` — общая точка рассылки: `all_admin_ids` (админы из `.env` + добавленные в админке этапа 27) + `notify_admins` (каждая отправка под своим try — заблокировавший бота админ не рвёт рассылку остальным); дубль этих хелперов из `handlers/my_tickets` убран, заявка на возврат идёт через него же. **Три уведомления**: (1) продан билет — в `tickets.sync_ticket_payment` внутри `if created` (ровно раз на билет): мероприятие, покупатель (кликабельная `tg://user?id=` — работает и без username), тип, цена + сводка по этому событию; (2) оплачена/продлена подписка — в `payments.sync_payment` (заголовки «НОВАЯ ПОДПИСКА»/«ПРОДЛЕНИЕ ПОДПИСКИ» по `kind`); (3) выход за неуплату — в `subscriptions.run_expiry_check` после пометки 'expired' (сводка уже без выбывшего). Все три вызова под `suppress` — сбой уведомления не влияет на выдачу билета/активацию подписки. **Сводки — по одному SQL, без N+1**: `repo.event_sales_summary` (продано билетов, занято мест с учётом парных = 2, сумма продаж, вместимость = `seats_male+seats_female` при гендер-балансе, иначе `seats_total`, NULL = без лимита → «из N» не пишется) — по конкретному мероприятию (решение раунда 4); `repo.club_members_summary` (DISTINCT активные подписчики + сумма их последних успешных НЕ-билетных оплат, выданный вручную/бесплатный = 0, старые платежи того же человека и билеты не в счёт). Тексты `admin_ticket_sale`/`admin_subscription_sale`/`admin_subscription_expired` в `texts.py`. **Проверка**: сводки по событиям прогнаны боевыми функциями в контейнере на боевых данных — совпали с прямым SQL («баня1»: 2 билета из 2 мест 2222 ₽; клуб: 2 подписчика 2833 ₽); логика клуба доп. проверена на синтетике в откаченной транзакции (бесплатный + оплативший → 2 человека, 3000 ₽ по последнему платежу, билет 9999 и старый платёж 2000 не считаются). Новый `tests/test_admin_alerts` (формат всех трёх сообщений, парные места, безлимит, без username); все 12 тестов зелёные. Выкат `dev→master` ff (`2d21194`) → авто-деплой: прод `v0.3.39`, «миграции в актуальном состоянии», bot/admin пересозданы, postgres/redis нетронуты, админка 200, логи чистые. **Хвост (не блокер)**: аннулирование подписки из веб-админки идёт тем же путём (`end_date=now()` → джоб), поэтому админам приходит «ВЫХОД ЗА НЕУПЛАТУ», хотя это ручное отключение — заказчик принял как есть, развести формулировки можно отдельной правкой. Заказчик подтвердил приёмку вживую («да ок»). На ветке `dev`.

- **2026-07-23 — Этап 41: Раздел «Мои бонусы» + кнопки к автосообщениям — tag `stage-41-done`** — прод `v0.3.38` (`a2a247a`), **миграций нет**. **Раздел «Мои бонусы»** (`kb.NAV_BONUSES`, экран `handlers/referral.nav_bonuses`, текст `texts.my_bonuses`): три числа одним запросом `repo.bonus_overview` — «доступно сейчас» = баланс `bonus_ledger` (SUM(delta), с учётом списаний), «скоро начислится» = SUM(`referrals.bonus_amount`) по связям в статусе `qualified` (бонус за друга ждёт даты события; `void`/`accrued` не в счёт), «всего начислено за всё время» = сумма положительных операций (реф-бонусы + ручные начисления + возвраты списанного) — тратами не уменьшается. Строка «скоро начислится» и пояснение про дату мероприятия показываются только при ненулевом ожидании; при полном нуле — подсказка пригласить друга. **Два входа** (критерий «без перегруза верхнего уровня»): кнопка `bonuses` подсажена ВТОРОЙ в ряд `referral` (новый ряд не добавлен — верхний уровень остался в 6 рядов, схема заказчика из `прав.txt` не поехала) + кнопка «Мои бонусы» на экране реф-ссылки (`referral_link_kb`); подпись/видимость правятся из веб-админки как у всех кнопок реестра (`_TOP_UNION` обновлён). **Кнопки к автосообщениям**: `event_notifications._broadcast` научился принимать `markup` → напоминание за 1 день о событии уходит с `kb.event_reminder_kb` («Мои билеты» · «Главное меню»); сообщения о бонусах — с `kb.bonus_accrued_kb` («Мои бонусы» · «Главное меню») и в отложенном начислении (`referral_jobs`, после даты события), и в уведомлении о покупке друга (`referral_notify`, в т.ч. мгновенном за подписку). Тесты: новый `tests/test_bonuses` (17 проверок — три числа, пустое состояние, состав всех четырёх клавиатур), `test_menu` дополнен рядом рефералки и контролем «6 рядов»; все 12 тестов зелёные. SQL сводки прогнан на локальной и боевой БД (`@Polarasing`: доступно/всего 1000 ₽). Выкат `dev→master` ff (`a2a247a`) → авто-деплой: прод `v0.3.38`, «миграции в актуальном состоянии», bot/admin пересозданы, postgres/redis нетронуты, админка 200, логи чистые. **Попутно закрылся хвост этапа 40**: в боевой БД реф-связь #2 (`@Polarasing → @hochapuri_hot`, категория `subscription`) стоит в статусе `accrued` с 1000 ₽ на балансе — мгновенное начисление за подписку на приёмке этапа 40 всё-таки отработало вживую, просто тогда это не проверили. **Не прогонялось отдельным сценарием**: переезд бонуса из «скоро начислится» в «доступно» после даты события (механика та же, что у джоба этапа 40; отдельной оплаты билета с последующим сдвигом даты на приёмке не делали) — попутно подтвердить на этапе 50. Заказчик подтвердил приёмку вживую. На ветке `dev`.

- **2026-07-23 — Этап 40: Рефералка на все покупки + уведомление пригласившему — tag `stage-40-done`** — реф-программа перестала быть «только про баню», прод `v0.3.37` (`c1dc58d`; feat `35bf3c5` + фикс сводки `c1dc58d`). **Миграция `0026_referral_categories`**: таблица `referral_rules` (category PK · discount_kind/value · bonus_kind/value; kind = `fixed`|`percent`, CHECK-констрейнты) — сид всех 5 категорий (banya/retreat/subscription/yoga/consult) из глобальных сумм этапа 17 через COALESCE → 500/1000, поэтому поведение бань не изменилось; `referrals` + `category` + `accrue_after` (+индекс) с бэкфиллом старых связей в `banya`/`starts_at`. **`services/referral_rules.py`** (новый, чистая логика): категории/типы, `discount_for` (режется ценой покупки), `bonus_for` (фиксированный бонус НЕ режется — решение заказчика), `is_instant`, `discount_phrase`, округление ВНИЗ до рубля. **`services/referral_notify.py`** (новый): единая «квалификация + уведомление» для всех категорий; покупки без даты (подписка/йога/консультации) начисляют бонус ПРЯМО ТАМ (идемпотентный `accrue_referral_bonus`), билеты ждут джоба. **Билеты**: скидка новичка теперь и на ретриты (категория по виду события), величина из правил категории. **Подписка**: реф-скидка новичка в `start_payment` (разовая, зафиксированную ставку не трогает, пол 1 ₽ — ЮKassa не берёт 0) + показ на экране сводки (`tariff_summary`, фикс `c1dc58d` — до него скидка всплывала только на экране оплаты и выглядела как «её нет»); в `start_promo_payment` — «не суммируются, берём выгоднее» (если выигрывает рефералка, промокод НЕ расходуется); квалификация в `sync_payment`, кроме продления. **Джоб** `referral_jobs` отбирает по `accrue_after` (анти-возврат по билету — только для событий). **Веб-админка**: форма сумм заменена таблицей правил по 5 категориям (величина + тип «Сумма ₽»/«% от покупки»), валидация целых/≥0/процент ≤100. Тексты: «Скидка действует 24 часа», уведомление пригласившему (сразу/после даты), реф-экран расширен с бани на все покупки. Осиротевшие `app_settings.referral_sums`/`set_referral_sum` удалены. Новый smoke-тест `tests/test_referral_rules` (33 проверки), все 11 тестов зелёные. Бэкапы прода: `backup_pre_stage40_2026-07-23_1113.sql` (до миграции) и `backup_pre_wipe_8058822980_2026-07-23_1127.sql` (перед чисткой тестового аккаунта). Выкат `dev→master` ff → авто-деплой: миграция `0025→0026` на проде, 5 категорий засеяны 500/1000, postgres/redis нетронуты, админка 200, логи чистые. **Приёмка**: вживую подтверждены скидка на ретрит (997 → 497 ₽) и на подписку (2222 → 1722 ₽, платёж #24), надпись про 24 часа, правила по категориям в админке. **Не прогонялось живой оплатой** (платёж не доводили до конца): уведомление пригласившему после покупки и мгновенное начисление за подписку — код и данные проверены прогоном боевых функций на боевых данных, но реальной оплаты не было; проверить попутно на этапе 41/50. **Йога/Консультации** покрыты только инфраструктурно — продажи появятся на этапах 46/47. Диагностика по ходу приёмки: «скидки нет» у заказчика объяснялось тестированием с аккаунта пригласившего (`@Polarasing` 2042819654) вместо новичка (`@hochapuri_hot` 8058822980). На ветке `dev`.

- **2026-07-16 — Этап 39: Новое главное меню — tag `stage-39-done`** — меню по схеме заказчика (`прав.txt`), прод `v0.3.35` (`42191e0`). **Миграций нет** (структура меню — код + реестр `services.menu`, таблица `menu_buttons` уже есть). **Верхняя кнопка «Клуб»** контекстна по подписке: гость — «Вступить в Клуб 11:11» (`join`, `NAV_JOIN`), подписчик — «Клуб 11:11 — Новая реальность» (`mysub`, `NAV_MYSUB`); новые дефолтные подписи в `BUTTON_DEFS`. **«Мероприятия» разделены** на «Бани» (`NAV_BANYA`) и «Ретриты» (`NAV_RETREAT`) — раздельные входы: `handlers/events._render_events_list(kind)` фильтрует `ev.visible_events` по `kind`, входы `show_banyas`/`show_retreats`; общий `NAV_EVENTS` оставлен для совместимости со старыми кнопками. **«Йога»/«Консультации»** (`NAV_YOGA`/`NAV_CONSULT`) добавлены **скрытыми по умолчанию** (`menu.DEFAULT_HIDDEN` — resolve_config отдаёт `is_visible=False` при отсутствии override → пустой ряд пропускается; заглушка-попап `navigation.nav_coming_soon` на случай раннего показа админом; реальные разделы — этапы 46/47). **«Пригласить друга»** вынесена из подменю «О клубе» на главную (ряд `referral`), из `ABOUTMENU_LAYOUT` убрана. **«Назад»/«К мероприятиям»** из карточки события возвращают в свой раздел по виду (`kb.events_nav_for_kind`; `event_tickets_kb`/`event_sold_out_kb`/`ticket_address_kb` получили `back_cb`). Подписи/видимость **всех** кнопок правятся из веб-админки (редактор «Кнопки меню» итерирует `BUTTON_DEFS` динамически; `_TOP_UNION` обновлён для страницы экрана `start`). Попутно: подсказка экрана «О клубе» очищена от «Пригласить друга». **Решение по «О клубе»**: схема `прав.txt` — 5 рядов без «О клубе», но инфо-экраны «Что внутри»/«Правила» терять нельзя → «О клубе» оставлен 6-м рядом снизу (заказчик подтвердил «все ок»). Тесты `test_menu` (переписан), `test_events`, `test_events_filter` — зелёные; компиляция + импорт ОК. Bump 0.3.34→0.3.35. Выкат `dev→master` ff (`42191e0`) → авто-деплой: бот/админка пересозданы на `v0.3.35`, postgres/redis нетронуты (данные целы), бот `@Club_11_bot` в polling без ошибок, админка `/login` 200. Заказчик подтвердил приёмку вживую («все ок»). На ветке `dev`.

- **2026-07-16 — Этап 38: Гарантийные баги + правки навигации — tag `stage-38-done`** — первый этап цикла 3 (гарантийный, бесплатно). Миграций нет. **(1) Показ событий** (`services.events.visible_events`): убрано скрытие распроданных — раздел «опустевал» после исчерпания мест; теперь ретриты показываются все будущие активные, баня = ближайшая по времени + отмеченные «показывать заранее», распроданность на видимость НЕ влияет. Причина исходного бага (ретрит/вторая баня пропадали): попадали в `sold_out_ids` (нет цен билетов или распроданы) и скрывались. `handlers.events.show_events` вычисляет `sold_out_ids` только для пометки; `keyboards.events_list_kb(groups, sold_out_ids)` вешает суффикс «· мест нет», событие остаётся кликабельным → карточка «Мест нет» + «Запрос в Поддержку» (было). **(2) Раздел `menu` убран** как дубль приветствия: удалены экран `menu` из `SCREEN_DEFS`, `texts.MAIN_MENU`, `menu.main_menu_kb`, layout `main` из `LAYOUTS`; `nav_menu` и `/menu` (`cmd_menu`) показывают стартовый экран (`welcome_kb`); все кнопки «В главное меню»/«Назад» переведены с `NAV_MENU` на `NAV_START` (в `keyboards`/`payment`/`promo`/`services.payments`/`services.menu`); `NAV_MENU` оставлен рабочим алиасом → стартовый экран (старые сообщения в истории чатов). **(3) «Мои билеты»**: `ticket_detail_kb` — кнопка «Запросить возврат» заменена на «Поддержка» (`NAV_SUPPORT`); тексты `ticket_detail`/`my_tickets_list` обновлены (возврат — через поддержку); обработчики `MYT_REFUND*` оставлены (недостижимы, безвредны). **(4) «Назад»**: ревизия клавиатур тарифов/оплаты/событий/билетов — тупиков нет. Bump 0.3.33→0.3.34. Тесты `test_events` (обновлён под новую сигнатуру `visible_events` без `sold_out_ids`), `test_menu`, `test_events_filter` — зелёные; `py_compile` + импорт всех затронутых модулей ОК. Выкат `dev→master` ff (`fc550e8`) → авто-деплой: прод `v0.3.34`, миграции в актуальном состоянии, `bot`/`admin` пересозданы, `postgres`/`redis` нетронуты (данные целы), админка `/login` 200, логи чистые. Попутно **починено расхождение STATUS**: прод стоял на `dd44cef`/`v0.3.33` с начала июля (STATUS ошибочно писал `v0.3.32`). Заказчик подтвердил приёмку вживую на проде (обошёл 5 пунктов). На ветке `dev`.

- **2026-06-27 — Этап 37: Картинка к экранам бота + безопасный показ edit/send — tag `stage-37-done`** — к любому редактируемому инфо-экрану (этап 22) можно прикрепить картинку из веб-админки; переходы между экранами не ломаются. **Миграция 0025_screen_photo**: `screen_texts.photo_url TEXT NULL` (NULL = без фото). **repo**: `get_screen_overrides` отдаёт `{key:{body,photo_url}}`; `upsert_screen_photo` — отдельно от текста (правки текста и фото независимы). **services.screens**: `resolve()` → `{text,photo_url}` + центральный `render()` — единое безопасное правило показа для всех точек (`navigation._show`, `/start`, `/menu`): без фото и текущее текстовое → `edit_text` (без регресса); без фото, текущее фото → delete+send; с фото, текущее фото → `edit_media` (на ошибку → delete+send_photo); с фото, текущее текст → delete+send_photo; новое сообщение → `answer_photo`/`answer`; всё под suppress. `CAPTION_LIMIT=1024`. **Веб-админка** (`webadmin/screens.py` + `screen_form.html`): загрузка/снятие картинки (S3, prefix=screens, как этап 26), превью + явная «Текущая картинка», миниатюра и бейдж «+ фото» в списке, серверная валидация текста ≤1024 при фото + живой счётчик, рендер ошибки; PRG. **Фикс по фидбэку (живой тест)**: кнопки с экрана-фото («Моя подписка»/«Мероприятия»/«Мои билеты»/тарифы/промокод) не работали — их хендлеры вне navigation делали `edit_text` напрямую (по фото падает). Локальный `_edit` в payment/tariffs/events/my_tickets/promo сделан фото-безопасным (фото → delete+send). Самопроверка: smoke `tests/test_screen_view.py` (4 ветки edit + новое сообщение + откат edit_media) зелёный; компиляция/импорт/Jinja-парс ОК. Bump 0.3.31→0.3.32. Бэкап боевой БД до миграции `backup_pre_stage37_2026-06-27_2202.sql` (65K). Выкат `dev→master` (`3b32dc6`) → авто-деплой: прод на `3b32dc6`, alembic `0025`, бот `v0.3.32`, колонка `photo_url` на месте, 4 контейнера running, админка 200, ошибок в логах нет. Заказчик подтвердил приёмку вживую («норм»). На ветке `dev`.

- **2026-06-27 — Этап 36: Веб-админка «Мероприятия» — поиск + фильтры + пагинация архива — tag `stage-36-done`** — раздел дополнен инструментами под рост числа мероприятий (раньше грузил ВСЕ события разом, без поиска/фильтров). **Поиск по названию** (ILIKE, частичный) над обоими списками. **Фильтры**: направление (баня/ретрит/все) + статус (в боте/скрыто/отменено/все), авто-применение. **Пагинация «Прошедших»** по 30 + счётчики в заголовках; фильтрация ушла в SQL — архив не грузится целиком. «Предстоящие» — полным списком сверху (их мало), тоже под фильтр. `events_repo`: общий `_events_filters` + `list_upcoming_events`/`list_past_events`(limit/offset)/`count_past_events`. `events.py`: чтение q/kind/status/page, расчёт страниц; убран Python-сплит upcoming/past (теперь `now()` в SQL). Шаблон: фильтр-бар (поиск+направление+статус+найти/сбросить), пагинация архива. Самопроверка: smoke `tests/test_events_filter.py` (14 проверок построителя) зелёный; headless Playwright на проде (вьюпорт 390px, 1 баня «тест»): фильтр-бар везде, поиск по названию/направлению/статусу даёт верный показ/скрытие, переполнение 0px. Пагинацию вживую не прогнать (в проде 1 событие, 0 прошедших) — покрыта тем же limit/offset-паттерном, что «Билеты» (этап 32), и юнит-тестом. Миграций нет. Bump 0.3.30→0.3.31. Выкат `dev→master` (`821dcfc`), прод `v0.3.31`, админка 200. Заказчик подтвердил приёмку («закрыть»). На ветке `dev`.

- **2026-06-27 — Этап 35: Адаптив всей веб-админки под мобильный — tag `stage-35-done`** — вся админка доведена под телефон (этап 34 сделал мобильным только чек-ин). **Мобильное меню** (`base.html`): чекбокс-хак без JS — гамбургер в шапке + off-canvas сайдбар с затемнением-оверлеем (`#nav-toggle:checked ~ .sidebar/.nav-overlay`), переход по ссылке сбрасывает состояние сам. **CSS `@media ≤860px`**: off-canvas сайдбар (`position:fixed`, `translateX`), компактные отступы topbar/content/panel, некарточные таблицы — горизонтальный локальный скролл (`.table-wrap{overflow-x:auto}`), `segmented`/`chart-head` в перенос, `form-grid`/`screen-grid` в 1 колонку. **Фикс хвоста этапа 34**: card-transform таблиц сделан opt-in (`.table.cards-sm` + `.table-wrap-cards` только у билетов) — прочие таблицы перестали рендериться «карточками» с пустыми подписями. **Фикс переполнений по фидбэку** (v0.3.30): формы в карточках (`.card .inline-form`) на мобилке — контролы в столбик во всю ширину, `min-width:0!important` перебивает зашитые инлайн `min-width:340/220/200px` (формы «Участник» в Подписках и «Поддержка/Админы» в Настройках вылезали за карточку); `.period-custom` (два date-поля) переносится; `.content{overflow-x:hidden}` — подстраховка от сдвига страницы. **Самопроверка**: headless Playwright на вьюпорте 390px по 6 разделам (дашборд/подписки/настройки/билеты/тарифы/мероприятия) — `scrollWidth == 390` везде, горизонтального скролла страницы нет; формы реально рефлоутся (нет в списке вылезающих), широкие таблицы скроллятся локально внутри `.table-wrap`. CSS-скобки сбалансированы, шаблоны парсятся. Bump 0.3.28→0.3.30. Выкат `dev→master` (`fbee758`), прод `v0.3.30`, миграций нет, админка 200. Заказчик подтвердил с телефона («да ок вроде»). На ветке `dev`.

- **2026-06-27 — Этап 34: Чек-ин на точке — онлайн-отметка «Пришёл» + сводка + адаптив под мобилку — tag `stage-34-done`** — на входе админ (в т.ч. с телефона) отмечает пришедших, видит «пришло X из N», повтор по одному билету не задваивается. **Миграция 0024_ticket_checkin**: `tickets.attended_at` + `attended_by` (логин админа, аудит «кто»; nullable, обратимая). **repo**: `mark_ticket_attended` (идемпотентно — UPDATE только `paid AND attended_at IS NULL`, повтор → 'already' = защита от двойного прохода), `unmark_ticket_attended` (снять ошибочную отметку), `checkin_stats` (билеты + ЛЮДИ оплачено/пришло; парный = 2 через `_PEOPLE_EXPR`). Статус-фильтры `attended`/`not_attended` в `_sold_tickets_filters`. **Админка «Билеты»**: кнопка «Отметить пришёл» (зелёная) ↔ «Отменить приход», бейдж «пришёл ✓» с временем/админом, подсветка строки, сводка «Пришло билетов X из N · людей Y из M», фильтр «Пришёл/Не пришёл», маркер «2 чел.» у парных. Всё через PRG. **Мобильный режим** (`@media ≤720px`): строки таблицы → карточки (`data-label`), кнопки крупные во всю ширину под палец, фильтры тоже — без горизонтального скролла. **Фикс по фидбэку заказчика (v0.3.28)**: посещённый билет считается использованным — `list_active_tickets` получил `AND attended_at IS NULL` (билет уходит из «Мои билеты» у юзера), `my_tickets._owned_ticket` блокирует открытие карточки/возврат по посещённому билету (попап «билет использован») — раньше пробитый билет висел в боте и допускал запрос возврата. Самопроверка: smoke `test_tickets_filter.py` (+ статус-фильтры чек-ина) зелёный; миграция — единственный head; на проде живой SQL-тест на реальном билете (счётчики, идемпотентность mark, скрытие посещённого из «Мои билеты») с ROLLBACK — без следов. Бэкап до миграции `backup_pre_stage34_2026-06-27_0811.sql`. Bump 0.3.26→0.3.28. Выкат `dev→master` (`10b9e57`, миграция 0023→0024 применилась на старте), прод `v0.3.28`, pg/redis нетронуты, админка 200. Заказчик подтвердил приёмку («все хорошо»). На ветке `dev`.

- **2026-06-27 — Этап 33: Номер билета (бот + поиск в админке) + фикс вёрстки даты — tag `stage-33-done`** — у билета появился видимый номер (`tickets.id`), по нему ищется билет в админке; фильтр даты в «Билетах» больше не разъезжается. **Бот «Мои билеты»**: номер в списке (кнопка `№123 · Событие · дата`) и в карточке (`Номер билета: №123` в шапке) — покупатель называет его менеджеру при вопросе/возврате (`keyboards.my_tickets_kb`, `texts.ticket_detail(ticket_id=…)`, проброс из `handlers/my_tickets.py`). **Админка «Билеты» — поиск по номеру**: выделен общий `repo._ticket_search_clause` (используют и список, и CSV-экспорт — сквозная нумерация $-параметров): `#123`/`# 123` → точный `t.id` без ILIKE; просто число → частичный tg_id ИЛИ точный `t.id`; текст (`#abc`) → прежний ILIKE по имени/@username/tg_id. В таблицу добавлена колонка «№», плейсхолдер поиска → «Имя, @username, ID или #номер». **Фикс вёрстки**: «Куплен с:»/«по:» вынесены в блок `.date-range` (две строки друг под другом, поля выровнены, не растягиваются правилом `.filter-bar .input{min-width:320px}`). Самопроверка: smoke `tests/test_tickets_filter.py` +12 проверок номера (36 зелёных); Jinja-парс `tickets.html` ОК; рендер текста/кнопки билета ОК; `py_compile` ОК. Bump 0.3.25→0.3.26. Выкат `dev→master` (`3d2f201`) → автодеплой: прод на `3d2f201`, бот `@Club_11_bot` v0.3.26 (миграций нет, pg/redis нетронуты), админка 200. Заказчик подтвердил приёмку («ок»). На ветке `dev`.

- **2026-06-27 — Этап 32: Веб-админка «Билеты» — поиск/фильтры/пагинация/CSV-список на вход — tag `stage-32-done`** — раздел «Билеты» (этап 21) дополнен инструментами поиска под объём в тысячи продаж (по фидбэку заказчика «как найти человека в куче»). **Поиск по покупателю** — одно поле: имя / `@username` / Telegram ID (частичное, регистронезависимо; ILIKE по `u.username`/`u.first_name` + `CAST(t.tg_id AS TEXT) LIKE`). **Фильтры**: статус (Все/Оплачен/Возврат запрошен/Возвращён) + **диапазон даты покупки** (`created_at::date` с/по — «ввёл id + дату → клиент»). **Пагинация** по 50 + счётчик «Найдено: N». **CSV-список на вход** (`/tickets/export.csv`) — печатный check-in лист, только оплаченные, сортировка по имени, UTF-8 с BOM (кириллица в Excel): № · Событие · Дата события · Имя · @username · **Telegram ID** · Тип билета · Гостей (парный = 2) · пустая «Отметка». Серверная фильтрация — общий `repo._sold_tickets_filters` (сквозная нумерация $-параметров) + `list_sold_tickets`(limit/offset)/`count_sold_tickets`/`list_checkin_tickets`(JOIN events). CSV уважает текущие фильтры (событие/поиск/дата). Самопроверка curl на dev-данных (14 билетов): поиск по имени/username/id даёт верные числа (Gats 4, Polarasing 8, по id 4); статусы 7/3/4; даты 6/10/4; id+дата → 1 (точечно); CSV — BOM, новые колонки, парный=2 гостя, paid-only. Smoke-тест `tests/test_tickets_filter.py` (22 проверки построителя фильтров, включая дату и сквозную нумерацию) — зелёный. Миграций нет. Bump 0.3.23→0.3.25 (с этапом 31). Деплой `dev→master` (`7b97f5a`) → авто-деплой, прод `v0.3.25`. Заказчик подтвердил вживую на проде («да ок»). На ветке `dev`.

- **2026-06-27 — Этап 31: UX меню — контекстное мини-меню — tag `stage-31-done`** — устранён перегруз приветствия/главного меню (было до 11 кнопок одного веса; фидбэк заказчика «разбегаются глаза»). Меню стало **контекстным по статусу подписки** (`repo.get_active_subscription` в `start.py`/`navigation.py` → флаг в `menu.welcome_kb`/`main_menu_kb`): гость — «Вступить в клуб» главным (первый ряд во всю ширину = иерархия позицией, без эмодзи по правилу проекта) + Мероприятия/Мои билеты + О клубе/Поддержка; подписчик — **один раздел «Моя подписка»** (продление живёт ВНУТРИ его экрана, на верхний уровень не дублируется — уточнение по фидбэку: «Продлить и Моя подписка — один раздел»), без «Вступить». Инфо-кнопки + рефералка убраны в **подменю «О клубе»** (новый `NAV_ABOUTMENU` + `aboutmenu_kb`: Что внутри · Правила · Пригласить друга · Назад; экран `aboutmenu` редактируем из админки). **Промокод убран с верхнего уровня** — ввод остался в флоу вступления (`tariffs.py`) и продления (`payment.py`), доступ не потерян (проверено). Верхний уровень — ≤5 кнопок (было 11). Подписи/видимость остаются правимыми из веб-админки (`LAYOUTS`-union для редактора); `BUTTON_DEFS` без `renew` (он не самостоятельная кнопка меню). Smoke-тест `tests/test_menu.py` (18 проверок раскладок гость/подписчик/подменю) — зелёный. Миграций нет. Заказчик прошёл два круга фидбэка (контекстность + убрать дубль «Продлить») и подтвердил на проде («да ок»). Bump 0.3.21→0.3.23. На ветке `dev`.

- **2026-06-26 — Этап 30: Сквозное тестирование и выкат цикла 2 в прод — tag `stage-30-done`** — весь цикл 2 (билеты «Фокус.Энергия», рефералка, веб-админка) выкачен на прод `163.5.153.150` и принят заказчиком вживую. DNS-блокер снят: A-запись `admin → 163.5.153.150` опубликована в nic.ru (заказчик в панели РУЦЕНТР, проверено через DoH — резолв за ~2 мин). Выкат: бэкап боевой БД (`backup_pre_stage30_2026-06-26_1306.sql`, 37K) → fast-forward `dev→master` (`b820fdc`, v0.3.22) → GitHub Actions авто-деплой (`git reset --hard` + `docker compose up -d --build`). Применены **миграции 0011→0023** (`alembic_version=0023_broadcasts`); pg/redis НЕ перезапускались — данные подписчиков цикла 1 целы; бот пересобран до v0.3.22 (polling, без ошибок, доп-админы из БД). Поднят контейнер `admin` (`127.0.0.1:8011`, новый сервис в прод-`docker-compose.yml`), первый админ `admin` засеян из `.env` (`ADMIN_WEB_*`, дописаны в боевой `.env` и `.secrets/server.env`). **Nginx + Let's Encrypt**: системный Nginx (`deploy/nginx/admin.agat-key.ru.conf` → proxy `127.0.0.1:8011`), `certbot --nginx` выпустил сертификат (до 2026-09-24, авто-renew). Внешняя проверка: `https://admin.agat-key.ru/login`→200 (SSL ok), http→https 301, вход верным паролем→303, неверным→401. Обновлён `install.sh` (сбор/запись `ADMIN_WEB_*` + подсказка по Nginx), runbook — `docs/выкат_веб-админки.md`. Заказчик прошёл UAT в браузере и боте, подтвердил: «вроде работает всё». **Хвосты (не блокеры, в проде остаются)**: нет PRG в «Подписках» (F5 может повторно продлить); косметические дубли в тестовой переписке. На момент закрытия событий в проде 0 (заказчик заводит реальные через админку). Прод-коммит `b820fdc`, разработка продолжается в `dev`.

- **2026-06-26 — Этап 11: Каркас веб-админки + авторизация + https — tag `stage-11-done`** — закрыт последний (https) критерий вместе с этапом 30. Локальная часть (FastAPI+Jinja2-сервис `src/webadmin/`, миграция `0011_admin_users`, вход/выход/смена пароля, макет по референсу) была проверена заказчиком ранее (4 из 5 критериев). Финальный критерий — `https://admin.agat-key.ru` с валидным сертификатом — выполнен при выкате цикла 2: A-запись DNS опубликована, системный Nginx + Let's Encrypt-сертификат выпущен (до 2026-09-24, авто-renew), заказчик открыл админку в браузере и подтвердил вживую. Тег ставился в самом конце цикла 2 (а не в момент локальной готовности), т.к. https-выпуск был привязан к прод-инфраструктуре этапа 30.

- **2026-06-26 — Этап 29: Дашборд — столбчатые графики динамики (регистрации + активность по датам) — tag `stage-29-done`** — в переключатель графиков на дашборде (этап 28) добавлены два bar-графика динамики, уважающие выбранный период и гранулярность (день ≤92 дн, иначе месяц — переиспользованы `axis`/`gran`/`chart_labels` графика дохода): **«Регистрации в боте по датам»** (`users.created_at`, count по корзинам) и **«Активность в боте по датам»** (`user_events`, уникальные пользователи за корзину). `stats_repo`: `registrations_buckets`, `activity_buckets` (DISTINCT tg_id за корзину), `active_users_total` (DISTINCT за период — **gran-независимый** «всего» для активности: сумма корзин-distinct зависит от дневной/месячной разбивки, поэтому итог считаем отдельно; для регистраций «всего» = сумма, count аддитивен). `stats.py`: `build_bar_svg` (серверный inline-SVG, оси/шкала как у графика дохода) + `_bar`; donut получил `kind='donut'`, bar — `kind='bar'`. `dashboard.html`: `.chart-pane` ветвится по `kind` (donut → svg+легенда, bar → подпись периода + svg); CSS `.chart-bar-note`. Самопроверка curl+psql: дашборд 200, 4 пункта в списке (2 donut + 2 bar), столбики рендерятся; «всего» сходится с прямым SQL на **двух** периодах (год — gran month, и 30 дней — gran day): регистрации 3, уникальная активность 4. Smoke-тест `tests/test_stats.py` дополнен 11 проверками bar (валидный SVG без nan, число столбиков, нулевой/пустой ряд, `_bar`) — зелёный. Bump 0.3.20→0.3.21. Верифицировал заказчик вживую («ок»). На ветке `dev`.

- **2026-06-26 — Этап 28: Дашборд — графики-распределения (бублики) + переключатель графиков — tag `stage-28-done`** — на дашборде веб-админки новый блок «Графики» с выпадающим списком выбора (1 экран, мгновенное переключение через крошечный inline-JS show/hide — без charting-библиотек). Первыми — два круговых (donut) графика, серверный inline-SVG (как график дохода этапа 20; сегменты через `stroke-dasharray`/`stroke-dashoffset` + `rotate(-90)`, сумма в центре, HTML-легенда с числами и %): **«Распределение подписчиков клуба»** (с активной подпиской / просроченные-бывшие / без подписки — категория на пользователя, суммы дают всех `users`) и **«Билеты: с подпиской на клуб vs гости»** (среди уникальных покупателей оплаченных билетов — у скольких есть активная подписка). `stats_repo`: `subscriber_distribution` (CTE `bool_or` активной подписки по `tg_id` + LEFT JOIN `users`) и `ticket_buyer_distribution` (DISTINCT покупатели `paid`-билетов × EXISTS активной подписки). `stats.py`: `build_donut_svg` + `_donut` (легенда/проценты). `dashboard.html`: панель с `<select>` + панами `.chart-pane` (первая видна, прочие `.is-hidden`) + JS-переключатель; CSS для donut. **Donut'ы — текущий срез** (период не применяется; период будет у столбчатых в этапе 29). «С подпиской» в donut билетов = активная подписка **сейчас** (не на момент покупки) — согласовано как простой наглядный срез. Самопроверка curl+psql: дашборд 200, обе панели + 5 сегментов-дуг (3+2) рендерятся; цифры сходятся с прямым SQL (подписчики 1/1/1 из 3 users → 33/33/33%; покупатели 1/2 → 33/67%). Smoke-тест `tests/test_stats.py` дополнен 10 проверками donut (валидный SVG без nan, число сегментов, пустой случай, проценты) — зелёный. Bump 0.3.19→0.3.20. Верифицировал заказчик вживую («ок»). На ветке `dev`.

- **2026-06-26 — Этап 27: Веб-админка — настройки (напоминания + поддержка + админы + диагностика) — tag `stage-27-done`** — перенос оставшихся разделов `/admin` в веб (последний из этапов 23–27 «перенос `/admin` в веб»). Новый раздел «Настройки» (`webadmin/settings.py` + `settings.html`, пункт nav после «Экраны бота») на одной странице: **напоминания** (единица + пороги early/soon/last в `bot_settings`, бот применяет на лету), **ссылка поддержки** (`@username`/URL с той же нормализацией, что в боте; пусто → кнопка «Перейти» у пользователя скрыта), **администраторы доступа** (основные из `.env` read-only + добавленные add/remove по tg_id), **FSM-диагностика** «кто на каком экране» (read-only, `repo.get_fsm_stuck`, обновление = перезагрузка). Источник истины — `services.app_settings`/`bot_settings` (тот же, что у бота). Напоминания и ссылку бот читает live, без рестарта. **Доп-админы — ключевая тонкость**: бот держит их в in-memory кеше (синхронная проверка прав), а веб — отдельный процесс → правки веба бот без reload не увидит. Решение: `add/remove` сделаны **DB-авторитетными** (read-modify-write по БД — иначе веб с пустым in-memory set затёр бы чужие id; проверено: добавление второго и удаление первого сохраняют остальных), добавлен `get_extra_admins` для показа; в `main.py` — периодический джоб `reload_extra_admins` (частота поллера ~1 мин) → бот подхватывает веб-правки без рестарта. **ИЗМЕНЕНИЕ ПЛАНА**: критерий «отключить вход в `/admin`» **отменён заказчиком** — `/admin` оставлен рабочим для быстрого доступа из бота (логика разделов и колбэки не удалялись; кратковременная заглушка на входе откачена коммитом `bb8df7a`). Функционал теперь **дублируется** в боте и вебе; оба пишут в один `bot_settings`, доп-админы согласованы (DB-авторитетно + reload). Самопроверка curl+psql: `/settings` 200 (4 секции + nav); POST напоминаний/поддержки/админов → 303, значения в БД верны (ссылка нормализована в `https://t.me/…`); валидация (нечисловой порог, неизвестная единица, битая ссылка, дубль/нечисловой админ) → 400; read-modify-write админов не затирает чужие id; тестовые данные `bot_settings` откачены. Bump 0.3.18→0.3.19. Верифицировал заказчик вживую (попросил вернуть `/admin`, веб не трогать). На ветке `dev`.

- **2026-06-26 — Этап 26: Веб-админка — рассылка (текст/фото/альбомы через бота) — tag `stage-26-done`** — перенос рассылки из `/admin` в веб по схеме **очередь + бот-джоб** (веб-процесс не держит бота, как мост этапа 21.1; четвёртый из этапов 23–27). **Миграция 0023_broadcasts** — таблица-очередь: `audience` (all/active/former/never), `body` (HTML), `photos` (JSON-массив `{url}` публичных ссылок S3), `status` (pending→sending→done), счётчики total/sent/blocked/failed, created_by, тайминги + частичный индекс `ix_broadcasts_pending`. **`repo`**: `create_broadcast`, `claim_pending_broadcast` (атомарный claim `FOR UPDATE SKIP LOCKED`), `set_broadcast_total`, `finish_broadcast`, `list_broadcasts` (фото хранятся как JSON-строка — кодек не настроен, `json.dumps/loads` в слое). **`services/broadcasts.py`** — бот-джоб `run_broadcast_queue`: в цикле забирает pending, шлёт по аудитории (`repo.get_audience_ids`) текст/одно фото/альбом **по S3-URL** (file_id у веб-загрузки нет, в отличие от бот-рассылки этапа 8), троттлинг (0.1с альбом / 0.05с текст), помечает заблокировавших `set_user_blocked` на `TelegramForbiddenError`, прочие ошибки → failed, итог `status='done'`+счётчики; ошибка одной задачи не валит джоб. Зарегистрирован в `main.py` с частотой поллера платежей (`payment_poll_interval_min`, ~1 мин — быстрый отклик). **Веб** `webadmin/broadcasts.py` + `broadcasts.html` (пункт nav «Рассылка» между «Промокоды» и «Экраны бота»): форма (аудитория + текст + до 10 фото/альбом, `multipart`), фото заливаются в **S3** (`services.storage`, как этап 8) → задача в очередь pending; список последних 50 рассылок со статусом и счётчиками (отпр./заблок./ошибок). Валидация: пустые текст+фото / неверная аудитория / >10 фото / фото без S3 → 400. PRG-redirect. Самопроверка (изолированно, без спама тест-аккаунтам: временно `is_blocked=true` 3 реальным + синтетический получатель, затем восстановлено): веб-рендер/навигация/валидация (400, строк нет); фото залилось в S3, URL в `photos` jsonb; бот-джоб — claim→total→отправка текста и **фото через `send_photo` с S3-URL** синтетику→недоставка (chat not found)→failed=1, пустой сегмент→total=0, все `done`, счётчики верны; тестовые данные удалены, `is_blocked` восстановлены. Фикс вёрстки по фидбэку заказчика: класс `.input` (height:42px) ломал textarea (видно 2 строки) и центрирование кнопки file-input → добавлен класс `.textarea` (height:auto+resize, как этап 22) и CSS `input[type=file].input{height:auto;padding}`. Артефакт: в psql текст тестовых рассылок — mojibake (Bash-curl на Windows искажает кириллицу в `-F`, как этап 23; из браузера UTF-8 корректен). Bump 0.3.17→0.3.18. Верифицировал заказчик вживую. На ветке `dev`.

- **2026-06-26 — Этап 25: Веб-админка — участники (поиск/карточка/история + добавление + аннулирование) — tag `stage-25-done`** — расширение раздела «Подписки» работой с участником (перенос из `/admin`; третий из этапов 23–27). В `webadmin/subscriptions.py` добавлены роуты: поиск по tg_id (`GET /subscriptions/find` → 303 на карточку), карточка (`GET /subscriptions/member/{tg_id}` + шаблон `member.html`), ручное добавление (`POST /subscriptions/member/add`), аннулирование (`POST /subscriptions/member/{tg_id}/annul`). **Карточка**: профиль (username/имя/email/блокировка), статус подписки (активна/нет + цена/период/даты/осталось/источник), доступ в клуб, последняя подписка, **история действий** (`repo.get_user_events`, последние 20). **Ручная выдача** (tg_id+срок+единица) — `repo.upsert_user` + `tariffs.get_current_tier` (текущая ставка движка) + `repo.add_subscription(source='manual', status='active')`; бот пускает в группу по join-request. **Аннулирование** — через механизм этапа 19 (`repo.disable_subscription_via_expiry`: `end_date=now()`, статус остаётся active → фон-проверка окончаний бота помечает expired и кикает на ближайшем проходе; веб-процесс не может кикнуть напрямую). Логика/хранение переиспользованы из `repo`/`services.tariffs` — тот же источник истины, что у бота. Все POST через PRG-redirect (F5 не дублирует — в отличие от латентного хвоста старого списка подписок). На странице `/subscriptions` — блок «Участник» (поиск + ручная выдача). **Фикс вёрстки по фидбэку заказчика**: формы блока были `card filter-bar`, глобальное правило `.filter-bar .input{min-width:320px}` растягивало два поля (ID + select единицы) → во flex-wrap кнопка «Выдать подписку» уезжала на новую строку влево; переписано на `card` + две строки `inline-form` (как форма продления), кнопка идёт сразу за полями. Самопроверка curl+psql: логин→формы на /subscriptions; поиск существующего (id 2042819654)→303 на карточку (профиль/подписка/история рендерятся); ручная выдача id 999000111 (2 мес)→подписка #19 manual/active по текущей ставке; аннулирование→end_date=now(); edge-cases (повторное аннулирование без активной, value=0, неверная единица, нечисловой ID)→400; тестовые данные откачены. Bump 0.3.15→0.3.16 (фикс вёрстки). Верифицировал заказчик вживую («ок всё»). На ветке `dev`.

- **2026-06-26 — Этап 24: Веб-админка — промокоды подписки (CRUD) — tag `stage-24-done`** — перенос раздела `/admin` «Промокоды» в веб (второй из этапов 23–27 «перенос `/admin` в веб»). Раздел «Промокоды» (`webadmin/promos.py` + `promos.html`, пункт nav между «Тарифы» и «Реферальная программа»): список всех кодов (условие/использовано N/лимит/срок/фикс-разово/статус), создание **одной формой** (код, тип `percent`/`fixed_price`, значение, лимит активаций, срок в днях, фиксация цены — в отличие от пошагового FSM в боте), вкл/выкл, удаление с подтверждением. Логика/хранение переиспользованы из `repo`/`services.promo` — тот же источник истины, что у бота; серверная валидация повторяет правила бота (процент 1–99, цена >0, лимит/срок — целые ≥0, дубль кода, нормализация кода в верхний регистр). **Без Redis-кеша** (бот валидирует промокод напрямую из БД при каждом применении) → правки из веба действуют сразу, инвалидация не нужна. Все POST через PRG-redirect (F5 не дублирует). Самопроверка curl+psql: логин→/promos 200; создание percent (лимит 5/срок 30дн/фикс) и fixed_price (555, без лимита/срока) — данные в БД корректны; валидация (дубль/процент 150/пустой код/цена 0/нечисловой лимит) отсекает до вставки (мусора 0); toggle (active=false) и delete работают; тестовые данные откачены; smoke-тест `test_promo.py` зелёный. Bump 0.3.14. Верифицировал заказчик вживую («проверил — ок»). На ветке `dev`.

- **2026-06-25 — Этап 23: Веб-админка — тарифы (ступени + цены за периоды + длительности) — tag `stage-23-done`** — перенос разделов `/admin` «Ценовые ступени» и «Длительности» в веб (первый из этапов 23–27 «перенос `/admin` в веб»). Раздел «Тарифы» (`webadmin/tariffs.py` + `tariffs.html`/`tier_form.html`, пункт nav): **ступени** — форма правки ставки ₽/мес, лимита мест-брекета (пусто/0 → безлимит), названия, активности + **матрица цен за периоды** (своя цена или авто = ставка×значение; пусто → авто-сброс `delete_tier_price`); **длительности** — добавить (значение+единица), вкл/выкл, удалить, **порядок ↑/↓**. Логика/хранение переиспользованы из `repo`/`services.tariffs` (ступени не создаются/не удаляются — фиксированный набор брекетов, как в `/admin`). **Веб-админка подключена к Redis** (`src.cache` в `app.py` lifecycle) — при любой правке тарифов инвалидируется кеш (`tariffs.invalidate`), бот применяет без рестарта (TTL кеша 300с, инвалидация даёт мгновенный эффект). Все POST через PRG-redirect. По фидбэку заказчика на верификации добавлено **управление порядком длительностей** (шёл нелогичный 1,12,18,3,6: `upsert_duration` ставил `sort_order=значение` и конфликтовал с сидовым 10/20/30): `repo.move_duration` (нормализующий swap sort_order, устойчив к дублям) + кнопки ↑/↓; новые длительности теперь встают в конец (`max+10`); текущие данные нормализованы (1,3,6,12,18). Самопроверка curl+psql+резолв ботом: правка ставки 900/лимита 5 + цена за 3 мес 2500 → бот отдаёт 2500 и авто-5400 за 6 мес; add/toggle/delete/move длительности работают, кеш инвалидируется (EXISTS=0); тестовые данные откачены. **Побочно**: на верификации заказчик заметил кракозябры в имени ступени #1 — оказалось mojibake от моей curl-правки (Bash на Windows исказил кириллицу в аргументе); восстановлено программно из mojibake, правка из браузера пишет UTF-8 корректно (подтверждено: страница отдаёт «Первые места», как и в этапе 12). Bump 0.3.13. Верифицировал заказчик вживую. На ветке `dev`.

- **2026-06-25 — Этап 22: Веб-админка — редактор экранов бота (тексты + кнопки) — tag `stage-22-done`** — владелец правит тексты инфо-экранов бота из веб-админки без правки кода (расширение этапа 19). Паттерн как `menu_buttons`: реестр редактируемых экранов `services/screens.py` (ключ → дефолтный текст из `texts.py`, источник истины) + миграция `0022_screen_texts` (переопределения текста, `body=NULL` → дефолт) + бот сливает реестр с БД при каждом рендере → правки **без рестарта**. Бот рендерит экраны через `screens.text()`: `handlers/navigation.py` (start/menu/about/rules/support, `support`/`support_no_link` по наличию ссылки) и `start.py` (`/start`, `/menu`); из `navigation.py` убран неиспользуемый импорт `texts`. **Редактируемые экраны** (объём согласован с заказчиком — инфо-экраны): Приветствие, Главное меню, Что внутри клуба, Правила, Поддержка + заглушка «ссылка не задана». Динамические экраны (цены/даты/списки) НЕ трогаем. **Веб-раздел «Экраны бота»** (`webadmin/screens.py` + `screens_list.html`/`screen_form.html`, пункт nav заменил «Кнопки меню»): список экранов с превью → страница экрана (textarea + живой предпросмотр HTML, PRG-redirect — F5 не дублирует). Для меню-экранов (Приветствие/Главное меню) на той же странице — редактор подписей/видимости их кнопок (объединение с этапом 19, `menu.buttons_for_layout`/`layout_keys`; подпись кнопки общая по ключу везде). Старый раздел `/menu-buttons` убран из навигации, роутер оставлен для прямых ссылок. Самопроверка curl+psql: логин→/screens 200, формы about (без кнопок)/start (с кнопками) 200, POST правит текст (пусто/дефолт→NULL) и кнопку, PRG 303, бот резолвит кастомный текст и подпись; тестовые данные откачены, БД чистая. Bump 0.3.11. Верифицировал заказчик вживую («вроде работает»). На ветке `dev`.

- **2026-06-25 — Этап 21.1: Возвраты — связь с покупателем + уведомление о возврате — tag `stage-21.1-done`** — допил раздела «Билеты» по фидбэку на демо этапа 21. **(A) Двусторонний мост менеджер↔покупатель через бота.** Прямая tg-ссылка по numeric id для юзеров без @username не работает (огранич. Telegram) → где есть username, рабочая `t.me/<username>`; для ВСЕХ — «Написать через бота» в карточке: очередь `manager_messages` (миграция 0020) + бот-джоб `_send_manager_messages` доставляет от имени бота (покупатель уже в диалоге), статус доставлено/не доставлено. **Ответ покупателя приходит В ВЕБ-АДМИНКУ** (а не в Telegram-аккаунт — по требованию заказчика): `handlers/manager_bridge.py` (регистрируется последним, `StateFilter(None)`, только при открытой переписке `has_manager_conversation` 14 дн) логирует входящее `direction='in'`; в списке «Билеты» — пометка «новый ответ N» (миграция 0021 `is_read`), открытие переписки помечает прочитанным. Панель переписки (in/out) в карточке. **(B) Уведомление о возврате** (миграция 0019 `refund_notified_at`/`refund_notify_failed`): отметка возврата → бот-джоб `_send_refund_notifications` шлёт «возврат произведён», пометка «уведомлён»/«не доставлено». **Фикс PRG**: отправка сообщения/возврат переведены на Post/Redirect/Get — обновление страницы больше не дублирует сообщение покупателю (был баг: F5 пересылал POST). В заявке на возврат (этап 18) добавлено указание подтвердить возврат в веб-админке (иначе билет активен, место занято) + связь через «Билеты». Верифицировано заказчиком вживую: написал покупателю **без username** (id 7060768358) из админки → доставлено, тот ответил → ответ пришёл в админку; «в остальном всё работает». Хвосты (не блокеры): нет PRG в «Подписках»; дубли в тестовой переписке до фикса. Заказчик: «закрыть этап, новый не начинать». На ветке `dev`. Версия 0.3.10.

- **2026-06-25 — Этап 21: Веб-админка — билеты + отметка фактического возврата — tag `stage-21-done`** — раздел «Билеты» (`webadmin/tickets.py` + `tickets.html`, пункт nav): список проданных билетов (`repo.list_sold_tickets`, статусы 'paid'/'refunded') с фильтром по событию (выпадающий список, авто-сабмит), покупатель (имя·@username·id)/тип/цена/дата покупки/статус; запрошенные на возврат подняты наверх и помечены красным пиллом «возврат запрошен». Кнопка «Отметить возврат» (с подтверждением) → `repo.mark_ticket_refunded`: атомарно статус 'paid'→'refunded' + аудит (`refunded_at`, `refunded_by`=логин админа из сессии; миграция 0018). Перевод в 'refunded' **освобождает место** (занятость считается по 'paid' — ничего доп. не нужно) и **убирает билет из «Моих билетов»** (бот `list_active_tickets` фильтрует 'paid'). **Анти-возврат рефералки** (этап 17) триггерится: в той же транзакции, если у приглашённого реф-связь на это событие ещё лишь 'qualified' (бонус не начислен) — переводим в 'void' (`void_referral`-логика инлайном), джоб бонус не начислит; на 'accrued' (уже выплачено) — no-op, ручной кейс. Идемпотентно (повтор → «уже возвращён»). Деньги возвращает менеджер вручную переводом — админка фиксирует факт. Самопроверка curl+psql: список/фильтр/пометки рендерятся; возврат билета #5 → refunded+аудит 'admin', место 2→1, повтор=ошибка; void проверен на синтетической qualified-связи (откатил). Заказчик подтвердил вживую («21 да ок»). На демо заказчик поднял ОТКРЫТЫЙ ВОПРОС: в разделе нет связи с покупателем (tg-ссылка) и уведомления о произведённом возврате + алерта менеджеру при недоставке (юзер не начинал диалог/закрыл ЛС) — в плане допила нет, решается отдельно. На ветке `dev`. Версия 0.3.7.

- **2026-06-25 — Этап 20: Админка — статистика (дашборд) + уведомления — tag `stage-20-done`** — две части. **(A) Дашборд** (`webadmin/stats.py` + `stats_repo.py`, новый `/` главной): доход за период крупным KPI со **сравнением с предыдущим периодом** той же длины (зелёный рост / красный спад, %), общий и **по направлениям** (подписка / баня / ретрит — суммы из `payments.status='succeeded'`, момент дохода `updated_at`; направление: kind in new/renewal → подписка, kind='ticket' → `events.kind`). Переключение периода: 30 дней (дефолт) · Год · Всё время · произвольный диапазон (невалидный → откат к 30 дням). **График дохода — серверный inline-SVG** (синяя линия+заливка текущий период, зелёный пунктир предыдущий; ровная шкала `_nice_max` 1/2/5×10^k, оси без артефактов), гранулярность авто: дни ≤92 дн, иначе месяцы; пустые корзины заполняются нулями (`_axis`). Доп. KPI: активные подписчики, платежей/средний чек, эффективность рефералки (состоялось/приглашений + выплачено бонусов из `bonus_ledger`). Nav: «Дашборд» стал «Дашборд·Статистика», заглушка «Статистика скоро» убрана. **(B) Уведомления**: миграция 0017 (`events.canceled_at` + таблица `event_notifications` PK event_id+kind — идемпотентность как `subscription_reminders`). Бот-джоб `services/event_notifications.py` (частота = проверка окончаний): (1) **напоминание за 1 день** всем купившим билеты по событиям в окне `event_reminder_offset_hours` (дефолт 24), claim 'day_before'; (2) **отмена события** — кнопка «Отменить» в `/events` (веб ставит `canceled_at`+is_active=false, отдельный процесс), бот-джоб на ближайшем проходе рассылает «СОБЫТИЕ ОТМЕНЕНО» купившим (claim 'canceled') и помечает их билеты на ручной возврат (`refund_requested`, как этап 18 → обработка менеджером в этапе 21). Smoke-тест `tests/test_stats.py` (resolve_period, _nice_max, delta, fmt_money, _axis, валидность SVG без NaN). Самопроверка curl+psql: KPI 3 396 ₽ сходится с БД, все периоды 200, custom/откат работают; живой прогон бот-джоба — отмена события #8 разослана 2 реальным юзерам + билеты помечены, напоминание за сутки заявлено по событию в окне. Заказчик проверил вживую дашборд и отмену события («1 и 2 работает»). Фактическая отметка возврата менеджером — Этап 21. На ветке `dev`. Версия 0.3.6.

- **2026-06-25 — Этап 19: Админка — подписки + кнопки меню — tag `stage-19-done`** — две части в веб-админке. **Подписки** (`/subscriptions`, `webadmin/subscriptions.py` + `subscriptions.html`): список активных подписок со сроками (`repo.list_active_subscriptions`), ручное продление на N единиц (month/day/hour/minute — `repo.set_subscription_end_date`, `end_date += add_period`, зафиксированная ставка сохраняется) и отключение. Веб-процесс отдельный от бота, прямой кик невозможен → отключение ставит активной подписке `end_date = now()` (`repo.disable_subscription_via_expiry`); фоновая проверка окончаний бота на ближайшем проходе помечает 'expired', кикает из группы и уведомляет (как штатное окончание), `get_active_subscription` сразу возвращает None. **Кнопки меню** (`/menu-buttons`, `webadmin/menu_buttons.py`): переименование и показ/скрытие пунктов меню бота без правки кода. Меню стало DB-управляемым: реестр кнопок/раскладки/дефолтов — новый `services/menu.py` (источник истины), переопределения — таблица `menu_buttons` (миграция 0016: key, label-override, is_visible). Бот при рендере приветствия и главного меню сливает реестр с БД (`menu.welcome_kb`/`main_menu_kb`) — применяется на лету; скрытая кнопка выпадает из ряда, опустевший ряд пропускается. Старые `kb.welcome_kb`/`main_menu_kb` удалены (единственный источник — `services.menu`). Проверено локально curl+psql: продление сдвигает `end_date`; сохранение в `/menu-buttons` переименовывает кнопку и убирает скрытую (рендер ботом подтверждён живым прогоном). Заказчик проверил вживую («Подписки — работает всё», «Кнопки меню — да работает»). На демо заказчик добавил пожелание править и **тексты экранов** → заведён новый **Этап 22** (без реализации; схема в `docs/навигация_экранов.md`), прежнее тестирование сдвинуто на Этап 23. На ветке `dev`. Версия 0.3.4.

- **2026-06-25 — Этап 18: Мои билеты + запрос на возврат — tag `stage-18-done`** — раздел «Мои билеты» в боте (`handlers/my_tickets.py`): кнопка в приветствии и главном меню. Список активных билетов — `repo.list_active_tickets`: `status='paid'` + `events.starts_at > now()` (прошедшие скрыты, сортировка по дате). Карточка билета — событие/направление/тип/дата/цена + **правила и адрес сразу на одном экране** (по фидбэку заказчика убрали отдельный шаг «согласие → адрес»: для уже оплаченного билета он лишний; шаг согласия остаётся только при первичной выдаче после оплаты). Запрос на возврат: «Запросить возврат» → подтверждение → заявка **всем админам** (`settings.admin_id_list` ∪ доп-админы из админки) с данными билета и **кликабельной ссылкой `tg://user?id=`** — менеджер открывает диалог даже без `@username` (его фидбэк: иначе с юзером без username не связаться). Поведение возврата (выбор заказчика — «пометка возврат запрошен»): миграция 0015 добавляет `tickets.refund_requested`/`refund_requested_at`; запрос ставит флаг через `repo.request_ticket_refund` (идемпотентно, UPDATE … WHERE refund_requested=false), но **статус остаётся `paid` → место НЕ освобождается** (без овербукинга и не пропадёт при отказе менеджера). Билет помечается «— возврат запрошен» (метка в списке, статус-строка в карточке, кнопка запроса исчезает, повторная заявка не шлётся и админов не дёргает). Кнопки «Перенос» нет (решение заказчика). **Фактическую отметку возврата менеджером** (статус → `refunded`, освобождение места, исчезновение из «Моих билетов») вынесли в новый **Этап 21** (веб-админка) — добавлен в ROADMAP, старое «тестирование» сдвинуто на Этап 22. Верифицировал заказчик вживую («ок всё»). На ветке `dev`. Версия 0.3.3.

- **2026-06-23 — Этап 17: Реферальная программа (бани) + бонусы — tag `stage-17-done`** — сквозная рефералка для бань. Миграция 0014: `referral_links` (код на юзера), `referrals` (кто кого привёл; pending→qualified→accrued|void; суммы фиксируются при квалификации), `bonus_ledger` (1 бонус=1 ₽, баланс=SUM(delta); идемпотентность через частичные уникальные индексы — 1 бонус на связь, 1 списание на платёж). Бот: `/ссылка`/`/link`/кнопка «Пригласить друга» → персональная deep-link, баланс, условия; `/start ref_<code>` привязывает **новичка** (кто ещё ничего не покупал — ни подписки, ни билета, ни успешного платежа; первая привязка побеждает). Единый расчёт цены билета `tickets.compute_ticket_pricing` (источник истины и для показа, и для оплаты): скидки (подписчик/промокод/**новичок** — фикс ₽ на первую баню, дефолт 500) НЕ суммируются, берётся максимальная (`ev.best_ticket_price`); бонусы добивают цену до −50% (`ev.bonus_cap`), оплата бонусами через тоггл на сводке. Бонус пригласившему (дефолт 1000 ₽) начисляется **после даты события** джобом `services.referral_jobs` (анти-возврат: билет приглашённого ещё `paid`), с уведомлением. Квалификация связи — при первой покупке бани новичком (`repo.qualify_referral`, в `sync_ticket_payment` после выдачи). Бонусы списываются под платёж с защитой от овердрафта (advisory-лок, `spend_bonuses`) и **возвращаются** при срыве/отмене платежа (`refund_bonuses`). По фидбэку заказчика на живом тесте добавлен фикс: кнопка «Отмена» на экране оплаты билета теперь реально отменяет незавершённый платёж и **сразу возвращает бонусы** (`cancel_ticket_payment` — сверяет статус в ЮKassa: если уже оплачено, выдаёт билет) — раньше зарезервированные бонусы «зависали» до таймаута ЮKassa. Веб-админка: раздел «Реферальная программа» (суммы скидки/бонуса, цепочки приглашений со статусами, ручное начисление/списание с защитой от овердрафта). Решения по `fixed_price`-промокодам (только подписка) и определению новичка — дефолты, согласованные с духом `docs/вопросы_заказчику.md`. Smoke-тесты: `test_events` (скидка новичка + `bonus_cap`), `test_referral` (хелперы кода/deep-link). Для живого теста заказчик сбросил 2 тестовых аккаунта до состояния новичка (их покупки — тестовые, удалены в dev-БД). Заказчик проверил вживую привязку/скидку/оплату и подтвердил фикс отмены («да ок»). На ветке `dev`. Версия 0.3.1.

- **2026-06-23 — Этап 16: Промокоды на билеты + стекинг (макс. скидка) — tag `stage-16-done`** — промокоды (этап 7) расширены на билеты. Ядро — чистый калькулятор `services.events.best_ticket_price`: скидка подписчика и промокод **НЕ суммируются**, берётся **максимальная** (меньшая итоговая цена); метка `applied` ('promo'/'subscriber'/'none'), на равенстве предпочитается скидка подписчика (активация промокода не тратится зря). Пример из ТЗ 1000 ₽: подписка −10% (900) vs промокод −5% (950) → 900 ₽. На билетах работают только `percent`-промокоды; `fixed_price` (₽/мес для подписки) отклоняется сообщением «действует только на подписку». Учёт активаций — единый лимит как у подписки (1 код = 1 активация на юзера, uniq `promo_id+tg_id`); активация расходуется только если промокод реально выиграл (`applied_promo_id` привязывается к платежу лишь при выигрыше). Бэкенд: `tickets.start_ticket_payment(promo_id=…)` ревалидирует percent-код в момент оплаты (источник истины) и считает лучшую цену; `repo.activate_ticket_payment` учитывает активацию (`_record_promo_redemption`) в той же транзакции выдачи билета. Бот-флоу: кнопка «Ввести промокод» на сводке билета → FSM-ввод (`TicketPromoStates`) → пересчёт; обработаны не найден/истёк/исчерпан/уже использован/только-для-подписки; если промокод применён, но скидка участника выгоднее — прозрачная пометка и сохраняется бóльшая скидка. Без миграций (`payments.promo_id` уже был). Решения по `fixed_price`-на-билете и единому лимиту активаций — дефолты (в `docs/вопросы_заказчику.md` прямо не описаны, согласованы с духом документа). Smoke-тест `tests/test_events.py` дополнен 8 проверками стекинга. Заказчик проверил вживую («да, работает 16 этап»). На ветке `dev`. Версия 0.2.8.

- **2026-06-23 — Этап 15: Скидка подписчикам 11:11 на билеты — tag `stage-15-done`** — при показе цены билета и при создании платежа бот проверяет активную подписку (`repo.get_active_subscription`) и применяет скидку события `subscriber_discount_percent` (механизм per-событие: у бань админ ставит общую, у каждого ретрита — свою). Не-подписчик платит полную цену. Скидка — чистая функция `services.events.apply_discount` (округление до копеек, ROUND_HALF_UP), едина для показа и для суммы платежа (`tickets.start_ticket_payment` берёт подписку как источник истины в момент оплаты). В меню билетов (карточка события) и на экране сводки для подписчика показывается разбивка со **зачёркнутой** старой ценой и выделенной новой («Мужской: <s>1 222 ₽</s> → <b>1 075,36 ₽</b>») — в кнопках Telegram зачёркивание невозможно, поэтому разбивка в тексте. Попутно меню билетов стало учитывать занятость мест (`seats_occupied`) при показе доступности типов. Промокоды на билеты и стекинг (макс. скидка) — Этап 16. Smoke-тест `tests/test_events.py` дополнен проверками `apply_discount` (0/12/50/100%, округление). Заказчик проверил вживую на своей активной подписке (Тест 1 −12%, Тест 2 −50%) — «да вроде все ок»; по его фидбэку добавлено зачёркивание старой цены. На ветке `dev`. Версия 0.2.7.

- **2026-06-23 — Этап 14: Бот — оплата билета + электронный билет + учёт мест — tag `stage-14-done`** — продажа билетов переиспользует платёжный конвейер подписок. Миграция 0013: таблица `tickets` (tg_id, event_id, ticket_type, price, status, payment_id, rules_agreed) + расширение `payments` полями `event_id`/`ticket_type`/`ticket_id` под `kind='ticket'` (`ticket_id` — защита от дубля при активации, аналог `subscription_id`). Флоу (`handlers/events.py`): выбор билета → сводка → «Перейти к оплате» → ЮKassa (`services/tickets.py` `start_ticket_payment`, чек 54-ФЗ) → «Проверить оплату»/поллер → выдача билета → экран правил с 2 кнопками «С правилами согласен (показать адрес)» / «Есть вопросы»; адрес показывается только владельцу билета после согласия (`set_ticket_rules_agreed`). Атомарный учёт мест (`repo.activate_ticket_payment`): под блокировкой строки платежа и строки события (`FOR UPDATE`) пересчёт занятости по проданным билетам (`services.events.seats_occupied`: пара = 2 места, М+Ж = 1М+1Ж) + проверка наличия; идемпотентность по `ticket_id`, мест в минус не уводит; нехватка между оплатой и подтверждением → платёж `refund_due` (ручной возврат) + уведомление. Поллер диспетчеризует pending по `kind` (билет → `tickets.sync_ticket_payment`, подписка → `sync_payment`). Сверх плана по фидбэку заказчика на живом тесте: (1) в админке — показ проданных/оставшихся мест по событию («продано/вместимость (ост. N)», заметная красная плашка «мест нет» — `events_repo.ticket_counts_by_event` + `.pill-danger`); (2) в боте полностью распроданные события скрываются из списка ДО выбора «ближайшей» бани (`event_available` + `sold_out_ids` в `visible_events`) — иначе распроданная баня занимала слот и прятала следующую доступную. ФИО покупателя не собираем (решение заказчика — различаем по Telegram id). Скидка подписчику и промокоды на билеты — этапы 15/16. Билет НЕ даёт доступ в группу. Smoke-тест `tests/test_events.py` (учёт занятости, скрытие распроданных, доступность по гендер-балансу). Заказчик прогнал 2 сценария оплаты вживую («работают») + подтвердил фиксы («да, ок вроде»). На ветке `dev`. Версия 0.2.5.

- **2026-06-23 — Этап 13: Бот «Мероприятия» + показ + выбор билета — tag `stage-13-done`** — раздел «Мероприятия» в самом боте поверх данных Этапа 12 (`events` + `event_ticket_prices`). Кнопка «Мероприятия» в приветствии и главном меню → список событий по бизнес-правилам показа (`services/events.py` `visible_events`): ретриты — все будущие активные; баня — только ближайшая + те, где стоит «показывать заранее»; прошедшие/неактивные скрыты; после даты ближайшей бани следующая становится ближайшей автоматически. Список сгруппирован по типу (заголовки «Энерго Баня»/«Ретрит», подпись кнопки начинается с типа — по фидбэку заказчика, иначе события шли «в куче»). Карточка события → меню типов билетов (М/Ж/парные М+Ж/Ж+Ж/М+М) с ценами из админки (только типы с заданной ценой, порядок `TICKET_TYPES`). Доступность мест по типу с учётом гендер-баланса (общий пул `seats_total` vs раздельно `seats_male/female`): распроданный тип — попап «Мест нет»; всё событие распродано — экран «Мест нет» + кнопка «Запрос в Поддержку» (ссылка из админки). Выбор билета → сводка с ценой (оплата — Этап 14). Новые файлы `services/events.py` (чистая логика) и `handlers/events.py` (роутер); `repo.list_events/get_event/get_event_prices` (бот не импортирует webadmin). Учёт занятых мест по проданным билетам — Этап 14 (таблицы `tickets` ещё нет): хелперы `seat_availability`/`has_seats` уже принимают занятость, пока считают занято=0. Smoke-тест `tests/test_events.py` (21 проверка: правила показа, группировка, доступность мест по гендер-балансу). Заказчик подтвердил вживую («да ок вроде» + «продолжить») показ списка и правила; цены меню/«мест нет»/автопереключение — частью вживую, частью покрыты тестом. На ветке `dev`. Версия 0.2.2.

- **2026-06-22 — Этап 12: Админка «Мероприятия» (CRUD) — tag `stage-12-done`** — миграция 0012 (`events`: тип баня/ретрит, дата-время, места общие `seats_total` или раздельно `seats_male/female`, флаги `gender_balance`/`show_in_advance`, `subscriber_discount_percent`, `address`, `rules_text`, `is_active`; `event_ticket_prices`: цена по 5 типам билетов male/female/pair_mf/pair_ff/pair_mm, FK ON DELETE CASCADE). Веб-раздел `/events` (`webadmin/events.py` + `events_repo.py`, шаблоны `events_list.html`/`event_form.html`): список-расписание (предстоящие/прошедшие) с бейджами типа, ценами, местами, статусом показа; форма создания/редактирования с серверной валидацией (нет цен → ошибка, скидка 0–100%, целые числа); удаление с подтверждением. Общие зависимости вынесены в `webadmin/deps.py`. Smoke (9 проверок) + живая проверка заказчиком в браузере. По фидбэку заказчика пофикшена вёрстка: центрирование `.btn`/`.badge` (inline-flex), колонка «Показ» nowrap, блок цен с `max-width`. Подтверждено («да, ок вроде всё»). Баг «кракозябры» в названии оказался только в демо-данных, вставленных через консоль (ввод из браузера — корректный UTF-8). На ветке `dev`.

- **2026-06-14 — Этап 10: Перенос на сервер с доступом к ЮKassa + автодеплой — tag `stage-10-done`** — прод поднят на новом сервере `163.5.153.150` (Ubuntu 24.04, x86_64, 1 CPU / ~1 ГБ RAM + swap 2 ГБ), с которого `api.yookassa.ru` доступен напрямую (авторизованный запрос — HTTP 200 за 0.29 с, с хоста и из контейнера); прокси `YOOKASSA_PROXY` (добавлялся для старого немецкого сервера) на новом НЕ используется. Развёрнуто okdeploy-процедурой: deploy-ключ в `authorized_keys`, swap, Docker 29 + Compose v5, clone `master`, боевой `.env` (без прокси, боевые интервалы: поллер 1 мин, окончания 30 мин, напоминания 60 мин 3/1/0 day), `docker compose up -d --build`. Миграции 0001→0009 применены, бот `@NEIRO_BRAIN01_BOT` в polling без конфликтов. Старый сервер `72.35.242.127` недоступен (ping не проходит, deploy-ключ отклонён — VPS пересоздана/удалена), конфликта `getUpdates` за токен нет. Автодеплой переключён: заказчик обновил GitHub Secret `SERVER_HOST` на новый IP (ключ тот же), проверочный push в `master` (bump 0.1.2) автоматически выкатился — сервер перемотался на `8d696c8`, пересобрался, бот стартовал с v0.1.2. Оплата end-to-end (ссылка → тестовая оплата → активация подписки) подтверждена заказчиком вживую («работает»).
- **2026-06-14 — Этап 9: Деплой CI/CD — tag `stage-9-done`** — деплой-артефакты: `scripts/install.sh` (поднятие стека с нуля на чистой Ubuntu — Docker, clone, `.env`, compose со своими pg+redis), `.github/workflows/deploy.yml` (автодеплой по push в `master` через `appleboy/ssh-action`: `git reset --hard origin/master` + `docker compose up -d --build`), `.gitattributes` (`* text=auto eol=lf` — защита от CRLF), README с инструкцией локального запуска. Настроен remote `github.com/dimatolpygin/club_order` (был пустой), `master` синхронизирован с `dev` (fast-forward) и стал веткой автодеплоя. ВАЖНО: первичный деплой этого этапа целился на `72.35.242.127` (HOSTKEY, Германия), но с того сервера не было маршрута до `api.yookassa.ru` — создание платёжной ссылки висло; это и породило Этап 10 (перенос на сервер с доступом к ЮKassa). Живой автодеплой и работающий прод-бот подтверждены в рамках Этапа 10 на сервере `163.5.153.150`. `install.sh` присутствует и соответствует выполненным шагам, но дословно не запускался (bring-up выполнен эквивалентной okdeploy-процедурой).
- **2026-06-13 — Этап 8: Админ-панель `/admin` — tag `stage-8-done`** — `/admin` собран воедино: главный экран — список всех команд с описанием (только админам, `_is_admin` на каждом хендлере). Новое к этапу 8: (1) раздел «Застрявшие в FSM» — список пользователей и их последний шаг сценария из `fsm_states`+users (`repo.get_fsm_stuck`), свежие сверху, с «N назад» и «Обновить»; (2) раздел «Напоминания» — правка единицы и порогов early/soon/last **на лету** через DB-настройки `bot_settings` (миграция 0009, `repo.get_settings/set_setting`, сервис `services.app_settings`), джоб `reminders.run_reminder_check` читает их каждым проходом с fallback на `.env`, без рестарта; (3) статистика дополнена занятыми местами по ступеням. Ранее из этапов 2/7 уже были: ступени/длительности (+удаление), карточка/поиск участника, ручное добавление оплатившего (создаёт активную подписку source='manual'), аннулирование с киком, промокоды. Сверх плана по запросу заказчика: **рассылка с фото и альбомами (media_group)** — фото грузятся в S3 (Beget, `services.storage` на boto3, ACL public-read; проверено: публичный URL отдаёт HTTP 200), рассылаются по ссылке с fallback на `file_id`; альбом собирается по `media_group_id` через debounce-буфер (фикс гонки параллельных апдейтов); статус «загружаю фото…» на время заливки. Конфиг S3 — в `.env` (`settings.s3_enabled`). Верифицировал заказчик вживую («все ок»). На ветке `dev`.
- **2026-06-13 — Этап 7: Промокоды — tag `stage-7-done`** — полноценные промокоды (миграция 0008): таблицы `promo_codes` (тип `fixed_price` спец-ставка ₽/мес | `percent` скидка %; значение; лимит `max_activations`/счётчик `used_count`; срок `expires_at`; флаг `fixes_price`; активность) и `promo_redemptions` (uniq `(promo_id, tg_id)` — один код = одна активация на юзера), плюс `payments.promo_id`. Пользователь: «Ввести промокод» (`handlers/promo.py`, `PromoStates`) → валидация → валидный код открывает экран спец-цены с выбором срока (кнопки уже со спец-суммой) → оплата по промокоду (`payments.start_promo_payment`, платёж с `promo_id`); невалидный код → корректное сообщение (не найден / истёк-отключён / исчерпан / уже использован). Учёт активаций в `repo.activate_payment`: при успешной оплате подписка `source='promo'`, атомарно `promo_redemptions` (`ON CONFLICT DO NOTHING`) + `used_count +1` — лимит соблюдается, дублей нет; опц. фиксация спец-ставки за подпиской. Чистая логика (`services.promo`: `validate` + `compute_pricing` для обоих типов, округление, фиксация) покрыта изолированным smoke-тестом `tests/test_promo.py` (18 проверок). Админ-раздел `/admin` → «Промокоды»: список, пошаговое создание (код→тип→значение→лимит→срок→фиксация), вкл/выкл. Сверх плана по фидбэку заказчика на живом тесте: (1) кнопка «Ввести промокод» на экране тарифов (юзер без подписки); (2) удаление длительностей в админке с подтверждением (`repo.delete_duration` + чистка цен периода) — раздел перестал раздуваться. Промо в продлении пока не показываем (решение отложено заказчиком). Правило «один код — одна активация на юзера» оставлено. Верифицировал заказчик вживую («работает всё»). На ветке `dev`.
- **2026-06-13 — Этап 6: Напоминания (крон 3/1/0 дней) — tag `stage-6-done`** — APScheduler-джоб `services/reminders.run_reminder_check` (интервал `REMINDER_CHECK_INTERVAL_MIN`) проходит по активным подпискам и шлёт до трёх напоминаний по приближении `end_date`: **early** «осталось N» + цена + дата (экран 14), **soon** «завтра заканчивается» + цена (экран 15), **last** «последний день» (экран 16) — каждое со своей клавиатурой (Продлить/Моя подписка/Поддержка). Цена в напоминании = `subscriptions.fixed_price` (зафиксированная ставка). Пороги именованные (early/soon/last), задаются в целых единицах `REMINDER_UNIT` (minute/hour/day/month) настройками `REMINDER_*_OFFSET` (дефолт 3/1/0 day) — «интервалы без правки кода»; тест-режим в минутах как у окончаний (этап 5). Триггер: `целых_единиц_до_конца ≤ порог`. **Защита от дублей двухуровневая**: (1) таблица `subscription_reminders` (миграция 0007, PK `(subscription_id, kind)`, FK CASCADE) + атомарный `repo.claim_reminder` через `ON CONFLICT DO NOTHING`; (2) за один проход шлётся только самое срочное напоминание, «пропущенные» окна короткой подписки помечаются отправленными без рассылки (чистая логика `due_reminder_kinds`, покрыта изолированным smoke-тестом `tests/test_reminders.py` — 7 проверок). Верифицировал заказчик вживую («вроде всё работает»). На ветке `dev`.
- **2026-06-13 — Этап 5: Фиксация цены, продление, авто-удаление — tag `stage-5-done`** — продление (`handlers/payment.py` NAV_RENEW/`renew:`) по зафиксированной ставке: `repo.activate_payment` ветвится по `payments.kind` ('new'/'renewal', миграция 0005), продление **продлевает ту же строку подписки** (`end_date += значение`, `fixed_price` не меняется), idempotent; fallback на новую от now, если активная истекла до подтверждения. Авто-окончание: APScheduler-джоб `services/subscriptions.run_expiry_check` (интервал `EXPIRY_CHECK_INTERVAL_MIN`) → `repo.expire_due_subscriptions` помечает `expired` + кик из группы (`kick_from_group` = ban+unban) + уведомление «подписка закончилась». Сброс зафиксированной цены — следствие `expired` (новая покупка по актуальной ступени). Сверх плана по запросу заказчика: (1) кик из группы при ручном аннулировании в админке (был баг — только статус менялся); (2) **гибкие единицы длительности** (месяц/день/час/минута) — миграция 0006 (`durations.unit`, `tier_prices` по (ступень,значение,единица), снимок `unit` в payments/subscriptions), `utils.add_period`, цена за период задаётся админом per единица (fallback ставка×значение), выбор единицы кнопками в «Добавить длительность» и ручной выдаче; заменил глобальный тест-флаг `SUBSCRIPTION_UNIT`. Верифицировал заказчик вживую (сценарий с минутами: покупка→активна→кик; продление; аннулирование). Логика покрыта изолированными smoke-тестами. На ветке `dev`.
- **2026-06-12 — Этап 4: Доступ в группу (join-request) — tag `stage-4-done`** — хендлер `chat_join_request` (`handlers/join.py`): активная подписка → авто-одобрение заявки + сообщение «Доступ открыт»; нет подписки → отклонение + пояснение «оформи подписку»; если approve упал технически (бот не админ/нет права) → экран «не удалось добавить» в поддержку. Заявки фильтруются по `CLUB_CHAT_ID`. Polling переведён на явные `allowed_updates` через `resolve_used_update_types()` — иначе апдейт `chat_join_request` не приходит (подтверждено логом: `['callback_query', 'chat_join_request', 'message']`). Тексты ACCESS_GRANTED/DENIED/FAILED. Верифицировал заказчик («вроде все работает»). На ветке `dev`.
- **2026-06-12 — Этап 3: Оплата ЮKassa + polling — tag `stage-3-done`** — реальный тестовый платёж через ЮKassa (без вебхука): создание платежа (idempotence key, чек 54-ФЗ через единую `build_receipt(user)` с email из `users.email`/заглушки), сохранение в `payments`, фоновый поллер pending (APScheduler, интервал из настроек) + кнопка «Проверить оплату». Активация подписки атомарна и идемпотентна (`repo.activate_payment` под `SELECT … FOR UPDATE` — повторная оплата/гонка поллер+кнопка не плодят дубль; подтверждено smoke-тестом). Экраны Оплата/Ожидание/Успех/Отмена, «Моя подписка» (статус, User ID, даты, фикс-цена, дни). Сверх плана по запросу заказчика на живом тесте: (1) цена за период — МАТРИЦА ступень×период (миграция 0004 `tier_prices`, админ задаёт абсолютную цену за 3/6/12 мес, иначе fallback ставка×месяцы; за юзером фиксируется месячная ставка для продлений); (2) у оплативших «Вступить в клуб» ведёт на «Мою подписку», кнопка «Перейти в закрытый чат» из `CLUB_CHAT_INVITE_URL`; (3) команда `/sub` + меню команд бота. Заказчик принял по живому тесту («всё ок»). Полностью на ветке `dev`. Этап 4 не начат.
- **2026-06-12 — Этап 2: Тарифы и места — tag `stage-2-done`** — ВАЖНО: по фидбэку заказчика переделана модель цен. Ступени СКРЫТЫ от юзера (внутренний механизм); пользователь видит только длительности (1/3/6/12 мес), помесячная ставка вычисляется автоматически движком `services.tariffs.get_current_tier` по числу занятых мест (кумулятивные брекеты: занято <11 → ступень 1, 12-й участник → ступень 2, …, дальше «Стандарт»). Авто-перескок подтверждён симуляцией на данных (лимит ступени=1 + 1 активная подписка → движок выбирает следующую ступень). Сумма = ставка × месяцы. Админка переведена на пошаговый FSM-сценарий (инлайн-меню → «введите цену/лимит»): правка ступеней/длительностей с инвалидацией Redis-кеша (без рестарта). Сверх плана по запросу заказчика (вынесено из этапа 8): карточка участника (поиск по tg_id, статус подписки/доступа), рассылка по базе без фото (с троттлингом и авто-пометкой заблокировавших), аннулирование подписки из карточки. Заказчик принял; стейт БД сброшен к сид-тарифам по ТЗ. Этап 3 (оплата) НЕ начат — пауза по просьбе заказчика.
- **2026-06-12 — Этап 1: Навигация и инфо-экраны — tag `stage-1-done`** — `/start` показывает приветствие «11:11» с 4 кнопками; экраны «Что внутри»/«Правила»/«Поддержка» открываются с HTML-форматированием, «Назад» работает. Флоу поддержки: тема → ввод текста → обращение уходит всем админам (лог «передано 1 админам», заказчик-админ получил). В `club_bot.fsm_states` пишется текущий экран каждого перехода (подтверждено SQL для двух юзеров: @Polarasing, @hochapuri_hot). Разделы тарифов/оплаты/промокодов — заглушки «скоро» (наполнятся на этапах 2–3,5,7). Верифицировал заказчик по логам и навигации, замечаний нет.
- **2026-06-12 — Этап 0: Каркас репозитория — tag `stage-0-done`** — стек поднялся (`docker compose -f docker-compose.dev.yml up`), Alembic применил миграцию 0001 в схему `club_bot` (таблицы `users`, `fsm_states`, `alembic_version` подтверждены SQL). `/start` отвечает заглушкой, в `club_bot.users` появилась запись `@Polarasing (id 2042819654, Kira)`. Hot-reload через watchfiles работает (правка `.py` → автоперезапуск ~6с). Верифицировал заказчик. На UAT всплыли и починены 2 бага: (1) миграции не коммитились — `CREATE SCHEMA`/`SET search_path` до `begin_transaction` открывали транзакцию раньше Alembic, тот не коммитил → таблицы откатывались; (2) на Windows hot-reload требует `WATCHFILES_FORCE_POLLING=true` (inotify через bind-mount Docker Desktop не работает).

## Протокол обновления

**При старте сессии**:
1. Прочитать этот файл.
2. `git tag -l "stage-*"` и сверить с колонкой «Закрывающий тег». Тег без записи в таблице = таблица устарела, обновить.
3. `git log --oneline -10` — что менялось последним.
4. Только после этого приступать к работе.

**При взятии этапа в работу**:
1. Обновить «Текущий этап» и «Активная работа».
2. Статус 🚧 в таблице.
3. Закоммитить отдельно: `chore(status): start stage N`.

**При закрытии этапа**:
1. Убедиться, что все чекбоксы в `07_ROADMAP.md` отмечены `[x]` и реально проверены.
2. Создать тег: `git tag stage-N-done -m "<описание>"`.
3. В таблице: ✅, «Закрывающий тег», «Последний коммит» (`git rev-parse --short HEAD`), «Дата закрытия».
4. Добавить запись в «Историю закрытий».
5. Указать новый «Текущий этап» и «Следующий шаг».
6. Закоммитить: `chore(status): close stage N`.

**Если этап заблокирован**:
1. Статус ⏸ в таблице.
2. В «Известные блокеры»: что блокирует, кого ждём, что нужно для разблокировки.

## Что НЕ хранится здесь

- Длинные размышления и планы — это `07_ROADMAP.md` и спецификации в `docs/`.
- TODO внутри текущего этапа — это таск-лист активной сессии.
- История изменений кода — это `git log`.

Этот файл — **только снимок текущего состояния по этапам**. Должен помещаться на один экран.
